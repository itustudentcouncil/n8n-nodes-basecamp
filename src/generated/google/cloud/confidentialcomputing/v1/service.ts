// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.0
//   protoc               unknown
// source: google/cloud/confidentialcomputing/v1/service.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";
import { type CallContext, type CallOptions } from "nice-grpc-common";
import { Timestamp } from "../../../protobuf/timestamp.js";
import { Status } from "../../../rpc/status.js";

export const protobufPackage = "google.cloud.confidentialcomputing.v1";

/** SigningAlgorithm enumerates all the supported signing algorithms. */
export enum SigningAlgorithm {
  /** SIGNING_ALGORITHM_UNSPECIFIED - Unspecified signing algorithm. */
  SIGNING_ALGORITHM_UNSPECIFIED = 0,
  /** RSASSA_PSS_SHA256 - RSASSA-PSS with a SHA256 digest. */
  RSASSA_PSS_SHA256 = 1,
  /** RSASSA_PKCS1V15_SHA256 - RSASSA-PKCS1 v1.5 with a SHA256 digest. */
  RSASSA_PKCS1V15_SHA256 = 2,
  /** ECDSA_P256_SHA256 - ECDSA on the P-256 Curve with a SHA256 digest. */
  ECDSA_P256_SHA256 = 3,
  UNRECOGNIZED = -1,
}

export function signingAlgorithmFromJSON(object: any): SigningAlgorithm {
  switch (object) {
    case 0:
    case "SIGNING_ALGORITHM_UNSPECIFIED":
      return SigningAlgorithm.SIGNING_ALGORITHM_UNSPECIFIED;
    case 1:
    case "RSASSA_PSS_SHA256":
      return SigningAlgorithm.RSASSA_PSS_SHA256;
    case 2:
    case "RSASSA_PKCS1V15_SHA256":
      return SigningAlgorithm.RSASSA_PKCS1V15_SHA256;
    case 3:
    case "ECDSA_P256_SHA256":
      return SigningAlgorithm.ECDSA_P256_SHA256;
    case -1:
    case "UNRECOGNIZED":
    default:
      return SigningAlgorithm.UNRECOGNIZED;
  }
}

export function signingAlgorithmToJSON(object: SigningAlgorithm): string {
  switch (object) {
    case SigningAlgorithm.SIGNING_ALGORITHM_UNSPECIFIED:
      return "SIGNING_ALGORITHM_UNSPECIFIED";
    case SigningAlgorithm.RSASSA_PSS_SHA256:
      return "RSASSA_PSS_SHA256";
    case SigningAlgorithm.RSASSA_PKCS1V15_SHA256:
      return "RSASSA_PKCS1V15_SHA256";
    case SigningAlgorithm.ECDSA_P256_SHA256:
      return "ECDSA_P256_SHA256";
    case SigningAlgorithm.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/**
 * Token type enum contains the different types of token responses Confidential
 * Space supports
 */
export enum TokenType {
  /** TOKEN_TYPE_UNSPECIFIED - Unspecified token type */
  TOKEN_TYPE_UNSPECIFIED = 0,
  /** TOKEN_TYPE_OIDC - OpenID Connect (OIDC) token type */
  TOKEN_TYPE_OIDC = 1,
  /** TOKEN_TYPE_PKI - Public Key Infrastructure (PKI) token type */
  TOKEN_TYPE_PKI = 2,
  /** TOKEN_TYPE_LIMITED_AWS - Limited claim token type for AWS integration */
  TOKEN_TYPE_LIMITED_AWS = 3,
  UNRECOGNIZED = -1,
}

export function tokenTypeFromJSON(object: any): TokenType {
  switch (object) {
    case 0:
    case "TOKEN_TYPE_UNSPECIFIED":
      return TokenType.TOKEN_TYPE_UNSPECIFIED;
    case 1:
    case "TOKEN_TYPE_OIDC":
      return TokenType.TOKEN_TYPE_OIDC;
    case 2:
    case "TOKEN_TYPE_PKI":
      return TokenType.TOKEN_TYPE_PKI;
    case 3:
    case "TOKEN_TYPE_LIMITED_AWS":
      return TokenType.TOKEN_TYPE_LIMITED_AWS;
    case -1:
    case "UNRECOGNIZED":
    default:
      return TokenType.UNRECOGNIZED;
  }
}

export function tokenTypeToJSON(object: TokenType): string {
  switch (object) {
    case TokenType.TOKEN_TYPE_UNSPECIFIED:
      return "TOKEN_TYPE_UNSPECIFIED";
    case TokenType.TOKEN_TYPE_OIDC:
      return "TOKEN_TYPE_OIDC";
    case TokenType.TOKEN_TYPE_PKI:
      return "TOKEN_TYPE_PKI";
    case TokenType.TOKEN_TYPE_LIMITED_AWS:
      return "TOKEN_TYPE_LIMITED_AWS";
    case TokenType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** A Challenge from the server used to guarantee freshness of attestations */
export interface Challenge {
  /**
   * Output only. The resource name for this Challenge in the format
   * `projects/* /locations/* /challenges/*`
   */
  name: string;
  /** Output only. The time at which this Challenge was created */
  createTime:
    | Date
    | undefined;
  /**
   * Output only. The time at which this Challenge will no longer be usable. It
   * is also the expiration time for any tokens generated from this Challenge.
   */
  expireTime:
    | Date
    | undefined;
  /** Output only. Indicates if this challenge has been used to generate a token. */
  used: boolean;
  /** Output only. Identical to nonce, but as a string. */
  tpmNonce: string;
}

/** Message for creating a Challenge */
export interface CreateChallengeRequest {
  /**
   * Required. The resource name of the location where the Challenge will be
   * used, in the format `projects/* /locations/*`.
   */
  parent: string;
  /**
   * Required. The Challenge to be created. Currently this field can be empty as
   * all the Challenge fields are set by the server.
   */
  challenge: Challenge | undefined;
}

/**
 * A request for an OIDC token, providing all the necessary information needed
 * for this service to verify the plaform state of the requestor.
 */
export interface VerifyAttestationRequest {
  /** Optional. A TDX with CCEL and RTMR Attestation Quote. */
  tdCcel?:
    | TdxCcelAttestation
    | undefined;
  /** Optional. An SEV-SNP Attestation Report. */
  sevSnpAttestation?:
    | SevSnpAttestation
    | undefined;
  /**
   * Required. The name of the Challenge whose nonce was used to generate the
   * attestation, in the format `projects/* /locations/* /challenges/*`. The
   * provided Challenge will be consumed, and cannot be used again.
   */
  challenge: string;
  /**
   * Optional. Credentials used to populate the "emails" claim in the
   * claims_token.
   */
  gcpCredentials:
    | GcpCredentials
    | undefined;
  /**
   * Required. The TPM-specific data provided by the attesting platform, used to
   * populate any of the claims regarding platform state.
   */
  tpmAttestation:
    | TpmAttestation
    | undefined;
  /** Optional. Optional information related to the Confidential Space TEE. */
  confidentialSpaceInfo:
    | ConfidentialSpaceInfo
    | undefined;
  /**
   * Optional. A collection of optional, workload-specified claims that modify
   * the token output.
   */
  tokenOptions: TokenOptions | undefined;
}

/** A TDX Attestation quote. */
export interface TdxCcelAttestation {
  /**
   * Optional. The Confidential Computing Event Log (CCEL) ACPI table. Formatted
   * as described in the ACPI Specification 6.5.
   */
  ccelAcpiTable: Buffer;
  /** Optional. The CCEL event log. Formatted as described in the UEFI 2.10. */
  ccelData: Buffer;
  /**
   * Optional. An Event Log containing additional events measured into the RTMR
   * that are not already present in the CCEL.
   */
  canonicalEventLog: Buffer;
  /**
   * Optional. The TDX attestation quote from the guest. It contains the RTMR
   * values.
   */
  tdQuote: Buffer;
}

/**
 * An SEV-SNP Attestation Report.
 * Contains the attestation report and the certificate bundle that the client
 * collects.
 */
export interface SevSnpAttestation {
  /**
   * Optional. The SEV-SNP Attestation Report
   * Format is in revision 1.55, §7.3 Attestation, Table 22. ATTESTATION_REPORT
   * Structure in this document:
   * https://www.amd.com/content/dam/amd/en/documents/epyc-technical-docs/specifications/56860.pdf
   */
  report: Buffer;
  /**
   * Optional. Certificate bundle defined in the GHCB protocol definition
   * Format is documented in GHCB revision 2.03, section 4.1.8.1 struct
   * cert_table in this document:
   * https://www.amd.com/content/dam/amd/en/documents/epyc-technical-docs/specifications/56421.pdf
   */
  auxBlob: Buffer;
}

/**
 * A response once an attestation has been successfully verified, containing a
 * signed OIDC token.
 */
export interface VerifyAttestationResponse {
  /** Output only. Same as claims_token, but as a string. */
  oidcClaimsToken: string;
  /**
   * Output only. A list of messages that carry the partial error details
   * related to VerifyAttestation.
   */
  partialErrors: Status[];
}

/**
 * Credentials issued by GCP which are linked to the platform attestation. These
 * will be verified server-side as part of attestaion verification.
 */
export interface GcpCredentials {
  /** Same as id_tokens, but as a string. */
  serviceAccountIdTokens: string[];
}

/** Options to modify claims in the token to generate custom-purpose tokens. */
export interface TokenOptions {
  /**
   * Optional. Optional string to issue the token with a custom audience claim.
   * Required if one or more nonces are specified.
   */
  audience: string;
  /**
   * Optional. Optional parameter to place one or more nonces in the eat_nonce
   * claim in the output token. The minimum size for JSON-encoded EATs is 10
   * bytes and the maximum size is 74 bytes.
   */
  nonce: string[];
  /** Optional. Optional token type to select what type of token to return. */
  tokenType: TokenType;
}

/**
 * TPM2 data containing everything necessary to validate any platform state
 * measured into the TPM.
 */
export interface TpmAttestation {
  /** TPM2 PCR Quotes generated by calling TPM2_Quote on each PCR bank. */
  quotes: TpmAttestation_Quote[];
  /**
   * The binary TCG Event Log containing events measured into the TPM by the
   * platform firmware and operating system. Formatted as described in the
   * "TCG PC Client Platform Firmware Profile Specification".
   */
  tcgEventLog: Buffer;
  /**
   * An Event Log containing additional events measured into the TPM that are
   * not already present in the tcg_event_log. Formatted as described in the
   * "Canonical Event Log Format" TCG Specification.
   */
  canonicalEventLog: Buffer;
  /**
   * DER-encoded X.509 certificate of the Attestation Key (otherwise known as
   * an AK or a TPM restricted signing key) used to generate the quotes.
   */
  akCert: Buffer;
  /**
   * List of DER-encoded X.509 certificates which, together with the ak_cert,
   * chain back to a trusted Root Certificate.
   */
  certChain: Buffer[];
}

/**
 * Information about Platform Control Registers (PCRs) including a signature
 * over their values, which can be used for remote validation.
 */
export interface TpmAttestation_Quote {
  /** The hash algorithm of the PCR bank being quoted, encoded as a TPM_ALG_ID */
  hashAlgo: number;
  /** Raw binary values of each PCRs being quoted. */
  pcrValues: { [key: number]: Buffer };
  /** TPM2 quote, encoded as a TPMS_ATTEST */
  rawQuote: Buffer;
  /** TPM2 signature, encoded as a TPMT_SIGNATURE */
  rawSignature: Buffer;
}

export interface TpmAttestation_Quote_PcrValuesEntry {
  key: number;
  value: Buffer;
}

/**
 * ConfidentialSpaceInfo contains information related to the Confidential Space
 * TEE.
 */
export interface ConfidentialSpaceInfo {
  /**
   * Optional. A list of signed entities containing container image signatures
   * that can be used for server-side signature verification.
   */
  signedEntities: SignedEntity[];
}

/**
 * SignedEntity represents an OCI image object containing everything necessary
 * to verify container image signatures.
 */
export interface SignedEntity {
  /**
   * Optional. A list of container image signatures attached to an OCI image
   * object.
   */
  containerImageSignatures: ContainerImageSignature[];
}

/**
 * ContainerImageSignature holds necessary metadata to verify a container image
 * signature.
 */
export interface ContainerImageSignature {
  /**
   * Optional. The binary signature payload following the SimpleSigning format
   * https://github.com/sigstore/cosign/blob/main/specs/SIGNATURE_SPEC.md#simple-signing.
   * This payload includes the container image digest.
   */
  payload: Buffer;
  /**
   * Optional. A signature over the payload.
   * The container image digest is incorporated into the signature as follows:
   * 1. Generate a SimpleSigning format payload that includes the container
   * image digest.
   * 2. Generate a signature over SHA256 digest of the payload.
   * The signature generation process can be represented as follows:
   * `Sign(sha256(SimpleSigningPayload(sha256(Image Manifest))))`
   */
  signature: Buffer;
  /** Optional. Reserved for future use. */
  publicKey: Buffer;
  /** Optional. Reserved for future use. */
  sigAlg: SigningAlgorithm;
}

function createBaseChallenge(): Challenge {
  return { name: "", createTime: undefined, expireTime: undefined, used: false, tpmNonce: "" };
}

export const Challenge: MessageFns<Challenge> = {
  encode(message: Challenge, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.createTime !== undefined) {
      Timestamp.encode(toTimestamp(message.createTime), writer.uint32(18).fork()).join();
    }
    if (message.expireTime !== undefined) {
      Timestamp.encode(toTimestamp(message.expireTime), writer.uint32(26).fork()).join();
    }
    if (message.used !== false) {
      writer.uint32(32).bool(message.used);
    }
    if (message.tpmNonce !== "") {
      writer.uint32(50).string(message.tpmNonce);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Challenge {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseChallenge();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.createTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.expireTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.used = reader.bool();
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.tpmNonce = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Challenge {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      createTime: isSet(object.createTime) ? fromJsonTimestamp(object.createTime) : undefined,
      expireTime: isSet(object.expireTime) ? fromJsonTimestamp(object.expireTime) : undefined,
      used: isSet(object.used) ? globalThis.Boolean(object.used) : false,
      tpmNonce: isSet(object.tpmNonce) ? globalThis.String(object.tpmNonce) : "",
    };
  },

  toJSON(message: Challenge): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.createTime !== undefined) {
      obj.createTime = message.createTime.toISOString();
    }
    if (message.expireTime !== undefined) {
      obj.expireTime = message.expireTime.toISOString();
    }
    if (message.used !== false) {
      obj.used = message.used;
    }
    if (message.tpmNonce !== "") {
      obj.tpmNonce = message.tpmNonce;
    }
    return obj;
  },

  create(base?: DeepPartial<Challenge>): Challenge {
    return Challenge.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Challenge>): Challenge {
    const message = createBaseChallenge();
    message.name = object.name ?? "";
    message.createTime = object.createTime ?? undefined;
    message.expireTime = object.expireTime ?? undefined;
    message.used = object.used ?? false;
    message.tpmNonce = object.tpmNonce ?? "";
    return message;
  },
};

function createBaseCreateChallengeRequest(): CreateChallengeRequest {
  return { parent: "", challenge: undefined };
}

export const CreateChallengeRequest: MessageFns<CreateChallengeRequest> = {
  encode(message: CreateChallengeRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.parent !== "") {
      writer.uint32(10).string(message.parent);
    }
    if (message.challenge !== undefined) {
      Challenge.encode(message.challenge, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateChallengeRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateChallengeRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.parent = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.challenge = Challenge.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateChallengeRequest {
    return {
      parent: isSet(object.parent) ? globalThis.String(object.parent) : "",
      challenge: isSet(object.challenge) ? Challenge.fromJSON(object.challenge) : undefined,
    };
  },

  toJSON(message: CreateChallengeRequest): unknown {
    const obj: any = {};
    if (message.parent !== "") {
      obj.parent = message.parent;
    }
    if (message.challenge !== undefined) {
      obj.challenge = Challenge.toJSON(message.challenge);
    }
    return obj;
  },

  create(base?: DeepPartial<CreateChallengeRequest>): CreateChallengeRequest {
    return CreateChallengeRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CreateChallengeRequest>): CreateChallengeRequest {
    const message = createBaseCreateChallengeRequest();
    message.parent = object.parent ?? "";
    message.challenge = (object.challenge !== undefined && object.challenge !== null)
      ? Challenge.fromPartial(object.challenge)
      : undefined;
    return message;
  },
};

function createBaseVerifyAttestationRequest(): VerifyAttestationRequest {
  return {
    tdCcel: undefined,
    sevSnpAttestation: undefined,
    challenge: "",
    gcpCredentials: undefined,
    tpmAttestation: undefined,
    confidentialSpaceInfo: undefined,
    tokenOptions: undefined,
  };
}

export const VerifyAttestationRequest: MessageFns<VerifyAttestationRequest> = {
  encode(message: VerifyAttestationRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.tdCcel !== undefined) {
      TdxCcelAttestation.encode(message.tdCcel, writer.uint32(50).fork()).join();
    }
    if (message.sevSnpAttestation !== undefined) {
      SevSnpAttestation.encode(message.sevSnpAttestation, writer.uint32(58).fork()).join();
    }
    if (message.challenge !== "") {
      writer.uint32(10).string(message.challenge);
    }
    if (message.gcpCredentials !== undefined) {
      GcpCredentials.encode(message.gcpCredentials, writer.uint32(18).fork()).join();
    }
    if (message.tpmAttestation !== undefined) {
      TpmAttestation.encode(message.tpmAttestation, writer.uint32(26).fork()).join();
    }
    if (message.confidentialSpaceInfo !== undefined) {
      ConfidentialSpaceInfo.encode(message.confidentialSpaceInfo, writer.uint32(34).fork()).join();
    }
    if (message.tokenOptions !== undefined) {
      TokenOptions.encode(message.tokenOptions, writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VerifyAttestationRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVerifyAttestationRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 6:
          if (tag !== 50) {
            break;
          }

          message.tdCcel = TdxCcelAttestation.decode(reader, reader.uint32());
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.sevSnpAttestation = SevSnpAttestation.decode(reader, reader.uint32());
          continue;
        case 1:
          if (tag !== 10) {
            break;
          }

          message.challenge = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.gcpCredentials = GcpCredentials.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.tpmAttestation = TpmAttestation.decode(reader, reader.uint32());
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.confidentialSpaceInfo = ConfidentialSpaceInfo.decode(reader, reader.uint32());
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.tokenOptions = TokenOptions.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VerifyAttestationRequest {
    return {
      tdCcel: isSet(object.tdCcel) ? TdxCcelAttestation.fromJSON(object.tdCcel) : undefined,
      sevSnpAttestation: isSet(object.sevSnpAttestation)
        ? SevSnpAttestation.fromJSON(object.sevSnpAttestation)
        : undefined,
      challenge: isSet(object.challenge) ? globalThis.String(object.challenge) : "",
      gcpCredentials: isSet(object.gcpCredentials) ? GcpCredentials.fromJSON(object.gcpCredentials) : undefined,
      tpmAttestation: isSet(object.tpmAttestation) ? TpmAttestation.fromJSON(object.tpmAttestation) : undefined,
      confidentialSpaceInfo: isSet(object.confidentialSpaceInfo)
        ? ConfidentialSpaceInfo.fromJSON(object.confidentialSpaceInfo)
        : undefined,
      tokenOptions: isSet(object.tokenOptions) ? TokenOptions.fromJSON(object.tokenOptions) : undefined,
    };
  },

  toJSON(message: VerifyAttestationRequest): unknown {
    const obj: any = {};
    if (message.tdCcel !== undefined) {
      obj.tdCcel = TdxCcelAttestation.toJSON(message.tdCcel);
    }
    if (message.sevSnpAttestation !== undefined) {
      obj.sevSnpAttestation = SevSnpAttestation.toJSON(message.sevSnpAttestation);
    }
    if (message.challenge !== "") {
      obj.challenge = message.challenge;
    }
    if (message.gcpCredentials !== undefined) {
      obj.gcpCredentials = GcpCredentials.toJSON(message.gcpCredentials);
    }
    if (message.tpmAttestation !== undefined) {
      obj.tpmAttestation = TpmAttestation.toJSON(message.tpmAttestation);
    }
    if (message.confidentialSpaceInfo !== undefined) {
      obj.confidentialSpaceInfo = ConfidentialSpaceInfo.toJSON(message.confidentialSpaceInfo);
    }
    if (message.tokenOptions !== undefined) {
      obj.tokenOptions = TokenOptions.toJSON(message.tokenOptions);
    }
    return obj;
  },

  create(base?: DeepPartial<VerifyAttestationRequest>): VerifyAttestationRequest {
    return VerifyAttestationRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<VerifyAttestationRequest>): VerifyAttestationRequest {
    const message = createBaseVerifyAttestationRequest();
    message.tdCcel = (object.tdCcel !== undefined && object.tdCcel !== null)
      ? TdxCcelAttestation.fromPartial(object.tdCcel)
      : undefined;
    message.sevSnpAttestation = (object.sevSnpAttestation !== undefined && object.sevSnpAttestation !== null)
      ? SevSnpAttestation.fromPartial(object.sevSnpAttestation)
      : undefined;
    message.challenge = object.challenge ?? "";
    message.gcpCredentials = (object.gcpCredentials !== undefined && object.gcpCredentials !== null)
      ? GcpCredentials.fromPartial(object.gcpCredentials)
      : undefined;
    message.tpmAttestation = (object.tpmAttestation !== undefined && object.tpmAttestation !== null)
      ? TpmAttestation.fromPartial(object.tpmAttestation)
      : undefined;
    message.confidentialSpaceInfo =
      (object.confidentialSpaceInfo !== undefined && object.confidentialSpaceInfo !== null)
        ? ConfidentialSpaceInfo.fromPartial(object.confidentialSpaceInfo)
        : undefined;
    message.tokenOptions = (object.tokenOptions !== undefined && object.tokenOptions !== null)
      ? TokenOptions.fromPartial(object.tokenOptions)
      : undefined;
    return message;
  },
};

function createBaseTdxCcelAttestation(): TdxCcelAttestation {
  return {
    ccelAcpiTable: Buffer.alloc(0),
    ccelData: Buffer.alloc(0),
    canonicalEventLog: Buffer.alloc(0),
    tdQuote: Buffer.alloc(0),
  };
}

export const TdxCcelAttestation: MessageFns<TdxCcelAttestation> = {
  encode(message: TdxCcelAttestation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.ccelAcpiTable.length !== 0) {
      writer.uint32(10).bytes(message.ccelAcpiTable);
    }
    if (message.ccelData.length !== 0) {
      writer.uint32(18).bytes(message.ccelData);
    }
    if (message.canonicalEventLog.length !== 0) {
      writer.uint32(26).bytes(message.canonicalEventLog);
    }
    if (message.tdQuote.length !== 0) {
      writer.uint32(34).bytes(message.tdQuote);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TdxCcelAttestation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTdxCcelAttestation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.ccelAcpiTable = Buffer.from(reader.bytes());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.ccelData = Buffer.from(reader.bytes());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.canonicalEventLog = Buffer.from(reader.bytes());
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.tdQuote = Buffer.from(reader.bytes());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TdxCcelAttestation {
    return {
      ccelAcpiTable: isSet(object.ccelAcpiTable) ? Buffer.from(bytesFromBase64(object.ccelAcpiTable)) : Buffer.alloc(0),
      ccelData: isSet(object.ccelData) ? Buffer.from(bytesFromBase64(object.ccelData)) : Buffer.alloc(0),
      canonicalEventLog: isSet(object.canonicalEventLog)
        ? Buffer.from(bytesFromBase64(object.canonicalEventLog))
        : Buffer.alloc(0),
      tdQuote: isSet(object.tdQuote) ? Buffer.from(bytesFromBase64(object.tdQuote)) : Buffer.alloc(0),
    };
  },

  toJSON(message: TdxCcelAttestation): unknown {
    const obj: any = {};
    if (message.ccelAcpiTable.length !== 0) {
      obj.ccelAcpiTable = base64FromBytes(message.ccelAcpiTable);
    }
    if (message.ccelData.length !== 0) {
      obj.ccelData = base64FromBytes(message.ccelData);
    }
    if (message.canonicalEventLog.length !== 0) {
      obj.canonicalEventLog = base64FromBytes(message.canonicalEventLog);
    }
    if (message.tdQuote.length !== 0) {
      obj.tdQuote = base64FromBytes(message.tdQuote);
    }
    return obj;
  },

  create(base?: DeepPartial<TdxCcelAttestation>): TdxCcelAttestation {
    return TdxCcelAttestation.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TdxCcelAttestation>): TdxCcelAttestation {
    const message = createBaseTdxCcelAttestation();
    message.ccelAcpiTable = object.ccelAcpiTable ?? Buffer.alloc(0);
    message.ccelData = object.ccelData ?? Buffer.alloc(0);
    message.canonicalEventLog = object.canonicalEventLog ?? Buffer.alloc(0);
    message.tdQuote = object.tdQuote ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseSevSnpAttestation(): SevSnpAttestation {
  return { report: Buffer.alloc(0), auxBlob: Buffer.alloc(0) };
}

export const SevSnpAttestation: MessageFns<SevSnpAttestation> = {
  encode(message: SevSnpAttestation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.report.length !== 0) {
      writer.uint32(10).bytes(message.report);
    }
    if (message.auxBlob.length !== 0) {
      writer.uint32(18).bytes(message.auxBlob);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SevSnpAttestation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSevSnpAttestation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.report = Buffer.from(reader.bytes());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.auxBlob = Buffer.from(reader.bytes());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SevSnpAttestation {
    return {
      report: isSet(object.report) ? Buffer.from(bytesFromBase64(object.report)) : Buffer.alloc(0),
      auxBlob: isSet(object.auxBlob) ? Buffer.from(bytesFromBase64(object.auxBlob)) : Buffer.alloc(0),
    };
  },

  toJSON(message: SevSnpAttestation): unknown {
    const obj: any = {};
    if (message.report.length !== 0) {
      obj.report = base64FromBytes(message.report);
    }
    if (message.auxBlob.length !== 0) {
      obj.auxBlob = base64FromBytes(message.auxBlob);
    }
    return obj;
  },

  create(base?: DeepPartial<SevSnpAttestation>): SevSnpAttestation {
    return SevSnpAttestation.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SevSnpAttestation>): SevSnpAttestation {
    const message = createBaseSevSnpAttestation();
    message.report = object.report ?? Buffer.alloc(0);
    message.auxBlob = object.auxBlob ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseVerifyAttestationResponse(): VerifyAttestationResponse {
  return { oidcClaimsToken: "", partialErrors: [] };
}

export const VerifyAttestationResponse: MessageFns<VerifyAttestationResponse> = {
  encode(message: VerifyAttestationResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.oidcClaimsToken !== "") {
      writer.uint32(18).string(message.oidcClaimsToken);
    }
    for (const v of message.partialErrors) {
      Status.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VerifyAttestationResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVerifyAttestationResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 2:
          if (tag !== 18) {
            break;
          }

          message.oidcClaimsToken = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.partialErrors.push(Status.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VerifyAttestationResponse {
    return {
      oidcClaimsToken: isSet(object.oidcClaimsToken) ? globalThis.String(object.oidcClaimsToken) : "",
      partialErrors: globalThis.Array.isArray(object?.partialErrors)
        ? object.partialErrors.map((e: any) => Status.fromJSON(e))
        : [],
    };
  },

  toJSON(message: VerifyAttestationResponse): unknown {
    const obj: any = {};
    if (message.oidcClaimsToken !== "") {
      obj.oidcClaimsToken = message.oidcClaimsToken;
    }
    if (message.partialErrors?.length) {
      obj.partialErrors = message.partialErrors.map((e) => Status.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<VerifyAttestationResponse>): VerifyAttestationResponse {
    return VerifyAttestationResponse.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<VerifyAttestationResponse>): VerifyAttestationResponse {
    const message = createBaseVerifyAttestationResponse();
    message.oidcClaimsToken = object.oidcClaimsToken ?? "";
    message.partialErrors = object.partialErrors?.map((e) => Status.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGcpCredentials(): GcpCredentials {
  return { serviceAccountIdTokens: [] };
}

export const GcpCredentials: MessageFns<GcpCredentials> = {
  encode(message: GcpCredentials, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.serviceAccountIdTokens) {
      writer.uint32(18).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GcpCredentials {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGcpCredentials();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 2:
          if (tag !== 18) {
            break;
          }

          message.serviceAccountIdTokens.push(reader.string());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GcpCredentials {
    return {
      serviceAccountIdTokens: globalThis.Array.isArray(object?.serviceAccountIdTokens)
        ? object.serviceAccountIdTokens.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: GcpCredentials): unknown {
    const obj: any = {};
    if (message.serviceAccountIdTokens?.length) {
      obj.serviceAccountIdTokens = message.serviceAccountIdTokens;
    }
    return obj;
  },

  create(base?: DeepPartial<GcpCredentials>): GcpCredentials {
    return GcpCredentials.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GcpCredentials>): GcpCredentials {
    const message = createBaseGcpCredentials();
    message.serviceAccountIdTokens = object.serviceAccountIdTokens?.map((e) => e) || [];
    return message;
  },
};

function createBaseTokenOptions(): TokenOptions {
  return { audience: "", nonce: [], tokenType: 0 };
}

export const TokenOptions: MessageFns<TokenOptions> = {
  encode(message: TokenOptions, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.audience !== "") {
      writer.uint32(10).string(message.audience);
    }
    for (const v of message.nonce) {
      writer.uint32(18).string(v!);
    }
    if (message.tokenType !== 0) {
      writer.uint32(24).int32(message.tokenType);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TokenOptions {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTokenOptions();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.audience = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.nonce.push(reader.string());
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.tokenType = reader.int32() as any;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TokenOptions {
    return {
      audience: isSet(object.audience) ? globalThis.String(object.audience) : "",
      nonce: globalThis.Array.isArray(object?.nonce) ? object.nonce.map((e: any) => globalThis.String(e)) : [],
      tokenType: isSet(object.tokenType) ? tokenTypeFromJSON(object.tokenType) : 0,
    };
  },

  toJSON(message: TokenOptions): unknown {
    const obj: any = {};
    if (message.audience !== "") {
      obj.audience = message.audience;
    }
    if (message.nonce?.length) {
      obj.nonce = message.nonce;
    }
    if (message.tokenType !== 0) {
      obj.tokenType = tokenTypeToJSON(message.tokenType);
    }
    return obj;
  },

  create(base?: DeepPartial<TokenOptions>): TokenOptions {
    return TokenOptions.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TokenOptions>): TokenOptions {
    const message = createBaseTokenOptions();
    message.audience = object.audience ?? "";
    message.nonce = object.nonce?.map((e) => e) || [];
    message.tokenType = object.tokenType ?? 0;
    return message;
  },
};

function createBaseTpmAttestation(): TpmAttestation {
  return {
    quotes: [],
    tcgEventLog: Buffer.alloc(0),
    canonicalEventLog: Buffer.alloc(0),
    akCert: Buffer.alloc(0),
    certChain: [],
  };
}

export const TpmAttestation: MessageFns<TpmAttestation> = {
  encode(message: TpmAttestation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.quotes) {
      TpmAttestation_Quote.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.tcgEventLog.length !== 0) {
      writer.uint32(18).bytes(message.tcgEventLog);
    }
    if (message.canonicalEventLog.length !== 0) {
      writer.uint32(26).bytes(message.canonicalEventLog);
    }
    if (message.akCert.length !== 0) {
      writer.uint32(34).bytes(message.akCert);
    }
    for (const v of message.certChain) {
      writer.uint32(42).bytes(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TpmAttestation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTpmAttestation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.quotes.push(TpmAttestation_Quote.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.tcgEventLog = Buffer.from(reader.bytes());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.canonicalEventLog = Buffer.from(reader.bytes());
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.akCert = Buffer.from(reader.bytes());
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.certChain.push(Buffer.from(reader.bytes()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TpmAttestation {
    return {
      quotes: globalThis.Array.isArray(object?.quotes)
        ? object.quotes.map((e: any) => TpmAttestation_Quote.fromJSON(e))
        : [],
      tcgEventLog: isSet(object.tcgEventLog) ? Buffer.from(bytesFromBase64(object.tcgEventLog)) : Buffer.alloc(0),
      canonicalEventLog: isSet(object.canonicalEventLog)
        ? Buffer.from(bytesFromBase64(object.canonicalEventLog))
        : Buffer.alloc(0),
      akCert: isSet(object.akCert) ? Buffer.from(bytesFromBase64(object.akCert)) : Buffer.alloc(0),
      certChain: globalThis.Array.isArray(object?.certChain)
        ? object.certChain.map((e: any) => Buffer.from(bytesFromBase64(e)))
        : [],
    };
  },

  toJSON(message: TpmAttestation): unknown {
    const obj: any = {};
    if (message.quotes?.length) {
      obj.quotes = message.quotes.map((e) => TpmAttestation_Quote.toJSON(e));
    }
    if (message.tcgEventLog.length !== 0) {
      obj.tcgEventLog = base64FromBytes(message.tcgEventLog);
    }
    if (message.canonicalEventLog.length !== 0) {
      obj.canonicalEventLog = base64FromBytes(message.canonicalEventLog);
    }
    if (message.akCert.length !== 0) {
      obj.akCert = base64FromBytes(message.akCert);
    }
    if (message.certChain?.length) {
      obj.certChain = message.certChain.map((e) => base64FromBytes(e));
    }
    return obj;
  },

  create(base?: DeepPartial<TpmAttestation>): TpmAttestation {
    return TpmAttestation.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TpmAttestation>): TpmAttestation {
    const message = createBaseTpmAttestation();
    message.quotes = object.quotes?.map((e) => TpmAttestation_Quote.fromPartial(e)) || [];
    message.tcgEventLog = object.tcgEventLog ?? Buffer.alloc(0);
    message.canonicalEventLog = object.canonicalEventLog ?? Buffer.alloc(0);
    message.akCert = object.akCert ?? Buffer.alloc(0);
    message.certChain = object.certChain?.map((e) => e) || [];
    return message;
  },
};

function createBaseTpmAttestation_Quote(): TpmAttestation_Quote {
  return { hashAlgo: 0, pcrValues: {}, rawQuote: Buffer.alloc(0), rawSignature: Buffer.alloc(0) };
}

export const TpmAttestation_Quote: MessageFns<TpmAttestation_Quote> = {
  encode(message: TpmAttestation_Quote, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.hashAlgo !== 0) {
      writer.uint32(8).int32(message.hashAlgo);
    }
    Object.entries(message.pcrValues).forEach(([key, value]) => {
      TpmAttestation_Quote_PcrValuesEntry.encode({ key: key as any, value }, writer.uint32(18).fork()).join();
    });
    if (message.rawQuote.length !== 0) {
      writer.uint32(26).bytes(message.rawQuote);
    }
    if (message.rawSignature.length !== 0) {
      writer.uint32(34).bytes(message.rawSignature);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TpmAttestation_Quote {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTpmAttestation_Quote();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.hashAlgo = reader.int32();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          const entry2 = TpmAttestation_Quote_PcrValuesEntry.decode(reader, reader.uint32());
          if (entry2.value !== undefined) {
            message.pcrValues[entry2.key] = entry2.value;
          }
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.rawQuote = Buffer.from(reader.bytes());
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.rawSignature = Buffer.from(reader.bytes());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TpmAttestation_Quote {
    return {
      hashAlgo: isSet(object.hashAlgo) ? globalThis.Number(object.hashAlgo) : 0,
      pcrValues: isObject(object.pcrValues)
        ? Object.entries(object.pcrValues).reduce<{ [key: number]: Buffer }>((acc, [key, value]) => {
          acc[globalThis.Number(key)] = Buffer.from(bytesFromBase64(value as string));
          return acc;
        }, {})
        : {},
      rawQuote: isSet(object.rawQuote) ? Buffer.from(bytesFromBase64(object.rawQuote)) : Buffer.alloc(0),
      rawSignature: isSet(object.rawSignature) ? Buffer.from(bytesFromBase64(object.rawSignature)) : Buffer.alloc(0),
    };
  },

  toJSON(message: TpmAttestation_Quote): unknown {
    const obj: any = {};
    if (message.hashAlgo !== 0) {
      obj.hashAlgo = Math.round(message.hashAlgo);
    }
    if (message.pcrValues) {
      const entries = Object.entries(message.pcrValues);
      if (entries.length > 0) {
        obj.pcrValues = {};
        entries.forEach(([k, v]) => {
          obj.pcrValues[k] = base64FromBytes(v);
        });
      }
    }
    if (message.rawQuote.length !== 0) {
      obj.rawQuote = base64FromBytes(message.rawQuote);
    }
    if (message.rawSignature.length !== 0) {
      obj.rawSignature = base64FromBytes(message.rawSignature);
    }
    return obj;
  },

  create(base?: DeepPartial<TpmAttestation_Quote>): TpmAttestation_Quote {
    return TpmAttestation_Quote.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TpmAttestation_Quote>): TpmAttestation_Quote {
    const message = createBaseTpmAttestation_Quote();
    message.hashAlgo = object.hashAlgo ?? 0;
    message.pcrValues = Object.entries(object.pcrValues ?? {}).reduce<{ [key: number]: Buffer }>(
      (acc, [key, value]) => {
        if (value !== undefined) {
          acc[globalThis.Number(key)] = value;
        }
        return acc;
      },
      {},
    );
    message.rawQuote = object.rawQuote ?? Buffer.alloc(0);
    message.rawSignature = object.rawSignature ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseTpmAttestation_Quote_PcrValuesEntry(): TpmAttestation_Quote_PcrValuesEntry {
  return { key: 0, value: Buffer.alloc(0) };
}

export const TpmAttestation_Quote_PcrValuesEntry: MessageFns<TpmAttestation_Quote_PcrValuesEntry> = {
  encode(message: TpmAttestation_Quote_PcrValuesEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== 0) {
      writer.uint32(8).int32(message.key);
    }
    if (message.value.length !== 0) {
      writer.uint32(18).bytes(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TpmAttestation_Quote_PcrValuesEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTpmAttestation_Quote_PcrValuesEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.key = reader.int32();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.value = Buffer.from(reader.bytes());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TpmAttestation_Quote_PcrValuesEntry {
    return {
      key: isSet(object.key) ? globalThis.Number(object.key) : 0,
      value: isSet(object.value) ? Buffer.from(bytesFromBase64(object.value)) : Buffer.alloc(0),
    };
  },

  toJSON(message: TpmAttestation_Quote_PcrValuesEntry): unknown {
    const obj: any = {};
    if (message.key !== 0) {
      obj.key = Math.round(message.key);
    }
    if (message.value.length !== 0) {
      obj.value = base64FromBytes(message.value);
    }
    return obj;
  },

  create(base?: DeepPartial<TpmAttestation_Quote_PcrValuesEntry>): TpmAttestation_Quote_PcrValuesEntry {
    return TpmAttestation_Quote_PcrValuesEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TpmAttestation_Quote_PcrValuesEntry>): TpmAttestation_Quote_PcrValuesEntry {
    const message = createBaseTpmAttestation_Quote_PcrValuesEntry();
    message.key = object.key ?? 0;
    message.value = object.value ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseConfidentialSpaceInfo(): ConfidentialSpaceInfo {
  return { signedEntities: [] };
}

export const ConfidentialSpaceInfo: MessageFns<ConfidentialSpaceInfo> = {
  encode(message: ConfidentialSpaceInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.signedEntities) {
      SignedEntity.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConfidentialSpaceInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConfidentialSpaceInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.signedEntities.push(SignedEntity.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConfidentialSpaceInfo {
    return {
      signedEntities: globalThis.Array.isArray(object?.signedEntities)
        ? object.signedEntities.map((e: any) => SignedEntity.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ConfidentialSpaceInfo): unknown {
    const obj: any = {};
    if (message.signedEntities?.length) {
      obj.signedEntities = message.signedEntities.map((e) => SignedEntity.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<ConfidentialSpaceInfo>): ConfidentialSpaceInfo {
    return ConfidentialSpaceInfo.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ConfidentialSpaceInfo>): ConfidentialSpaceInfo {
    const message = createBaseConfidentialSpaceInfo();
    message.signedEntities = object.signedEntities?.map((e) => SignedEntity.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSignedEntity(): SignedEntity {
  return { containerImageSignatures: [] };
}

export const SignedEntity: MessageFns<SignedEntity> = {
  encode(message: SignedEntity, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.containerImageSignatures) {
      ContainerImageSignature.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SignedEntity {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSignedEntity();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.containerImageSignatures.push(ContainerImageSignature.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SignedEntity {
    return {
      containerImageSignatures: globalThis.Array.isArray(object?.containerImageSignatures)
        ? object.containerImageSignatures.map((e: any) => ContainerImageSignature.fromJSON(e))
        : [],
    };
  },

  toJSON(message: SignedEntity): unknown {
    const obj: any = {};
    if (message.containerImageSignatures?.length) {
      obj.containerImageSignatures = message.containerImageSignatures.map((e) => ContainerImageSignature.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<SignedEntity>): SignedEntity {
    return SignedEntity.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SignedEntity>): SignedEntity {
    const message = createBaseSignedEntity();
    message.containerImageSignatures =
      object.containerImageSignatures?.map((e) => ContainerImageSignature.fromPartial(e)) || [];
    return message;
  },
};

function createBaseContainerImageSignature(): ContainerImageSignature {
  return { payload: Buffer.alloc(0), signature: Buffer.alloc(0), publicKey: Buffer.alloc(0), sigAlg: 0 };
}

export const ContainerImageSignature: MessageFns<ContainerImageSignature> = {
  encode(message: ContainerImageSignature, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.payload.length !== 0) {
      writer.uint32(10).bytes(message.payload);
    }
    if (message.signature.length !== 0) {
      writer.uint32(18).bytes(message.signature);
    }
    if (message.publicKey.length !== 0) {
      writer.uint32(26).bytes(message.publicKey);
    }
    if (message.sigAlg !== 0) {
      writer.uint32(32).int32(message.sigAlg);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ContainerImageSignature {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseContainerImageSignature();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.payload = Buffer.from(reader.bytes());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.signature = Buffer.from(reader.bytes());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.publicKey = Buffer.from(reader.bytes());
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.sigAlg = reader.int32() as any;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ContainerImageSignature {
    return {
      payload: isSet(object.payload) ? Buffer.from(bytesFromBase64(object.payload)) : Buffer.alloc(0),
      signature: isSet(object.signature) ? Buffer.from(bytesFromBase64(object.signature)) : Buffer.alloc(0),
      publicKey: isSet(object.publicKey) ? Buffer.from(bytesFromBase64(object.publicKey)) : Buffer.alloc(0),
      sigAlg: isSet(object.sigAlg) ? signingAlgorithmFromJSON(object.sigAlg) : 0,
    };
  },

  toJSON(message: ContainerImageSignature): unknown {
    const obj: any = {};
    if (message.payload.length !== 0) {
      obj.payload = base64FromBytes(message.payload);
    }
    if (message.signature.length !== 0) {
      obj.signature = base64FromBytes(message.signature);
    }
    if (message.publicKey.length !== 0) {
      obj.publicKey = base64FromBytes(message.publicKey);
    }
    if (message.sigAlg !== 0) {
      obj.sigAlg = signingAlgorithmToJSON(message.sigAlg);
    }
    return obj;
  },

  create(base?: DeepPartial<ContainerImageSignature>): ContainerImageSignature {
    return ContainerImageSignature.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ContainerImageSignature>): ContainerImageSignature {
    const message = createBaseContainerImageSignature();
    message.payload = object.payload ?? Buffer.alloc(0);
    message.signature = object.signature ?? Buffer.alloc(0);
    message.publicKey = object.publicKey ?? Buffer.alloc(0);
    message.sigAlg = object.sigAlg ?? 0;
    return message;
  },
};

/** Service describing handlers for resources */
export type ConfidentialComputingDefinition = typeof ConfidentialComputingDefinition;
export const ConfidentialComputingDefinition = {
  name: "ConfidentialComputing",
  fullName: "google.cloud.confidentialcomputing.v1.ConfidentialComputing",
  methods: {
    /** Creates a new Challenge in a given project and location. */
    createChallenge: {
      name: "CreateChallenge",
      requestType: CreateChallengeRequest,
      requestStream: false,
      responseType: Challenge,
      responseStream: false,
      options: {
        _unknownFields: {
          8410: [Buffer.from([16, 112, 97, 114, 101, 110, 116, 44, 99, 104, 97, 108, 108, 101, 110, 103, 101])],
          578365826: [
            Buffer.from([
              59,
              58,
              9,
              99,
              104,
              97,
              108,
              108,
              101,
              110,
              103,
              101,
              34,
              46,
              47,
              118,
              49,
              47,
              123,
              112,
              97,
              114,
              101,
              110,
              116,
              61,
              112,
              114,
              111,
              106,
              101,
              99,
              116,
              115,
              47,
              42,
              47,
              108,
              111,
              99,
              97,
              116,
              105,
              111,
              110,
              115,
              47,
              42,
              125,
              47,
              99,
              104,
              97,
              108,
              108,
              101,
              110,
              103,
              101,
              115,
            ]),
          ],
        },
      },
    },
    /** Verifies the provided attestation info, returning a signed OIDC token. */
    verifyAttestation: {
      name: "VerifyAttestation",
      requestType: VerifyAttestationRequest,
      requestStream: false,
      responseType: VerifyAttestationResponse,
      responseStream: false,
      options: {
        _unknownFields: {
          578365826: [
            Buffer.from([
              74,
              58,
              1,
              42,
              34,
              69,
              47,
              118,
              49,
              47,
              123,
              99,
              104,
              97,
              108,
              108,
              101,
              110,
              103,
              101,
              61,
              112,
              114,
              111,
              106,
              101,
              99,
              116,
              115,
              47,
              42,
              47,
              108,
              111,
              99,
              97,
              116,
              105,
              111,
              110,
              115,
              47,
              42,
              47,
              99,
              104,
              97,
              108,
              108,
              101,
              110,
              103,
              101,
              115,
              47,
              42,
              125,
              58,
              118,
              101,
              114,
              105,
              102,
              121,
              65,
              116,
              116,
              101,
              115,
              116,
              97,
              116,
              105,
              111,
              110,
            ]),
          ],
        },
      },
    },
  },
} as const;

export interface ConfidentialComputingServiceImplementation<CallContextExt = {}> {
  /** Creates a new Challenge in a given project and location. */
  createChallenge(
    request: CreateChallengeRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<Challenge>>;
  /** Verifies the provided attestation info, returning a signed OIDC token. */
  verifyAttestation(
    request: VerifyAttestationRequest,
    context: CallContext & CallContextExt,
  ): Promise<DeepPartial<VerifyAttestationResponse>>;
}

export interface ConfidentialComputingClient<CallOptionsExt = {}> {
  /** Creates a new Challenge in a given project and location. */
  createChallenge(
    request: DeepPartial<CreateChallengeRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<Challenge>;
  /** Verifies the provided attestation info, returning a signed OIDC token. */
  verifyAttestation(
    request: DeepPartial<VerifyAttestationRequest>,
    options?: CallOptions & CallOptionsExt,
  ): Promise<VerifyAttestationResponse>;
}

function bytesFromBase64(b64: string): Uint8Array {
  return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
}

function base64FromBytes(arr: Uint8Array): string {
  return globalThis.Buffer.from(arr).toString("base64");
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = numberToLong(Math.trunc(date.getTime() / 1_000));
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds.toNumber() || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function numberToLong(number: number) {
  return Long.fromNumber(number);
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

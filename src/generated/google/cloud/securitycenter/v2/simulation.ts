// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.0
//   protoc               unknown
// source: google/cloud/securitycenter/v2/simulation.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";
import { Timestamp } from "../../../protobuf/timestamp.js";
import { CloudProvider, cloudProviderFromJSON, cloudProviderToJSON } from "./resource.js";
import { ResourceValueConfigMetadata } from "./valued_resource.js";

export const protobufPackage = "google.cloud.securitycenter.v2";

/** Attack path simulation */
export interface Simulation {
  /**
   * Full resource name of the Simulation:
   * `organizations/123/simulations/456`
   */
  name: string;
  /** Output only. Time simulation was created */
  createTime:
    | Date
    | undefined;
  /**
   * Resource value configurations' metadata used in this simulation. Maximum of
   * 100.
   */
  resourceValueConfigsMetadata: ResourceValueConfigMetadata[];
  /** Indicates which cloud provider was used in this simulation. */
  cloudProvider: CloudProvider;
}

function createBaseSimulation(): Simulation {
  return { name: "", createTime: undefined, resourceValueConfigsMetadata: [], cloudProvider: 0 };
}

export const Simulation: MessageFns<Simulation> = {
  encode(message: Simulation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.createTime !== undefined) {
      Timestamp.encode(toTimestamp(message.createTime), writer.uint32(18).fork()).join();
    }
    for (const v of message.resourceValueConfigsMetadata) {
      ResourceValueConfigMetadata.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.cloudProvider !== 0) {
      writer.uint32(32).int32(message.cloudProvider);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Simulation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSimulation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.createTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.resourceValueConfigsMetadata.push(ResourceValueConfigMetadata.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.cloudProvider = reader.int32() as any;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Simulation {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      createTime: isSet(object.createTime) ? fromJsonTimestamp(object.createTime) : undefined,
      resourceValueConfigsMetadata: globalThis.Array.isArray(object?.resourceValueConfigsMetadata)
        ? object.resourceValueConfigsMetadata.map((e: any) => ResourceValueConfigMetadata.fromJSON(e))
        : [],
      cloudProvider: isSet(object.cloudProvider) ? cloudProviderFromJSON(object.cloudProvider) : 0,
    };
  },

  toJSON(message: Simulation): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.createTime !== undefined) {
      obj.createTime = message.createTime.toISOString();
    }
    if (message.resourceValueConfigsMetadata?.length) {
      obj.resourceValueConfigsMetadata = message.resourceValueConfigsMetadata.map((e) =>
        ResourceValueConfigMetadata.toJSON(e)
      );
    }
    if (message.cloudProvider !== 0) {
      obj.cloudProvider = cloudProviderToJSON(message.cloudProvider);
    }
    return obj;
  },

  create(base?: DeepPartial<Simulation>): Simulation {
    return Simulation.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Simulation>): Simulation {
    const message = createBaseSimulation();
    message.name = object.name ?? "";
    message.createTime = object.createTime ?? undefined;
    message.resourceValueConfigsMetadata =
      object.resourceValueConfigsMetadata?.map((e) => ResourceValueConfigMetadata.fromPartial(e)) || [];
    message.cloudProvider = object.cloudProvider ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = numberToLong(Math.trunc(date.getTime() / 1_000));
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds.toNumber() || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function numberToLong(number: number) {
  return Long.fromNumber(number);
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.0
//   protoc               unknown
// source: google/cloud/recommender/v1beta1/recommender_config.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";
import { Struct } from "../../../protobuf/struct.js";
import { Timestamp } from "../../../protobuf/timestamp.js";

export const protobufPackage = "google.cloud.recommender.v1beta1";

/** Configuration for a Recommender. */
export interface RecommenderConfig {
  /**
   * Name of recommender config.
   * Eg,
   * projects/[PROJECT_NUMBER]/locations/[LOCATION]/recommenders/[RECOMMENDER_ID]/config
   */
  name: string;
  /**
   * RecommenderGenerationConfig which configures the Generation of
   * recommendations for this recommender.
   */
  recommenderGenerationConfig:
    | RecommenderGenerationConfig
    | undefined;
  /**
   * Fingerprint of the RecommenderConfig. Provides optimistic locking when
   * updating.
   */
  etag: string;
  /** Last time when the config was updated. */
  updateTime:
    | Date
    | undefined;
  /**
   * Output only. Immutable. The revision ID of the config.
   * A new revision is committed whenever the config is changed in any way.
   * The format is an 8-character hexadecimal string.
   */
  revisionId: string;
  /**
   * Allows clients to store small amounts of arbitrary data. Annotations must
   * follow the Kubernetes syntax.
   * The total size of all keys and values combined is limited to 256k.
   * Key can have 2 segments: prefix (optional) and name (required),
   * separated by a slash (/).
   * Prefix must be a DNS subdomain.
   * Name must be 63 characters or less, begin and end with alphanumerics,
   * with dashes (-), underscores (_), dots (.), and alphanumerics between.
   */
  annotations: { [key: string]: string };
  /**
   * A user-settable field to provide a human-readable name to be used in user
   * interfaces.
   */
  displayName: string;
}

export interface RecommenderConfig_AnnotationsEntry {
  key: string;
  value: string;
}

/**
 * A Configuration to customize the generation of recommendations.
 * Eg, customizing the lookback period considered when generating a
 * recommendation.
 */
export interface RecommenderGenerationConfig {
  /**
   * Parameters for this RecommenderGenerationConfig. These configs can be used
   * by or are applied to all subtypes.
   */
  params: { [key: string]: any } | undefined;
}

function createBaseRecommenderConfig(): RecommenderConfig {
  return {
    name: "",
    recommenderGenerationConfig: undefined,
    etag: "",
    updateTime: undefined,
    revisionId: "",
    annotations: {},
    displayName: "",
  };
}

export const RecommenderConfig: MessageFns<RecommenderConfig> = {
  encode(message: RecommenderConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.recommenderGenerationConfig !== undefined) {
      RecommenderGenerationConfig.encode(message.recommenderGenerationConfig, writer.uint32(18).fork()).join();
    }
    if (message.etag !== "") {
      writer.uint32(26).string(message.etag);
    }
    if (message.updateTime !== undefined) {
      Timestamp.encode(toTimestamp(message.updateTime), writer.uint32(34).fork()).join();
    }
    if (message.revisionId !== "") {
      writer.uint32(42).string(message.revisionId);
    }
    Object.entries(message.annotations).forEach(([key, value]) => {
      RecommenderConfig_AnnotationsEntry.encode({ key: key as any, value }, writer.uint32(50).fork()).join();
    });
    if (message.displayName !== "") {
      writer.uint32(58).string(message.displayName);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RecommenderConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRecommenderConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.recommenderGenerationConfig = RecommenderGenerationConfig.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.etag = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.updateTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.revisionId = reader.string();
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          const entry6 = RecommenderConfig_AnnotationsEntry.decode(reader, reader.uint32());
          if (entry6.value !== undefined) {
            message.annotations[entry6.key] = entry6.value;
          }
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.displayName = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RecommenderConfig {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      recommenderGenerationConfig: isSet(object.recommenderGenerationConfig)
        ? RecommenderGenerationConfig.fromJSON(object.recommenderGenerationConfig)
        : undefined,
      etag: isSet(object.etag) ? globalThis.String(object.etag) : "",
      updateTime: isSet(object.updateTime) ? fromJsonTimestamp(object.updateTime) : undefined,
      revisionId: isSet(object.revisionId) ? globalThis.String(object.revisionId) : "",
      annotations: isObject(object.annotations)
        ? Object.entries(object.annotations).reduce<{ [key: string]: string }>((acc, [key, value]) => {
          acc[key] = String(value);
          return acc;
        }, {})
        : {},
      displayName: isSet(object.displayName) ? globalThis.String(object.displayName) : "",
    };
  },

  toJSON(message: RecommenderConfig): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.recommenderGenerationConfig !== undefined) {
      obj.recommenderGenerationConfig = RecommenderGenerationConfig.toJSON(message.recommenderGenerationConfig);
    }
    if (message.etag !== "") {
      obj.etag = message.etag;
    }
    if (message.updateTime !== undefined) {
      obj.updateTime = message.updateTime.toISOString();
    }
    if (message.revisionId !== "") {
      obj.revisionId = message.revisionId;
    }
    if (message.annotations) {
      const entries = Object.entries(message.annotations);
      if (entries.length > 0) {
        obj.annotations = {};
        entries.forEach(([k, v]) => {
          obj.annotations[k] = v;
        });
      }
    }
    if (message.displayName !== "") {
      obj.displayName = message.displayName;
    }
    return obj;
  },

  create(base?: DeepPartial<RecommenderConfig>): RecommenderConfig {
    return RecommenderConfig.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RecommenderConfig>): RecommenderConfig {
    const message = createBaseRecommenderConfig();
    message.name = object.name ?? "";
    message.recommenderGenerationConfig =
      (object.recommenderGenerationConfig !== undefined && object.recommenderGenerationConfig !== null)
        ? RecommenderGenerationConfig.fromPartial(object.recommenderGenerationConfig)
        : undefined;
    message.etag = object.etag ?? "";
    message.updateTime = object.updateTime ?? undefined;
    message.revisionId = object.revisionId ?? "";
    message.annotations = Object.entries(object.annotations ?? {}).reduce<{ [key: string]: string }>(
      (acc, [key, value]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    message.displayName = object.displayName ?? "";
    return message;
  },
};

function createBaseRecommenderConfig_AnnotationsEntry(): RecommenderConfig_AnnotationsEntry {
  return { key: "", value: "" };
}

export const RecommenderConfig_AnnotationsEntry: MessageFns<RecommenderConfig_AnnotationsEntry> = {
  encode(message: RecommenderConfig_AnnotationsEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RecommenderConfig_AnnotationsEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRecommenderConfig_AnnotationsEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RecommenderConfig_AnnotationsEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: RecommenderConfig_AnnotationsEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create(base?: DeepPartial<RecommenderConfig_AnnotationsEntry>): RecommenderConfig_AnnotationsEntry {
    return RecommenderConfig_AnnotationsEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RecommenderConfig_AnnotationsEntry>): RecommenderConfig_AnnotationsEntry {
    const message = createBaseRecommenderConfig_AnnotationsEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseRecommenderGenerationConfig(): RecommenderGenerationConfig {
  return { params: undefined };
}

export const RecommenderGenerationConfig: MessageFns<RecommenderGenerationConfig> = {
  encode(message: RecommenderGenerationConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.params !== undefined) {
      Struct.encode(Struct.wrap(message.params), writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RecommenderGenerationConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRecommenderGenerationConfig();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.params = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RecommenderGenerationConfig {
    return { params: isObject(object.params) ? object.params : undefined };
  },

  toJSON(message: RecommenderGenerationConfig): unknown {
    const obj: any = {};
    if (message.params !== undefined) {
      obj.params = message.params;
    }
    return obj;
  },

  create(base?: DeepPartial<RecommenderGenerationConfig>): RecommenderGenerationConfig {
    return RecommenderGenerationConfig.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RecommenderGenerationConfig>): RecommenderGenerationConfig {
    const message = createBaseRecommenderGenerationConfig();
    message.params = object.params ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = numberToLong(Math.trunc(date.getTime() / 1_000));
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds.toNumber() || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function numberToLong(number: number) {
  return Long.fromNumber(number);
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

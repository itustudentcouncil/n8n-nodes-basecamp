// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.0
//   protoc               unknown
// source: google/cloud/healthcare/logging/hl7v2.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";
import { Status } from "../../../rpc/status.js";

export const protobufPackage = "google.cloud.healthcare.logging";

/** A log entry for a HL7v2 import long-running operation. */
export interface ImportHl7V2LogEntry {
  /**
   * The source in Cloud Storage (for example,
   * `gs://{bucket_id}/{path/to/file}`).
   */
  source: string;
  /** The error code and message. */
  error: Status | undefined;
}

/** A log entry for a HL7v2 store Pub/Sub notification. */
export interface Hl7V2NotificationLogEntry {
  /**
   * The HL7v2 message being created (for example,
   * `projects/{projectId}/locations/{locationId}/datasets/{datasetId}/hl7V2Stores/{hl7v2StoreId}/messages/{hl7v2MessageId}`).
   */
  resourceName: string;
  /** The Pub/Sub topic that the notification is published on. */
  pubsubTopic: string;
  /** The error code and message. */
  error: Status | undefined;
}

function createBaseImportHl7V2LogEntry(): ImportHl7V2LogEntry {
  return { source: "", error: undefined };
}

export const ImportHl7V2LogEntry: MessageFns<ImportHl7V2LogEntry> = {
  encode(message: ImportHl7V2LogEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.source !== "") {
      writer.uint32(10).string(message.source);
    }
    if (message.error !== undefined) {
      Status.encode(message.error, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ImportHl7V2LogEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseImportHl7V2LogEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.source = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.error = Status.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ImportHl7V2LogEntry {
    return {
      source: isSet(object.source) ? globalThis.String(object.source) : "",
      error: isSet(object.error) ? Status.fromJSON(object.error) : undefined,
    };
  },

  toJSON(message: ImportHl7V2LogEntry): unknown {
    const obj: any = {};
    if (message.source !== "") {
      obj.source = message.source;
    }
    if (message.error !== undefined) {
      obj.error = Status.toJSON(message.error);
    }
    return obj;
  },

  create(base?: DeepPartial<ImportHl7V2LogEntry>): ImportHl7V2LogEntry {
    return ImportHl7V2LogEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ImportHl7V2LogEntry>): ImportHl7V2LogEntry {
    const message = createBaseImportHl7V2LogEntry();
    message.source = object.source ?? "";
    message.error = (object.error !== undefined && object.error !== null)
      ? Status.fromPartial(object.error)
      : undefined;
    return message;
  },
};

function createBaseHl7V2NotificationLogEntry(): Hl7V2NotificationLogEntry {
  return { resourceName: "", pubsubTopic: "", error: undefined };
}

export const Hl7V2NotificationLogEntry: MessageFns<Hl7V2NotificationLogEntry> = {
  encode(message: Hl7V2NotificationLogEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.resourceName !== "") {
      writer.uint32(10).string(message.resourceName);
    }
    if (message.pubsubTopic !== "") {
      writer.uint32(18).string(message.pubsubTopic);
    }
    if (message.error !== undefined) {
      Status.encode(message.error, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Hl7V2NotificationLogEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseHl7V2NotificationLogEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.resourceName = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.pubsubTopic = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.error = Status.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Hl7V2NotificationLogEntry {
    return {
      resourceName: isSet(object.resourceName) ? globalThis.String(object.resourceName) : "",
      pubsubTopic: isSet(object.pubsubTopic) ? globalThis.String(object.pubsubTopic) : "",
      error: isSet(object.error) ? Status.fromJSON(object.error) : undefined,
    };
  },

  toJSON(message: Hl7V2NotificationLogEntry): unknown {
    const obj: any = {};
    if (message.resourceName !== "") {
      obj.resourceName = message.resourceName;
    }
    if (message.pubsubTopic !== "") {
      obj.pubsubTopic = message.pubsubTopic;
    }
    if (message.error !== undefined) {
      obj.error = Status.toJSON(message.error);
    }
    return obj;
  },

  create(base?: DeepPartial<Hl7V2NotificationLogEntry>): Hl7V2NotificationLogEntry {
    return Hl7V2NotificationLogEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Hl7V2NotificationLogEntry>): Hl7V2NotificationLogEntry {
    const message = createBaseHl7V2NotificationLogEntry();
    message.resourceName = object.resourceName ?? "";
    message.pubsubTopic = object.pubsubTopic ?? "";
    message.error = (object.error !== undefined && object.error !== null)
      ? Status.fromPartial(object.error)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

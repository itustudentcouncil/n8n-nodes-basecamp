// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.0
//   protoc               unknown
// source: google/monitoring/dashboard/v1/collapsible_group.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";

export const protobufPackage = "google.monitoring.dashboard.v1";

/**
 * A widget that groups the other widgets. All widgets that are within
 * the area spanned by the grouping widget are considered member widgets.
 */
export interface CollapsibleGroup {
  /** The collapsed state of the widget on first page load. */
  collapsed: boolean;
}

function createBaseCollapsibleGroup(): CollapsibleGroup {
  return { collapsed: false };
}

export const CollapsibleGroup: MessageFns<CollapsibleGroup> = {
  encode(message: CollapsibleGroup, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.collapsed !== false) {
      writer.uint32(8).bool(message.collapsed);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CollapsibleGroup {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCollapsibleGroup();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.collapsed = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CollapsibleGroup {
    return { collapsed: isSet(object.collapsed) ? globalThis.Boolean(object.collapsed) : false };
  },

  toJSON(message: CollapsibleGroup): unknown {
    const obj: any = {};
    if (message.collapsed !== false) {
      obj.collapsed = message.collapsed;
    }
    return obj;
  },

  create(base?: DeepPartial<CollapsibleGroup>): CollapsibleGroup {
    return CollapsibleGroup.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CollapsibleGroup>): CollapsibleGroup {
    const message = createBaseCollapsibleGroup();
    message.collapsed = object.collapsed ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.0
//   protoc               unknown
// source: google/monitoring/dashboard/v1/table_display_options.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";

export const protobufPackage = "google.monitoring.dashboard.v1";

/** Table display options that can be reused. */
export interface TableDisplayOptions {
  /**
   * Optional. This field is unused and has been replaced by
   * TimeSeriesTable.column_settings
   *
   * @deprecated
   */
  shownColumns: string[];
}

function createBaseTableDisplayOptions(): TableDisplayOptions {
  return { shownColumns: [] };
}

export const TableDisplayOptions: MessageFns<TableDisplayOptions> = {
  encode(message: TableDisplayOptions, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.shownColumns) {
      writer.uint32(10).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TableDisplayOptions {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTableDisplayOptions();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.shownColumns.push(reader.string());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TableDisplayOptions {
    return {
      shownColumns: globalThis.Array.isArray(object?.shownColumns)
        ? object.shownColumns.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: TableDisplayOptions): unknown {
    const obj: any = {};
    if (message.shownColumns?.length) {
      obj.shownColumns = message.shownColumns;
    }
    return obj;
  },

  create(base?: DeepPartial<TableDisplayOptions>): TableDisplayOptions {
    return TableDisplayOptions.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TableDisplayOptions>): TableDisplayOptions {
    const message = createBaseTableDisplayOptions();
    message.shownColumns = object.shownColumns?.map((e) => e) || [];
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

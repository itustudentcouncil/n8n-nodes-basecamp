// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.0
//   protoc               unknown
// source: google/monitoring/v3/mutation_record.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";
import { Timestamp } from "../../protobuf/timestamp.js";

export const protobufPackage = "google.monitoring.v3";

/** Describes a change made to a configuration. */
export interface MutationRecord {
  /** When the change occurred. */
  mutateTime:
    | Date
    | undefined;
  /** The email address of the user making the change. */
  mutatedBy: string;
}

function createBaseMutationRecord(): MutationRecord {
  return { mutateTime: undefined, mutatedBy: "" };
}

export const MutationRecord: MessageFns<MutationRecord> = {
  encode(message: MutationRecord, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.mutateTime !== undefined) {
      Timestamp.encode(toTimestamp(message.mutateTime), writer.uint32(10).fork()).join();
    }
    if (message.mutatedBy !== "") {
      writer.uint32(18).string(message.mutatedBy);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MutationRecord {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMutationRecord();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.mutateTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.mutatedBy = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MutationRecord {
    return {
      mutateTime: isSet(object.mutateTime) ? fromJsonTimestamp(object.mutateTime) : undefined,
      mutatedBy: isSet(object.mutatedBy) ? globalThis.String(object.mutatedBy) : "",
    };
  },

  toJSON(message: MutationRecord): unknown {
    const obj: any = {};
    if (message.mutateTime !== undefined) {
      obj.mutateTime = message.mutateTime.toISOString();
    }
    if (message.mutatedBy !== "") {
      obj.mutatedBy = message.mutatedBy;
    }
    return obj;
  },

  create(base?: DeepPartial<MutationRecord>): MutationRecord {
    return MutationRecord.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MutationRecord>): MutationRecord {
    const message = createBaseMutationRecord();
    message.mutateTime = object.mutateTime ?? undefined;
    message.mutatedBy = object.mutatedBy ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = numberToLong(Math.trunc(date.getTime() / 1_000));
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds.toNumber() || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function numberToLong(number: number) {
  return Long.fromNumber(number);
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

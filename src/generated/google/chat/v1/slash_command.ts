// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.0
//   protoc               unknown
// source: google/chat/v1/slash_command.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";

export const protobufPackage = "google.chat.v1";

/**
 * A [slash
 * command](https://developers.google.com/workspace/chat/slash-commands) in
 * Google Chat.
 */
export interface SlashCommand {
  /** The ID of the slash command invoked. */
  commandId: Long;
}

function createBaseSlashCommand(): SlashCommand {
  return { commandId: Long.ZERO };
}

export const SlashCommand: MessageFns<SlashCommand> = {
  encode(message: SlashCommand, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (!message.commandId.equals(Long.ZERO)) {
      writer.uint32(8).int64(message.commandId.toString());
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SlashCommand {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSlashCommand();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.commandId = Long.fromString(reader.int64().toString());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SlashCommand {
    return { commandId: isSet(object.commandId) ? Long.fromValue(object.commandId) : Long.ZERO };
  },

  toJSON(message: SlashCommand): unknown {
    const obj: any = {};
    if (!message.commandId.equals(Long.ZERO)) {
      obj.commandId = (message.commandId || Long.ZERO).toString();
    }
    return obj;
  },

  create(base?: DeepPartial<SlashCommand>): SlashCommand {
    return SlashCommand.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SlashCommand>): SlashCommand {
    const message = createBaseSlashCommand();
    message.commandId = (object.commandId !== undefined && object.commandId !== null)
      ? Long.fromValue(object.commandId)
      : Long.ZERO;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

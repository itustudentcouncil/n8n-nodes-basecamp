// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.0
//   protoc               unknown
// source: google/actions/sdk/v2/interactionmodel/type/class_reference.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";

export const protobufPackage = "google.actions.sdk.v2.interactionmodel.type";

/**
 * A reference to a class which is used to declare the type of a field or return
 * value. Enums are also a type of class that can be referenced using
 * ClassReference.
 */
export interface ClassReference {
  /**
   * Required. Name of a built-in type or custom type of the parameter. Examples:
   * `PizzaToppings`, `actions.type.Number`
   */
  name: string;
  /** Optional. Indicates whether the data type represents a list of values. */
  list: boolean;
}

function createBaseClassReference(): ClassReference {
  return { name: "", list: false };
}

export const ClassReference: MessageFns<ClassReference> = {
  encode(message: ClassReference, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.list !== false) {
      writer.uint32(16).bool(message.list);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ClassReference {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseClassReference();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.list = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ClassReference {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      list: isSet(object.list) ? globalThis.Boolean(object.list) : false,
    };
  },

  toJSON(message: ClassReference): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.list !== false) {
      obj.list = message.list;
    }
    return obj;
  },

  create(base?: DeepPartial<ClassReference>): ClassReference {
    return ClassReference.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ClassReference>): ClassReference {
    const message = createBaseClassReference();
    message.name = object.name ?? "";
    message.list = object.list ?? false;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

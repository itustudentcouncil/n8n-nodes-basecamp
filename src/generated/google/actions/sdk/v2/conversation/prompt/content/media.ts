// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.0
//   protoc               unknown
// source: google/actions/sdk/v2/conversation/prompt/content/media.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";
import { Duration } from "../../../../../../protobuf/duration.js";
import { Image } from "./image.js";

export const protobufPackage = "google.actions.sdk.v2.conversation";

/**
 * Represents one media object.
 * Contains information about the media, such as name, description, url, etc.
 */
export interface Media {
  /** Media type. */
  mediaType: Media_MediaType;
  /** Start offset of the first media object. */
  startOffset:
    | Duration
    | undefined;
  /**
   * Optional media control types this media response session can support.
   * If set, request will be made to 3p when a certain media event happens.
   * If not set, 3p must still handle two default control type, FINISHED and
   * FAILED.
   */
  optionalMediaControls: Media_OptionalMediaControls[];
  /** List of Media Objects */
  mediaObjects: MediaObject[];
}

/** Media type of this response. */
export enum Media_MediaType {
  /** MEDIA_TYPE_UNSPECIFIED - Unspecified media type. */
  MEDIA_TYPE_UNSPECIFIED = 0,
  /** AUDIO - Audio file. */
  AUDIO = 1,
  /** MEDIA_STATUS_ACK - Response to acknowledge a media status report. */
  MEDIA_STATUS_ACK = 2,
  UNRECOGNIZED = -1,
}

export function media_MediaTypeFromJSON(object: any): Media_MediaType {
  switch (object) {
    case 0:
    case "MEDIA_TYPE_UNSPECIFIED":
      return Media_MediaType.MEDIA_TYPE_UNSPECIFIED;
    case 1:
    case "AUDIO":
      return Media_MediaType.AUDIO;
    case 2:
    case "MEDIA_STATUS_ACK":
      return Media_MediaType.MEDIA_STATUS_ACK;
    case -1:
    case "UNRECOGNIZED":
    default:
      return Media_MediaType.UNRECOGNIZED;
  }
}

export function media_MediaTypeToJSON(object: Media_MediaType): string {
  switch (object) {
    case Media_MediaType.MEDIA_TYPE_UNSPECIFIED:
      return "MEDIA_TYPE_UNSPECIFIED";
    case Media_MediaType.AUDIO:
      return "AUDIO";
    case Media_MediaType.MEDIA_STATUS_ACK:
      return "MEDIA_STATUS_ACK";
    case Media_MediaType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Optional media control types the media response can support */
export enum Media_OptionalMediaControls {
  /** OPTIONAL_MEDIA_CONTROLS_UNSPECIFIED - Unspecified value */
  OPTIONAL_MEDIA_CONTROLS_UNSPECIFIED = 0,
  /** PAUSED - Paused event. Triggered when user pauses the media. */
  PAUSED = 1,
  /**
   * STOPPED - Stopped event. Triggered when user exits out of 3p session during media
   * play.
   */
  STOPPED = 2,
  UNRECOGNIZED = -1,
}

export function media_OptionalMediaControlsFromJSON(object: any): Media_OptionalMediaControls {
  switch (object) {
    case 0:
    case "OPTIONAL_MEDIA_CONTROLS_UNSPECIFIED":
      return Media_OptionalMediaControls.OPTIONAL_MEDIA_CONTROLS_UNSPECIFIED;
    case 1:
    case "PAUSED":
      return Media_OptionalMediaControls.PAUSED;
    case 2:
    case "STOPPED":
      return Media_OptionalMediaControls.STOPPED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return Media_OptionalMediaControls.UNRECOGNIZED;
  }
}

export function media_OptionalMediaControlsToJSON(object: Media_OptionalMediaControls): string {
  switch (object) {
    case Media_OptionalMediaControls.OPTIONAL_MEDIA_CONTROLS_UNSPECIFIED:
      return "OPTIONAL_MEDIA_CONTROLS_UNSPECIFIED";
    case Media_OptionalMediaControls.PAUSED:
      return "PAUSED";
    case Media_OptionalMediaControls.STOPPED:
      return "STOPPED";
    case Media_OptionalMediaControls.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** Represents a single media object */
export interface MediaObject {
  /** Name of this media object. */
  name: string;
  /** Description of this media object. */
  description: string;
  /** The url pointing to the media content. */
  url: string;
  /** Image to show with the media card. */
  image: MediaImage | undefined;
}

/** Image to show with the media card. */
export interface MediaImage {
  /** A large image, such as the cover of the album, etc. */
  large?:
    | Image
    | undefined;
  /**
   * A small image icon displayed on the right from the title.
   * It's resized to 36x36 dp.
   */
  icon?: Image | undefined;
}

function createBaseMedia(): Media {
  return { mediaType: 0, startOffset: undefined, optionalMediaControls: [], mediaObjects: [] };
}

export const Media: MessageFns<Media> = {
  encode(message: Media, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.mediaType !== 0) {
      writer.uint32(64).int32(message.mediaType);
    }
    if (message.startOffset !== undefined) {
      Duration.encode(message.startOffset, writer.uint32(42).fork()).join();
    }
    writer.uint32(50).fork();
    for (const v of message.optionalMediaControls) {
      writer.int32(v);
    }
    writer.join();
    for (const v of message.mediaObjects) {
      MediaObject.encode(v!, writer.uint32(58).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Media {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMedia();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 8:
          if (tag !== 64) {
            break;
          }

          message.mediaType = reader.int32() as any;
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.startOffset = Duration.decode(reader, reader.uint32());
          continue;
        case 6:
          if (tag === 48) {
            message.optionalMediaControls.push(reader.int32() as any);

            continue;
          }

          if (tag === 50) {
            const end2 = reader.uint32() + reader.pos;
            while (reader.pos < end2) {
              message.optionalMediaControls.push(reader.int32() as any);
            }

            continue;
          }

          break;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.mediaObjects.push(MediaObject.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Media {
    return {
      mediaType: isSet(object.mediaType) ? media_MediaTypeFromJSON(object.mediaType) : 0,
      startOffset: isSet(object.startOffset) ? Duration.fromJSON(object.startOffset) : undefined,
      optionalMediaControls: globalThis.Array.isArray(object?.optionalMediaControls)
        ? object.optionalMediaControls.map((e: any) => media_OptionalMediaControlsFromJSON(e))
        : [],
      mediaObjects: globalThis.Array.isArray(object?.mediaObjects)
        ? object.mediaObjects.map((e: any) => MediaObject.fromJSON(e))
        : [],
    };
  },

  toJSON(message: Media): unknown {
    const obj: any = {};
    if (message.mediaType !== 0) {
      obj.mediaType = media_MediaTypeToJSON(message.mediaType);
    }
    if (message.startOffset !== undefined) {
      obj.startOffset = Duration.toJSON(message.startOffset);
    }
    if (message.optionalMediaControls?.length) {
      obj.optionalMediaControls = message.optionalMediaControls.map((e) => media_OptionalMediaControlsToJSON(e));
    }
    if (message.mediaObjects?.length) {
      obj.mediaObjects = message.mediaObjects.map((e) => MediaObject.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<Media>): Media {
    return Media.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Media>): Media {
    const message = createBaseMedia();
    message.mediaType = object.mediaType ?? 0;
    message.startOffset = (object.startOffset !== undefined && object.startOffset !== null)
      ? Duration.fromPartial(object.startOffset)
      : undefined;
    message.optionalMediaControls = object.optionalMediaControls?.map((e) => e) || [];
    message.mediaObjects = object.mediaObjects?.map((e) => MediaObject.fromPartial(e)) || [];
    return message;
  },
};

function createBaseMediaObject(): MediaObject {
  return { name: "", description: "", url: "", image: undefined };
}

export const MediaObject: MessageFns<MediaObject> = {
  encode(message: MediaObject, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.description !== "") {
      writer.uint32(18).string(message.description);
    }
    if (message.url !== "") {
      writer.uint32(26).string(message.url);
    }
    if (message.image !== undefined) {
      MediaImage.encode(message.image, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MediaObject {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMediaObject();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.description = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.url = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.image = MediaImage.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MediaObject {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      url: isSet(object.url) ? globalThis.String(object.url) : "",
      image: isSet(object.image) ? MediaImage.fromJSON(object.image) : undefined,
    };
  },

  toJSON(message: MediaObject): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.url !== "") {
      obj.url = message.url;
    }
    if (message.image !== undefined) {
      obj.image = MediaImage.toJSON(message.image);
    }
    return obj;
  },

  create(base?: DeepPartial<MediaObject>): MediaObject {
    return MediaObject.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MediaObject>): MediaObject {
    const message = createBaseMediaObject();
    message.name = object.name ?? "";
    message.description = object.description ?? "";
    message.url = object.url ?? "";
    message.image = (object.image !== undefined && object.image !== null)
      ? MediaImage.fromPartial(object.image)
      : undefined;
    return message;
  },
};

function createBaseMediaImage(): MediaImage {
  return { large: undefined, icon: undefined };
}

export const MediaImage: MessageFns<MediaImage> = {
  encode(message: MediaImage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.large !== undefined) {
      Image.encode(message.large, writer.uint32(10).fork()).join();
    }
    if (message.icon !== undefined) {
      Image.encode(message.icon, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MediaImage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseMediaImage();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.large = Image.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.icon = Image.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): MediaImage {
    return {
      large: isSet(object.large) ? Image.fromJSON(object.large) : undefined,
      icon: isSet(object.icon) ? Image.fromJSON(object.icon) : undefined,
    };
  },

  toJSON(message: MediaImage): unknown {
    const obj: any = {};
    if (message.large !== undefined) {
      obj.large = Image.toJSON(message.large);
    }
    if (message.icon !== undefined) {
      obj.icon = Image.toJSON(message.icon);
    }
    return obj;
  },

  create(base?: DeepPartial<MediaImage>): MediaImage {
    return MediaImage.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<MediaImage>): MediaImage {
    const message = createBaseMediaImage();
    message.large = (object.large !== undefined && object.large !== null) ? Image.fromPartial(object.large) : undefined;
    message.icon = (object.icon !== undefined && object.icon !== null) ? Image.fromPartial(object.icon) : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

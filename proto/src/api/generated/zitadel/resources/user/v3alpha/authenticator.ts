// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.0
//   protoc               unknown
// source: zitadel/resources/user/v3alpha/authenticator.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";
import { Struct } from "../../../../google/protobuf/struct.js";
import { Timestamp } from "../../../../google/protobuf/timestamp.js";
import { Details } from "../../object/v3alpha/object.js";

export const protobufPackage = "zitadel.resources.user.v3alpha";

export enum AuthNKeyType {
  AUTHN_KEY_TYPE_UNSPECIFIED = 0,
  AUTHN_KEY_TYPE_JSON = 1,
  UNRECOGNIZED = -1,
}

export function authNKeyTypeFromJSON(object: any): AuthNKeyType {
  switch (object) {
    case 0:
    case "AUTHN_KEY_TYPE_UNSPECIFIED":
      return AuthNKeyType.AUTHN_KEY_TYPE_UNSPECIFIED;
    case 1:
    case "AUTHN_KEY_TYPE_JSON":
      return AuthNKeyType.AUTHN_KEY_TYPE_JSON;
    case -1:
    case "UNRECOGNIZED":
    default:
      return AuthNKeyType.UNRECOGNIZED;
  }
}

export function authNKeyTypeToJSON(object: AuthNKeyType): string {
  switch (object) {
    case AuthNKeyType.AUTHN_KEY_TYPE_UNSPECIFIED:
      return "AUTHN_KEY_TYPE_UNSPECIFIED";
    case AuthNKeyType.AUTHN_KEY_TYPE_JSON:
      return "AUTHN_KEY_TYPE_JSON";
    case AuthNKeyType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum WebAuthNAuthenticatorType {
  WEB_AUTH_N_AUTHENTICATOR_UNSPECIFIED = 0,
  WEB_AUTH_N_AUTHENTICATOR_PLATFORM = 1,
  WEB_AUTH_N_AUTHENTICATOR_CROSS_PLATFORM = 2,
  UNRECOGNIZED = -1,
}

export function webAuthNAuthenticatorTypeFromJSON(object: any): WebAuthNAuthenticatorType {
  switch (object) {
    case 0:
    case "WEB_AUTH_N_AUTHENTICATOR_UNSPECIFIED":
      return WebAuthNAuthenticatorType.WEB_AUTH_N_AUTHENTICATOR_UNSPECIFIED;
    case 1:
    case "WEB_AUTH_N_AUTHENTICATOR_PLATFORM":
      return WebAuthNAuthenticatorType.WEB_AUTH_N_AUTHENTICATOR_PLATFORM;
    case 2:
    case "WEB_AUTH_N_AUTHENTICATOR_CROSS_PLATFORM":
      return WebAuthNAuthenticatorType.WEB_AUTH_N_AUTHENTICATOR_CROSS_PLATFORM;
    case -1:
    case "UNRECOGNIZED":
    default:
      return WebAuthNAuthenticatorType.UNRECOGNIZED;
  }
}

export function webAuthNAuthenticatorTypeToJSON(object: WebAuthNAuthenticatorType): string {
  switch (object) {
    case WebAuthNAuthenticatorType.WEB_AUTH_N_AUTHENTICATOR_UNSPECIFIED:
      return "WEB_AUTH_N_AUTHENTICATOR_UNSPECIFIED";
    case WebAuthNAuthenticatorType.WEB_AUTH_N_AUTHENTICATOR_PLATFORM:
      return "WEB_AUTH_N_AUTHENTICATOR_PLATFORM";
    case WebAuthNAuthenticatorType.WEB_AUTH_N_AUTHENTICATOR_CROSS_PLATFORM:
      return "WEB_AUTH_N_AUTHENTICATOR_CROSS_PLATFORM";
    case WebAuthNAuthenticatorType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface Authenticators {
  /** All of the user's usernames, which will be used for identification during authentication. */
  usernames: Username[];
  /** If the user has set a password, the time it was last changed will be returned. */
  password:
    | Password
    | undefined;
  /** Meta information about the user's WebAuthN authenticators. */
  webAuthN: WebAuthN[];
  /**
   * A list of the user's time-based one-time-password (TOTP) authenticators,
   * incl. the name for identification.
   */
  totps: TOTP[];
  /** A list of the user's one-time-password (OTP) SMS authenticators. */
  otpSms: OTPSMS[];
  /** A list of the user's one-time-password (OTP) Email authenticators. */
  otpEmail: OTPEmail[];
  /** A list of the user's authentication keys. They can be used to authenticate e.g. by JWT Profile. */
  authenticationKeys: AuthenticationKey[];
  /** A list of the user's linked identity providers (IDPs). */
  identityProviders: IdentityProvider[];
}

export interface Username {
  /** unique identifier of the username. */
  usernameId: string;
  /** The user's unique username. It is used for identification during authentication. */
  username: string;
  /**
   * By default usernames must be unique across all organizations in an instance.
   * This option allow to restrict the uniqueness to the user's own organization.
   * As a result, this username can only be used if the authentication is limited
   * to the corresponding organization.
   *
   * This can be useful if you provide multiple usernames for a single user, where one
   * if specific to your organization, e.g.:
   * - gigi-giraffe@zitadel.com (unique across organizations)
   * - gigi-giraffe (unique only inside the ZITADEL organization)
   */
  isOrganizationSpecific: boolean;
}

export interface SetUsername {
  /** Set the user's username. This will be used for identification during authentication. */
  username: string;
  /**
   * By default username must be unique across all organizations in an instance.
   * This option allow to restrict the uniqueness to the user's own organization.
   * As a result, this username can only be used if the authentication is limited
   * to the corresponding organization.
   *
   * This can be useful if you provide multiple usernames for a single user, where one
   * if specific to your organization, e.g.:
   * - gigi-giraffe@zitadel.com (unique across organizations)
   * - gigi-giraffe (unique only inside the ZITADEL organization)
   */
  isOrganizationSpecific: boolean;
}

export interface Password {
  /** States the time the password was last changed. */
  lastChanged: Date | undefined;
}

export interface WebAuthN {
  /** unique identifier of the WebAuthN authenticator. */
  webAuthNId: string;
  /** Name of the WebAuthN authenticator. This is used for easier identification. */
  name: string;
  /** State whether the WebAuthN registration has been completed. */
  isVerified: boolean;
  /**
   * States if the user has been verified during the registration. Authentication with this device
   * will be considered as multi factor authentication (MFA) without the need to check a password
   * (typically known as Passkeys).
   * Without user verification it will be a second factor authentication (2FA), typically done
   * after a password check.
   *
   * More on WebAuthN User Verification: https://www.w3.org/TR/webauthn/#user-verification
   */
  userVerified: boolean;
}

export interface StartWebAuthNRegistration {
  /** Domain on which the user currently is or will be authenticated. */
  domain: string;
  /**
   * Optionally specify the authenticator type of the passkey device (platform or cross-platform).
   * If none is provided, both values are allowed.
   */
  authenticatorType: WebAuthNAuthenticatorType;
  /**
   * Optionally provide a one time code generated by ZITADEL.
   * This is required to start the passkey registration without user authentication.
   */
  code?: AuthenticatorRegistrationCode | undefined;
}

export interface VerifyWebAuthNRegistration {
  /**
   * PublicKeyCredential Interface.
   * Generated helper methods populate the field from JSON created by a WebAuthN client.
   * See also:  https://www.w3.org/TR/webauthn/#publickeycredential
   */
  publicKeyCredential:
    | { [key: string]: any }
    | undefined;
  /** Provide a name for the WebAuthN device. This will help identify it in the future. */
  webAuthNName: string;
}

export interface OTPSMS {
  /** unique identifier of the one-time-password (OTP) SMS authenticator. */
  otpSmsId: string;
  /** The phone number used for the OTP SMS authenticator. */
  phone: string;
  /** State whether the OTP SMS registration has been completed. */
  isVerified: boolean;
}

export interface OTPEmail {
  /** unique identifier of the one-time-password (OTP) Email authenticator. */
  otpEmailId: string;
  /** The email address used for the OTP Email authenticator. */
  address: string;
  /** State whether the OTP Email registration has been completed. */
  isVerified: boolean;
}

export interface TOTP {
  /** unique identifier of the time-based one-time-password (TOTP) authenticator. */
  totpId: string;
  /** The name provided during registration. This is used for easier identification. */
  name: string;
  /** State whether the TOTP registration has been completed. */
  isVerified: boolean;
}

export interface AuthenticationKey {
  /** ID is the read-only unique identifier of the authentication key. */
  authenticationKeyId: string;
  details:
    | Details
    | undefined;
  /** the file type of the key */
  type: AuthNKeyType;
  /** After the expiration date, the key will no longer be usable for authentication. */
  expirationDate: Date | undefined;
}

export interface IdentityProvider {
  /** IDP ID is the read-only unique identifier of the identity provider in ZITADEL. */
  idpId: string;
  /** IDP name is the name of the identity provider in ZITADEL. */
  idpName: string;
  /**
   * The user ID represents the ID provided by the identity provider.
   * This ID is used to link the user in ZITADEL with the identity provider.
   */
  userId: string;
  /** The username represents the username provided by the identity provider. */
  username: string;
}

export interface SetAuthenticators {
  usernames: SetUsername[];
  password: SetPassword | undefined;
}

export interface SetPassword {
  /** Provide the plain text password. ZITADEL will take care to store it in a secure way (hash). */
  password?:
    | string
    | undefined;
  /**
   * Encoded hash of a password in Modular Crypt Format:
   * https://zitadel.com/docs/concepts/architecture/secrets#hashed-secrets.
   */
  hash?:
    | string
    | undefined;
  /** Provide if the user needs to change the password on the next use. */
  changeRequired: boolean;
  /** Provide the current password to verify you're allowed to change the password. */
  currentPassword?:
    | string
    | undefined;
  /** Or provider the verification code generated during password reset request. */
  verificationCode?: string | undefined;
}

export interface SendPasswordResetEmail {
  /**
   * Optionally set a url_template, which will be used in the password reset mail
   * sent by ZITADEL to guide the user to your password change page.
   * If no template is set, the default ZITADEL url will be used.
   */
  urlTemplate?: string | undefined;
}

export interface SendPasswordResetSMS {
}

export interface ReturnPasswordResetCode {
}

export interface AuthenticatorRegistrationCode {
  /** ID to the one time code generated by ZITADEL. */
  id: string;
  /** one time code generated by ZITADEL. */
  code: string;
}

export interface SendWebAuthNRegistrationLink {
  /**
   * Optionally set a url_template, which will be used in the mail sent by ZITADEL
   * to guide the user to your passkey registration page.
   * If no template is set, the default ZITADEL url will be used.
   */
  urlTemplate?: string | undefined;
}

export interface ReturnWebAuthNRegistrationCode {
}

export interface RedirectURLs {
  /** URL to which the user will be redirected after a successful login. */
  successUrl: string;
  /** URL to which the user will be redirected after a failed login. */
  failureUrl: string;
}

export interface LDAPCredentials {
  /** Username used to login through LDAP. */
  username: string;
  /** Password used to login through LDAP. */
  password: string;
}

export interface IdentityProviderIntent {
  /** ID of the identity provider (IDP) intent. */
  idpIntentId: string;
  /** Token of the identity provider (IDP) intent. */
  idpIntentToken: string;
  /** If the user was already federated and linked to a ZITADEL user, it's id will be returned. */
  userId?: string | undefined;
}

export interface IDPInformation {
  /** ID of the identity provider. */
  idpId: string;
  /** ID of the user provided by the identity provider. */
  userId: string;
  /** Username of the user provided by the identity provider. */
  userName: string;
  /** Complete information returned by the identity provider. */
  rawInformation:
    | { [key: string]: any }
    | undefined;
  /** OAuth/OIDC access (and id_token) returned by the identity provider. */
  oauth?:
    | IDPOAuthAccessInformation
    | undefined;
  /** LDAP entity attributes returned by the identity provider */
  ldap?:
    | IDPLDAPAccessInformation
    | undefined;
  /** SAMLResponse returned by the identity provider */
  saml?: IDPSAMLAccessInformation | undefined;
}

export interface IDPOAuthAccessInformation {
  /** The access_token returned by the identity provider. */
  accessToken: string;
  /** In case the provider returned an id_token. */
  idToken?: string | undefined;
}

export interface IDPLDAPAccessInformation {
  /** The attributes of the user returned by the identity provider. */
  attributes: { [key: string]: any } | undefined;
}

export interface IDPSAMLAccessInformation {
  /** The SAML assertion returned by the identity provider. */
  assertion: Buffer;
}

export interface IDPAuthenticator {
  /** ID of the identity provider */
  idpId: string;
  /** ID of the user provided by the identity provider */
  userId: string;
  /** Username of the user provided by the identity provider. */
  userName: string;
}

function createBaseAuthenticators(): Authenticators {
  return {
    usernames: [],
    password: undefined,
    webAuthN: [],
    totps: [],
    otpSms: [],
    otpEmail: [],
    authenticationKeys: [],
    identityProviders: [],
  };
}

export const Authenticators: MessageFns<Authenticators> = {
  encode(message: Authenticators, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.usernames) {
      Username.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.password !== undefined) {
      Password.encode(message.password, writer.uint32(18).fork()).join();
    }
    for (const v of message.webAuthN) {
      WebAuthN.encode(v!, writer.uint32(26).fork()).join();
    }
    for (const v of message.totps) {
      TOTP.encode(v!, writer.uint32(34).fork()).join();
    }
    for (const v of message.otpSms) {
      OTPSMS.encode(v!, writer.uint32(42).fork()).join();
    }
    for (const v of message.otpEmail) {
      OTPEmail.encode(v!, writer.uint32(50).fork()).join();
    }
    for (const v of message.authenticationKeys) {
      AuthenticationKey.encode(v!, writer.uint32(58).fork()).join();
    }
    for (const v of message.identityProviders) {
      IdentityProvider.encode(v!, writer.uint32(66).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Authenticators {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAuthenticators();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.usernames.push(Username.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.password = Password.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.webAuthN.push(WebAuthN.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.totps.push(TOTP.decode(reader, reader.uint32()));
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.otpSms.push(OTPSMS.decode(reader, reader.uint32()));
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.otpEmail.push(OTPEmail.decode(reader, reader.uint32()));
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.authenticationKeys.push(AuthenticationKey.decode(reader, reader.uint32()));
          continue;
        case 8:
          if (tag !== 66) {
            break;
          }

          message.identityProviders.push(IdentityProvider.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Authenticators {
    return {
      usernames: globalThis.Array.isArray(object?.usernames)
        ? object.usernames.map((e: any) => Username.fromJSON(e))
        : [],
      password: isSet(object.password) ? Password.fromJSON(object.password) : undefined,
      webAuthN: globalThis.Array.isArray(object?.webAuthN) ? object.webAuthN.map((e: any) => WebAuthN.fromJSON(e)) : [],
      totps: globalThis.Array.isArray(object?.totps) ? object.totps.map((e: any) => TOTP.fromJSON(e)) : [],
      otpSms: globalThis.Array.isArray(object?.otpSms) ? object.otpSms.map((e: any) => OTPSMS.fromJSON(e)) : [],
      otpEmail: globalThis.Array.isArray(object?.otpEmail) ? object.otpEmail.map((e: any) => OTPEmail.fromJSON(e)) : [],
      authenticationKeys: globalThis.Array.isArray(object?.authenticationKeys)
        ? object.authenticationKeys.map((e: any) => AuthenticationKey.fromJSON(e))
        : [],
      identityProviders: globalThis.Array.isArray(object?.identityProviders)
        ? object.identityProviders.map((e: any) => IdentityProvider.fromJSON(e))
        : [],
    };
  },

  toJSON(message: Authenticators): unknown {
    const obj: any = {};
    if (message.usernames?.length) {
      obj.usernames = message.usernames.map((e) => Username.toJSON(e));
    }
    if (message.password !== undefined) {
      obj.password = Password.toJSON(message.password);
    }
    if (message.webAuthN?.length) {
      obj.webAuthN = message.webAuthN.map((e) => WebAuthN.toJSON(e));
    }
    if (message.totps?.length) {
      obj.totps = message.totps.map((e) => TOTP.toJSON(e));
    }
    if (message.otpSms?.length) {
      obj.otpSms = message.otpSms.map((e) => OTPSMS.toJSON(e));
    }
    if (message.otpEmail?.length) {
      obj.otpEmail = message.otpEmail.map((e) => OTPEmail.toJSON(e));
    }
    if (message.authenticationKeys?.length) {
      obj.authenticationKeys = message.authenticationKeys.map((e) => AuthenticationKey.toJSON(e));
    }
    if (message.identityProviders?.length) {
      obj.identityProviders = message.identityProviders.map((e) => IdentityProvider.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<Authenticators>): Authenticators {
    return Authenticators.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Authenticators>): Authenticators {
    const message = createBaseAuthenticators();
    message.usernames = object.usernames?.map((e) => Username.fromPartial(e)) || [];
    message.password = (object.password !== undefined && object.password !== null)
      ? Password.fromPartial(object.password)
      : undefined;
    message.webAuthN = object.webAuthN?.map((e) => WebAuthN.fromPartial(e)) || [];
    message.totps = object.totps?.map((e) => TOTP.fromPartial(e)) || [];
    message.otpSms = object.otpSms?.map((e) => OTPSMS.fromPartial(e)) || [];
    message.otpEmail = object.otpEmail?.map((e) => OTPEmail.fromPartial(e)) || [];
    message.authenticationKeys = object.authenticationKeys?.map((e) => AuthenticationKey.fromPartial(e)) || [];
    message.identityProviders = object.identityProviders?.map((e) => IdentityProvider.fromPartial(e)) || [];
    return message;
  },
};

function createBaseUsername(): Username {
  return { usernameId: "", username: "", isOrganizationSpecific: false };
}

export const Username: MessageFns<Username> = {
  encode(message: Username, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.usernameId !== "") {
      writer.uint32(10).string(message.usernameId);
    }
    if (message.username !== "") {
      writer.uint32(18).string(message.username);
    }
    if (message.isOrganizationSpecific !== false) {
      writer.uint32(24).bool(message.isOrganizationSpecific);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Username {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUsername();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.usernameId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.username = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.isOrganizationSpecific = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Username {
    return {
      usernameId: isSet(object.usernameId) ? globalThis.String(object.usernameId) : "",
      username: isSet(object.username) ? globalThis.String(object.username) : "",
      isOrganizationSpecific: isSet(object.isOrganizationSpecific)
        ? globalThis.Boolean(object.isOrganizationSpecific)
        : false,
    };
  },

  toJSON(message: Username): unknown {
    const obj: any = {};
    if (message.usernameId !== "") {
      obj.usernameId = message.usernameId;
    }
    if (message.username !== "") {
      obj.username = message.username;
    }
    if (message.isOrganizationSpecific !== false) {
      obj.isOrganizationSpecific = message.isOrganizationSpecific;
    }
    return obj;
  },

  create(base?: DeepPartial<Username>): Username {
    return Username.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Username>): Username {
    const message = createBaseUsername();
    message.usernameId = object.usernameId ?? "";
    message.username = object.username ?? "";
    message.isOrganizationSpecific = object.isOrganizationSpecific ?? false;
    return message;
  },
};

function createBaseSetUsername(): SetUsername {
  return { username: "", isOrganizationSpecific: false };
}

export const SetUsername: MessageFns<SetUsername> = {
  encode(message: SetUsername, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.username !== "") {
      writer.uint32(10).string(message.username);
    }
    if (message.isOrganizationSpecific !== false) {
      writer.uint32(16).bool(message.isOrganizationSpecific);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetUsername {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetUsername();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.username = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.isOrganizationSpecific = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetUsername {
    return {
      username: isSet(object.username) ? globalThis.String(object.username) : "",
      isOrganizationSpecific: isSet(object.isOrganizationSpecific)
        ? globalThis.Boolean(object.isOrganizationSpecific)
        : false,
    };
  },

  toJSON(message: SetUsername): unknown {
    const obj: any = {};
    if (message.username !== "") {
      obj.username = message.username;
    }
    if (message.isOrganizationSpecific !== false) {
      obj.isOrganizationSpecific = message.isOrganizationSpecific;
    }
    return obj;
  },

  create(base?: DeepPartial<SetUsername>): SetUsername {
    return SetUsername.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SetUsername>): SetUsername {
    const message = createBaseSetUsername();
    message.username = object.username ?? "";
    message.isOrganizationSpecific = object.isOrganizationSpecific ?? false;
    return message;
  },
};

function createBasePassword(): Password {
  return { lastChanged: undefined };
}

export const Password: MessageFns<Password> = {
  encode(message: Password, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.lastChanged !== undefined) {
      Timestamp.encode(toTimestamp(message.lastChanged), writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Password {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePassword();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.lastChanged = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Password {
    return { lastChanged: isSet(object.lastChanged) ? fromJsonTimestamp(object.lastChanged) : undefined };
  },

  toJSON(message: Password): unknown {
    const obj: any = {};
    if (message.lastChanged !== undefined) {
      obj.lastChanged = message.lastChanged.toISOString();
    }
    return obj;
  },

  create(base?: DeepPartial<Password>): Password {
    return Password.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Password>): Password {
    const message = createBasePassword();
    message.lastChanged = object.lastChanged ?? undefined;
    return message;
  },
};

function createBaseWebAuthN(): WebAuthN {
  return { webAuthNId: "", name: "", isVerified: false, userVerified: false };
}

export const WebAuthN: MessageFns<WebAuthN> = {
  encode(message: WebAuthN, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.webAuthNId !== "") {
      writer.uint32(10).string(message.webAuthNId);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.isVerified !== false) {
      writer.uint32(24).bool(message.isVerified);
    }
    if (message.userVerified !== false) {
      writer.uint32(32).bool(message.userVerified);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): WebAuthN {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseWebAuthN();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.webAuthNId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.isVerified = reader.bool();
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.userVerified = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): WebAuthN {
    return {
      webAuthNId: isSet(object.webAuthNId) ? globalThis.String(object.webAuthNId) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      isVerified: isSet(object.isVerified) ? globalThis.Boolean(object.isVerified) : false,
      userVerified: isSet(object.userVerified) ? globalThis.Boolean(object.userVerified) : false,
    };
  },

  toJSON(message: WebAuthN): unknown {
    const obj: any = {};
    if (message.webAuthNId !== "") {
      obj.webAuthNId = message.webAuthNId;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.isVerified !== false) {
      obj.isVerified = message.isVerified;
    }
    if (message.userVerified !== false) {
      obj.userVerified = message.userVerified;
    }
    return obj;
  },

  create(base?: DeepPartial<WebAuthN>): WebAuthN {
    return WebAuthN.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<WebAuthN>): WebAuthN {
    const message = createBaseWebAuthN();
    message.webAuthNId = object.webAuthNId ?? "";
    message.name = object.name ?? "";
    message.isVerified = object.isVerified ?? false;
    message.userVerified = object.userVerified ?? false;
    return message;
  },
};

function createBaseStartWebAuthNRegistration(): StartWebAuthNRegistration {
  return { domain: "", authenticatorType: 0, code: undefined };
}

export const StartWebAuthNRegistration: MessageFns<StartWebAuthNRegistration> = {
  encode(message: StartWebAuthNRegistration, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.domain !== "") {
      writer.uint32(10).string(message.domain);
    }
    if (message.authenticatorType !== 0) {
      writer.uint32(16).int32(message.authenticatorType);
    }
    if (message.code !== undefined) {
      AuthenticatorRegistrationCode.encode(message.code, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StartWebAuthNRegistration {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStartWebAuthNRegistration();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.domain = reader.string();
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.authenticatorType = reader.int32() as any;
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.code = AuthenticatorRegistrationCode.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StartWebAuthNRegistration {
    return {
      domain: isSet(object.domain) ? globalThis.String(object.domain) : "",
      authenticatorType: isSet(object.authenticatorType)
        ? webAuthNAuthenticatorTypeFromJSON(object.authenticatorType)
        : 0,
      code: isSet(object.code) ? AuthenticatorRegistrationCode.fromJSON(object.code) : undefined,
    };
  },

  toJSON(message: StartWebAuthNRegistration): unknown {
    const obj: any = {};
    if (message.domain !== "") {
      obj.domain = message.domain;
    }
    if (message.authenticatorType !== 0) {
      obj.authenticatorType = webAuthNAuthenticatorTypeToJSON(message.authenticatorType);
    }
    if (message.code !== undefined) {
      obj.code = AuthenticatorRegistrationCode.toJSON(message.code);
    }
    return obj;
  },

  create(base?: DeepPartial<StartWebAuthNRegistration>): StartWebAuthNRegistration {
    return StartWebAuthNRegistration.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StartWebAuthNRegistration>): StartWebAuthNRegistration {
    const message = createBaseStartWebAuthNRegistration();
    message.domain = object.domain ?? "";
    message.authenticatorType = object.authenticatorType ?? 0;
    message.code = (object.code !== undefined && object.code !== null)
      ? AuthenticatorRegistrationCode.fromPartial(object.code)
      : undefined;
    return message;
  },
};

function createBaseVerifyWebAuthNRegistration(): VerifyWebAuthNRegistration {
  return { publicKeyCredential: undefined, webAuthNName: "" };
}

export const VerifyWebAuthNRegistration: MessageFns<VerifyWebAuthNRegistration> = {
  encode(message: VerifyWebAuthNRegistration, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.publicKeyCredential !== undefined) {
      Struct.encode(Struct.wrap(message.publicKeyCredential), writer.uint32(10).fork()).join();
    }
    if (message.webAuthNName !== "") {
      writer.uint32(18).string(message.webAuthNName);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): VerifyWebAuthNRegistration {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseVerifyWebAuthNRegistration();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.publicKeyCredential = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.webAuthNName = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): VerifyWebAuthNRegistration {
    return {
      publicKeyCredential: isObject(object.publicKeyCredential) ? object.publicKeyCredential : undefined,
      webAuthNName: isSet(object.webAuthNName) ? globalThis.String(object.webAuthNName) : "",
    };
  },

  toJSON(message: VerifyWebAuthNRegistration): unknown {
    const obj: any = {};
    if (message.publicKeyCredential !== undefined) {
      obj.publicKeyCredential = message.publicKeyCredential;
    }
    if (message.webAuthNName !== "") {
      obj.webAuthNName = message.webAuthNName;
    }
    return obj;
  },

  create(base?: DeepPartial<VerifyWebAuthNRegistration>): VerifyWebAuthNRegistration {
    return VerifyWebAuthNRegistration.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<VerifyWebAuthNRegistration>): VerifyWebAuthNRegistration {
    const message = createBaseVerifyWebAuthNRegistration();
    message.publicKeyCredential = object.publicKeyCredential ?? undefined;
    message.webAuthNName = object.webAuthNName ?? "";
    return message;
  },
};

function createBaseOTPSMS(): OTPSMS {
  return { otpSmsId: "", phone: "", isVerified: false };
}

export const OTPSMS: MessageFns<OTPSMS> = {
  encode(message: OTPSMS, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.otpSmsId !== "") {
      writer.uint32(10).string(message.otpSmsId);
    }
    if (message.phone !== "") {
      writer.uint32(18).string(message.phone);
    }
    if (message.isVerified !== false) {
      writer.uint32(24).bool(message.isVerified);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OTPSMS {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOTPSMS();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.otpSmsId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.phone = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.isVerified = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OTPSMS {
    return {
      otpSmsId: isSet(object.otpSmsId) ? globalThis.String(object.otpSmsId) : "",
      phone: isSet(object.phone) ? globalThis.String(object.phone) : "",
      isVerified: isSet(object.isVerified) ? globalThis.Boolean(object.isVerified) : false,
    };
  },

  toJSON(message: OTPSMS): unknown {
    const obj: any = {};
    if (message.otpSmsId !== "") {
      obj.otpSmsId = message.otpSmsId;
    }
    if (message.phone !== "") {
      obj.phone = message.phone;
    }
    if (message.isVerified !== false) {
      obj.isVerified = message.isVerified;
    }
    return obj;
  },

  create(base?: DeepPartial<OTPSMS>): OTPSMS {
    return OTPSMS.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<OTPSMS>): OTPSMS {
    const message = createBaseOTPSMS();
    message.otpSmsId = object.otpSmsId ?? "";
    message.phone = object.phone ?? "";
    message.isVerified = object.isVerified ?? false;
    return message;
  },
};

function createBaseOTPEmail(): OTPEmail {
  return { otpEmailId: "", address: "", isVerified: false };
}

export const OTPEmail: MessageFns<OTPEmail> = {
  encode(message: OTPEmail, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.otpEmailId !== "") {
      writer.uint32(10).string(message.otpEmailId);
    }
    if (message.address !== "") {
      writer.uint32(18).string(message.address);
    }
    if (message.isVerified !== false) {
      writer.uint32(24).bool(message.isVerified);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OTPEmail {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseOTPEmail();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.otpEmailId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.address = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.isVerified = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): OTPEmail {
    return {
      otpEmailId: isSet(object.otpEmailId) ? globalThis.String(object.otpEmailId) : "",
      address: isSet(object.address) ? globalThis.String(object.address) : "",
      isVerified: isSet(object.isVerified) ? globalThis.Boolean(object.isVerified) : false,
    };
  },

  toJSON(message: OTPEmail): unknown {
    const obj: any = {};
    if (message.otpEmailId !== "") {
      obj.otpEmailId = message.otpEmailId;
    }
    if (message.address !== "") {
      obj.address = message.address;
    }
    if (message.isVerified !== false) {
      obj.isVerified = message.isVerified;
    }
    return obj;
  },

  create(base?: DeepPartial<OTPEmail>): OTPEmail {
    return OTPEmail.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<OTPEmail>): OTPEmail {
    const message = createBaseOTPEmail();
    message.otpEmailId = object.otpEmailId ?? "";
    message.address = object.address ?? "";
    message.isVerified = object.isVerified ?? false;
    return message;
  },
};

function createBaseTOTP(): TOTP {
  return { totpId: "", name: "", isVerified: false };
}

export const TOTP: MessageFns<TOTP> = {
  encode(message: TOTP, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.totpId !== "") {
      writer.uint32(10).string(message.totpId);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.isVerified !== false) {
      writer.uint32(24).bool(message.isVerified);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TOTP {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseTOTP();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.totpId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.isVerified = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): TOTP {
    return {
      totpId: isSet(object.totpId) ? globalThis.String(object.totpId) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      isVerified: isSet(object.isVerified) ? globalThis.Boolean(object.isVerified) : false,
    };
  },

  toJSON(message: TOTP): unknown {
    const obj: any = {};
    if (message.totpId !== "") {
      obj.totpId = message.totpId;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.isVerified !== false) {
      obj.isVerified = message.isVerified;
    }
    return obj;
  },

  create(base?: DeepPartial<TOTP>): TOTP {
    return TOTP.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<TOTP>): TOTP {
    const message = createBaseTOTP();
    message.totpId = object.totpId ?? "";
    message.name = object.name ?? "";
    message.isVerified = object.isVerified ?? false;
    return message;
  },
};

function createBaseAuthenticationKey(): AuthenticationKey {
  return { authenticationKeyId: "", details: undefined, type: 0, expirationDate: undefined };
}

export const AuthenticationKey: MessageFns<AuthenticationKey> = {
  encode(message: AuthenticationKey, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.authenticationKeyId !== "") {
      writer.uint32(10).string(message.authenticationKeyId);
    }
    if (message.details !== undefined) {
      Details.encode(message.details, writer.uint32(18).fork()).join();
    }
    if (message.type !== 0) {
      writer.uint32(24).int32(message.type);
    }
    if (message.expirationDate !== undefined) {
      Timestamp.encode(toTimestamp(message.expirationDate), writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AuthenticationKey {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAuthenticationKey();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.authenticationKeyId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.details = Details.decode(reader, reader.uint32());
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.expirationDate = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AuthenticationKey {
    return {
      authenticationKeyId: isSet(object.authenticationKeyId) ? globalThis.String(object.authenticationKeyId) : "",
      details: isSet(object.details) ? Details.fromJSON(object.details) : undefined,
      type: isSet(object.type) ? authNKeyTypeFromJSON(object.type) : 0,
      expirationDate: isSet(object.expirationDate) ? fromJsonTimestamp(object.expirationDate) : undefined,
    };
  },

  toJSON(message: AuthenticationKey): unknown {
    const obj: any = {};
    if (message.authenticationKeyId !== "") {
      obj.authenticationKeyId = message.authenticationKeyId;
    }
    if (message.details !== undefined) {
      obj.details = Details.toJSON(message.details);
    }
    if (message.type !== 0) {
      obj.type = authNKeyTypeToJSON(message.type);
    }
    if (message.expirationDate !== undefined) {
      obj.expirationDate = message.expirationDate.toISOString();
    }
    return obj;
  },

  create(base?: DeepPartial<AuthenticationKey>): AuthenticationKey {
    return AuthenticationKey.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AuthenticationKey>): AuthenticationKey {
    const message = createBaseAuthenticationKey();
    message.authenticationKeyId = object.authenticationKeyId ?? "";
    message.details = (object.details !== undefined && object.details !== null)
      ? Details.fromPartial(object.details)
      : undefined;
    message.type = object.type ?? 0;
    message.expirationDate = object.expirationDate ?? undefined;
    return message;
  },
};

function createBaseIdentityProvider(): IdentityProvider {
  return { idpId: "", idpName: "", userId: "", username: "" };
}

export const IdentityProvider: MessageFns<IdentityProvider> = {
  encode(message: IdentityProvider, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.idpId !== "") {
      writer.uint32(10).string(message.idpId);
    }
    if (message.idpName !== "") {
      writer.uint32(26).string(message.idpName);
    }
    if (message.userId !== "") {
      writer.uint32(34).string(message.userId);
    }
    if (message.username !== "") {
      writer.uint32(42).string(message.username);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IdentityProvider {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIdentityProvider();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.idpId = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.idpName = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.userId = reader.string();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.username = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IdentityProvider {
    return {
      idpId: isSet(object.idpId) ? globalThis.String(object.idpId) : "",
      idpName: isSet(object.idpName) ? globalThis.String(object.idpName) : "",
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      username: isSet(object.username) ? globalThis.String(object.username) : "",
    };
  },

  toJSON(message: IdentityProvider): unknown {
    const obj: any = {};
    if (message.idpId !== "") {
      obj.idpId = message.idpId;
    }
    if (message.idpName !== "") {
      obj.idpName = message.idpName;
    }
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.username !== "") {
      obj.username = message.username;
    }
    return obj;
  },

  create(base?: DeepPartial<IdentityProvider>): IdentityProvider {
    return IdentityProvider.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<IdentityProvider>): IdentityProvider {
    const message = createBaseIdentityProvider();
    message.idpId = object.idpId ?? "";
    message.idpName = object.idpName ?? "";
    message.userId = object.userId ?? "";
    message.username = object.username ?? "";
    return message;
  },
};

function createBaseSetAuthenticators(): SetAuthenticators {
  return { usernames: [], password: undefined };
}

export const SetAuthenticators: MessageFns<SetAuthenticators> = {
  encode(message: SetAuthenticators, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.usernames) {
      SetUsername.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.password !== undefined) {
      SetPassword.encode(message.password, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetAuthenticators {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetAuthenticators();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.usernames.push(SetUsername.decode(reader, reader.uint32()));
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.password = SetPassword.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetAuthenticators {
    return {
      usernames: globalThis.Array.isArray(object?.usernames)
        ? object.usernames.map((e: any) => SetUsername.fromJSON(e))
        : [],
      password: isSet(object.password) ? SetPassword.fromJSON(object.password) : undefined,
    };
  },

  toJSON(message: SetAuthenticators): unknown {
    const obj: any = {};
    if (message.usernames?.length) {
      obj.usernames = message.usernames.map((e) => SetUsername.toJSON(e));
    }
    if (message.password !== undefined) {
      obj.password = SetPassword.toJSON(message.password);
    }
    return obj;
  },

  create(base?: DeepPartial<SetAuthenticators>): SetAuthenticators {
    return SetAuthenticators.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SetAuthenticators>): SetAuthenticators {
    const message = createBaseSetAuthenticators();
    message.usernames = object.usernames?.map((e) => SetUsername.fromPartial(e)) || [];
    message.password = (object.password !== undefined && object.password !== null)
      ? SetPassword.fromPartial(object.password)
      : undefined;
    return message;
  },
};

function createBaseSetPassword(): SetPassword {
  return {
    password: undefined,
    hash: undefined,
    changeRequired: false,
    currentPassword: undefined,
    verificationCode: undefined,
  };
}

export const SetPassword: MessageFns<SetPassword> = {
  encode(message: SetPassword, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.password !== undefined) {
      writer.uint32(10).string(message.password);
    }
    if (message.hash !== undefined) {
      writer.uint32(18).string(message.hash);
    }
    if (message.changeRequired !== false) {
      writer.uint32(24).bool(message.changeRequired);
    }
    if (message.currentPassword !== undefined) {
      writer.uint32(34).string(message.currentPassword);
    }
    if (message.verificationCode !== undefined) {
      writer.uint32(42).string(message.verificationCode);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SetPassword {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSetPassword();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.password = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.hash = reader.string();
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.changeRequired = reader.bool();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.currentPassword = reader.string();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.verificationCode = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SetPassword {
    return {
      password: isSet(object.password) ? globalThis.String(object.password) : undefined,
      hash: isSet(object.hash) ? globalThis.String(object.hash) : undefined,
      changeRequired: isSet(object.changeRequired) ? globalThis.Boolean(object.changeRequired) : false,
      currentPassword: isSet(object.currentPassword) ? globalThis.String(object.currentPassword) : undefined,
      verificationCode: isSet(object.verificationCode) ? globalThis.String(object.verificationCode) : undefined,
    };
  },

  toJSON(message: SetPassword): unknown {
    const obj: any = {};
    if (message.password !== undefined) {
      obj.password = message.password;
    }
    if (message.hash !== undefined) {
      obj.hash = message.hash;
    }
    if (message.changeRequired !== false) {
      obj.changeRequired = message.changeRequired;
    }
    if (message.currentPassword !== undefined) {
      obj.currentPassword = message.currentPassword;
    }
    if (message.verificationCode !== undefined) {
      obj.verificationCode = message.verificationCode;
    }
    return obj;
  },

  create(base?: DeepPartial<SetPassword>): SetPassword {
    return SetPassword.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SetPassword>): SetPassword {
    const message = createBaseSetPassword();
    message.password = object.password ?? undefined;
    message.hash = object.hash ?? undefined;
    message.changeRequired = object.changeRequired ?? false;
    message.currentPassword = object.currentPassword ?? undefined;
    message.verificationCode = object.verificationCode ?? undefined;
    return message;
  },
};

function createBaseSendPasswordResetEmail(): SendPasswordResetEmail {
  return { urlTemplate: undefined };
}

export const SendPasswordResetEmail: MessageFns<SendPasswordResetEmail> = {
  encode(message: SendPasswordResetEmail, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.urlTemplate !== undefined) {
      writer.uint32(18).string(message.urlTemplate);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SendPasswordResetEmail {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSendPasswordResetEmail();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 2:
          if (tag !== 18) {
            break;
          }

          message.urlTemplate = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SendPasswordResetEmail {
    return { urlTemplate: isSet(object.urlTemplate) ? globalThis.String(object.urlTemplate) : undefined };
  },

  toJSON(message: SendPasswordResetEmail): unknown {
    const obj: any = {};
    if (message.urlTemplate !== undefined) {
      obj.urlTemplate = message.urlTemplate;
    }
    return obj;
  },

  create(base?: DeepPartial<SendPasswordResetEmail>): SendPasswordResetEmail {
    return SendPasswordResetEmail.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SendPasswordResetEmail>): SendPasswordResetEmail {
    const message = createBaseSendPasswordResetEmail();
    message.urlTemplate = object.urlTemplate ?? undefined;
    return message;
  },
};

function createBaseSendPasswordResetSMS(): SendPasswordResetSMS {
  return {};
}

export const SendPasswordResetSMS: MessageFns<SendPasswordResetSMS> = {
  encode(_: SendPasswordResetSMS, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SendPasswordResetSMS {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSendPasswordResetSMS();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): SendPasswordResetSMS {
    return {};
  },

  toJSON(_: SendPasswordResetSMS): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<SendPasswordResetSMS>): SendPasswordResetSMS {
    return SendPasswordResetSMS.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<SendPasswordResetSMS>): SendPasswordResetSMS {
    const message = createBaseSendPasswordResetSMS();
    return message;
  },
};

function createBaseReturnPasswordResetCode(): ReturnPasswordResetCode {
  return {};
}

export const ReturnPasswordResetCode: MessageFns<ReturnPasswordResetCode> = {
  encode(_: ReturnPasswordResetCode, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReturnPasswordResetCode {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReturnPasswordResetCode();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ReturnPasswordResetCode {
    return {};
  },

  toJSON(_: ReturnPasswordResetCode): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<ReturnPasswordResetCode>): ReturnPasswordResetCode {
    return ReturnPasswordResetCode.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<ReturnPasswordResetCode>): ReturnPasswordResetCode {
    const message = createBaseReturnPasswordResetCode();
    return message;
  },
};

function createBaseAuthenticatorRegistrationCode(): AuthenticatorRegistrationCode {
  return { id: "", code: "" };
}

export const AuthenticatorRegistrationCode: MessageFns<AuthenticatorRegistrationCode> = {
  encode(message: AuthenticatorRegistrationCode, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.code !== "") {
      writer.uint32(18).string(message.code);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AuthenticatorRegistrationCode {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAuthenticatorRegistrationCode();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.code = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AuthenticatorRegistrationCode {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      code: isSet(object.code) ? globalThis.String(object.code) : "",
    };
  },

  toJSON(message: AuthenticatorRegistrationCode): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.code !== "") {
      obj.code = message.code;
    }
    return obj;
  },

  create(base?: DeepPartial<AuthenticatorRegistrationCode>): AuthenticatorRegistrationCode {
    return AuthenticatorRegistrationCode.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AuthenticatorRegistrationCode>): AuthenticatorRegistrationCode {
    const message = createBaseAuthenticatorRegistrationCode();
    message.id = object.id ?? "";
    message.code = object.code ?? "";
    return message;
  },
};

function createBaseSendWebAuthNRegistrationLink(): SendWebAuthNRegistrationLink {
  return { urlTemplate: undefined };
}

export const SendWebAuthNRegistrationLink: MessageFns<SendWebAuthNRegistrationLink> = {
  encode(message: SendWebAuthNRegistrationLink, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.urlTemplate !== undefined) {
      writer.uint32(10).string(message.urlTemplate);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SendWebAuthNRegistrationLink {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSendWebAuthNRegistrationLink();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.urlTemplate = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SendWebAuthNRegistrationLink {
    return { urlTemplate: isSet(object.urlTemplate) ? globalThis.String(object.urlTemplate) : undefined };
  },

  toJSON(message: SendWebAuthNRegistrationLink): unknown {
    const obj: any = {};
    if (message.urlTemplate !== undefined) {
      obj.urlTemplate = message.urlTemplate;
    }
    return obj;
  },

  create(base?: DeepPartial<SendWebAuthNRegistrationLink>): SendWebAuthNRegistrationLink {
    return SendWebAuthNRegistrationLink.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SendWebAuthNRegistrationLink>): SendWebAuthNRegistrationLink {
    const message = createBaseSendWebAuthNRegistrationLink();
    message.urlTemplate = object.urlTemplate ?? undefined;
    return message;
  },
};

function createBaseReturnWebAuthNRegistrationCode(): ReturnWebAuthNRegistrationCode {
  return {};
}

export const ReturnWebAuthNRegistrationCode: MessageFns<ReturnWebAuthNRegistrationCode> = {
  encode(_: ReturnWebAuthNRegistrationCode, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReturnWebAuthNRegistrationCode {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReturnWebAuthNRegistrationCode();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): ReturnWebAuthNRegistrationCode {
    return {};
  },

  toJSON(_: ReturnWebAuthNRegistrationCode): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<ReturnWebAuthNRegistrationCode>): ReturnWebAuthNRegistrationCode {
    return ReturnWebAuthNRegistrationCode.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<ReturnWebAuthNRegistrationCode>): ReturnWebAuthNRegistrationCode {
    const message = createBaseReturnWebAuthNRegistrationCode();
    return message;
  },
};

function createBaseRedirectURLs(): RedirectURLs {
  return { successUrl: "", failureUrl: "" };
}

export const RedirectURLs: MessageFns<RedirectURLs> = {
  encode(message: RedirectURLs, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.successUrl !== "") {
      writer.uint32(10).string(message.successUrl);
    }
    if (message.failureUrl !== "") {
      writer.uint32(18).string(message.failureUrl);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RedirectURLs {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRedirectURLs();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.successUrl = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.failureUrl = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RedirectURLs {
    return {
      successUrl: isSet(object.successUrl) ? globalThis.String(object.successUrl) : "",
      failureUrl: isSet(object.failureUrl) ? globalThis.String(object.failureUrl) : "",
    };
  },

  toJSON(message: RedirectURLs): unknown {
    const obj: any = {};
    if (message.successUrl !== "") {
      obj.successUrl = message.successUrl;
    }
    if (message.failureUrl !== "") {
      obj.failureUrl = message.failureUrl;
    }
    return obj;
  },

  create(base?: DeepPartial<RedirectURLs>): RedirectURLs {
    return RedirectURLs.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RedirectURLs>): RedirectURLs {
    const message = createBaseRedirectURLs();
    message.successUrl = object.successUrl ?? "";
    message.failureUrl = object.failureUrl ?? "";
    return message;
  },
};

function createBaseLDAPCredentials(): LDAPCredentials {
  return { username: "", password: "" };
}

export const LDAPCredentials: MessageFns<LDAPCredentials> = {
  encode(message: LDAPCredentials, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.username !== "") {
      writer.uint32(10).string(message.username);
    }
    if (message.password !== "") {
      writer.uint32(18).string(message.password);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LDAPCredentials {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLDAPCredentials();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.username = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.password = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): LDAPCredentials {
    return {
      username: isSet(object.username) ? globalThis.String(object.username) : "",
      password: isSet(object.password) ? globalThis.String(object.password) : "",
    };
  },

  toJSON(message: LDAPCredentials): unknown {
    const obj: any = {};
    if (message.username !== "") {
      obj.username = message.username;
    }
    if (message.password !== "") {
      obj.password = message.password;
    }
    return obj;
  },

  create(base?: DeepPartial<LDAPCredentials>): LDAPCredentials {
    return LDAPCredentials.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<LDAPCredentials>): LDAPCredentials {
    const message = createBaseLDAPCredentials();
    message.username = object.username ?? "";
    message.password = object.password ?? "";
    return message;
  },
};

function createBaseIdentityProviderIntent(): IdentityProviderIntent {
  return { idpIntentId: "", idpIntentToken: "", userId: undefined };
}

export const IdentityProviderIntent: MessageFns<IdentityProviderIntent> = {
  encode(message: IdentityProviderIntent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.idpIntentId !== "") {
      writer.uint32(10).string(message.idpIntentId);
    }
    if (message.idpIntentToken !== "") {
      writer.uint32(18).string(message.idpIntentToken);
    }
    if (message.userId !== undefined) {
      writer.uint32(26).string(message.userId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IdentityProviderIntent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIdentityProviderIntent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.idpIntentId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.idpIntentToken = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.userId = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IdentityProviderIntent {
    return {
      idpIntentId: isSet(object.idpIntentId) ? globalThis.String(object.idpIntentId) : "",
      idpIntentToken: isSet(object.idpIntentToken) ? globalThis.String(object.idpIntentToken) : "",
      userId: isSet(object.userId) ? globalThis.String(object.userId) : undefined,
    };
  },

  toJSON(message: IdentityProviderIntent): unknown {
    const obj: any = {};
    if (message.idpIntentId !== "") {
      obj.idpIntentId = message.idpIntentId;
    }
    if (message.idpIntentToken !== "") {
      obj.idpIntentToken = message.idpIntentToken;
    }
    if (message.userId !== undefined) {
      obj.userId = message.userId;
    }
    return obj;
  },

  create(base?: DeepPartial<IdentityProviderIntent>): IdentityProviderIntent {
    return IdentityProviderIntent.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<IdentityProviderIntent>): IdentityProviderIntent {
    const message = createBaseIdentityProviderIntent();
    message.idpIntentId = object.idpIntentId ?? "";
    message.idpIntentToken = object.idpIntentToken ?? "";
    message.userId = object.userId ?? undefined;
    return message;
  },
};

function createBaseIDPInformation(): IDPInformation {
  return {
    idpId: "",
    userId: "",
    userName: "",
    rawInformation: undefined,
    oauth: undefined,
    ldap: undefined,
    saml: undefined,
  };
}

export const IDPInformation: MessageFns<IDPInformation> = {
  encode(message: IDPInformation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.idpId !== "") {
      writer.uint32(10).string(message.idpId);
    }
    if (message.userId !== "") {
      writer.uint32(18).string(message.userId);
    }
    if (message.userName !== "") {
      writer.uint32(26).string(message.userName);
    }
    if (message.rawInformation !== undefined) {
      Struct.encode(Struct.wrap(message.rawInformation), writer.uint32(34).fork()).join();
    }
    if (message.oauth !== undefined) {
      IDPOAuthAccessInformation.encode(message.oauth, writer.uint32(42).fork()).join();
    }
    if (message.ldap !== undefined) {
      IDPLDAPAccessInformation.encode(message.ldap, writer.uint32(50).fork()).join();
    }
    if (message.saml !== undefined) {
      IDPSAMLAccessInformation.encode(message.saml, writer.uint32(58).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IDPInformation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIDPInformation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.idpId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.userId = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.userName = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.rawInformation = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.oauth = IDPOAuthAccessInformation.decode(reader, reader.uint32());
          continue;
        case 6:
          if (tag !== 50) {
            break;
          }

          message.ldap = IDPLDAPAccessInformation.decode(reader, reader.uint32());
          continue;
        case 7:
          if (tag !== 58) {
            break;
          }

          message.saml = IDPSAMLAccessInformation.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IDPInformation {
    return {
      idpId: isSet(object.idpId) ? globalThis.String(object.idpId) : "",
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      userName: isSet(object.userName) ? globalThis.String(object.userName) : "",
      rawInformation: isObject(object.rawInformation) ? object.rawInformation : undefined,
      oauth: isSet(object.oauth) ? IDPOAuthAccessInformation.fromJSON(object.oauth) : undefined,
      ldap: isSet(object.ldap) ? IDPLDAPAccessInformation.fromJSON(object.ldap) : undefined,
      saml: isSet(object.saml) ? IDPSAMLAccessInformation.fromJSON(object.saml) : undefined,
    };
  },

  toJSON(message: IDPInformation): unknown {
    const obj: any = {};
    if (message.idpId !== "") {
      obj.idpId = message.idpId;
    }
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.userName !== "") {
      obj.userName = message.userName;
    }
    if (message.rawInformation !== undefined) {
      obj.rawInformation = message.rawInformation;
    }
    if (message.oauth !== undefined) {
      obj.oauth = IDPOAuthAccessInformation.toJSON(message.oauth);
    }
    if (message.ldap !== undefined) {
      obj.ldap = IDPLDAPAccessInformation.toJSON(message.ldap);
    }
    if (message.saml !== undefined) {
      obj.saml = IDPSAMLAccessInformation.toJSON(message.saml);
    }
    return obj;
  },

  create(base?: DeepPartial<IDPInformation>): IDPInformation {
    return IDPInformation.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<IDPInformation>): IDPInformation {
    const message = createBaseIDPInformation();
    message.idpId = object.idpId ?? "";
    message.userId = object.userId ?? "";
    message.userName = object.userName ?? "";
    message.rawInformation = object.rawInformation ?? undefined;
    message.oauth = (object.oauth !== undefined && object.oauth !== null)
      ? IDPOAuthAccessInformation.fromPartial(object.oauth)
      : undefined;
    message.ldap = (object.ldap !== undefined && object.ldap !== null)
      ? IDPLDAPAccessInformation.fromPartial(object.ldap)
      : undefined;
    message.saml = (object.saml !== undefined && object.saml !== null)
      ? IDPSAMLAccessInformation.fromPartial(object.saml)
      : undefined;
    return message;
  },
};

function createBaseIDPOAuthAccessInformation(): IDPOAuthAccessInformation {
  return { accessToken: "", idToken: undefined };
}

export const IDPOAuthAccessInformation: MessageFns<IDPOAuthAccessInformation> = {
  encode(message: IDPOAuthAccessInformation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.accessToken !== "") {
      writer.uint32(10).string(message.accessToken);
    }
    if (message.idToken !== undefined) {
      writer.uint32(18).string(message.idToken);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IDPOAuthAccessInformation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIDPOAuthAccessInformation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.accessToken = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.idToken = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IDPOAuthAccessInformation {
    return {
      accessToken: isSet(object.accessToken) ? globalThis.String(object.accessToken) : "",
      idToken: isSet(object.idToken) ? globalThis.String(object.idToken) : undefined,
    };
  },

  toJSON(message: IDPOAuthAccessInformation): unknown {
    const obj: any = {};
    if (message.accessToken !== "") {
      obj.accessToken = message.accessToken;
    }
    if (message.idToken !== undefined) {
      obj.idToken = message.idToken;
    }
    return obj;
  },

  create(base?: DeepPartial<IDPOAuthAccessInformation>): IDPOAuthAccessInformation {
    return IDPOAuthAccessInformation.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<IDPOAuthAccessInformation>): IDPOAuthAccessInformation {
    const message = createBaseIDPOAuthAccessInformation();
    message.accessToken = object.accessToken ?? "";
    message.idToken = object.idToken ?? undefined;
    return message;
  },
};

function createBaseIDPLDAPAccessInformation(): IDPLDAPAccessInformation {
  return { attributes: undefined };
}

export const IDPLDAPAccessInformation: MessageFns<IDPLDAPAccessInformation> = {
  encode(message: IDPLDAPAccessInformation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.attributes !== undefined) {
      Struct.encode(Struct.wrap(message.attributes), writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IDPLDAPAccessInformation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIDPLDAPAccessInformation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.attributes = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IDPLDAPAccessInformation {
    return { attributes: isObject(object.attributes) ? object.attributes : undefined };
  },

  toJSON(message: IDPLDAPAccessInformation): unknown {
    const obj: any = {};
    if (message.attributes !== undefined) {
      obj.attributes = message.attributes;
    }
    return obj;
  },

  create(base?: DeepPartial<IDPLDAPAccessInformation>): IDPLDAPAccessInformation {
    return IDPLDAPAccessInformation.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<IDPLDAPAccessInformation>): IDPLDAPAccessInformation {
    const message = createBaseIDPLDAPAccessInformation();
    message.attributes = object.attributes ?? undefined;
    return message;
  },
};

function createBaseIDPSAMLAccessInformation(): IDPSAMLAccessInformation {
  return { assertion: Buffer.alloc(0) };
}

export const IDPSAMLAccessInformation: MessageFns<IDPSAMLAccessInformation> = {
  encode(message: IDPSAMLAccessInformation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.assertion.length !== 0) {
      writer.uint32(10).bytes(message.assertion);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IDPSAMLAccessInformation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIDPSAMLAccessInformation();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.assertion = Buffer.from(reader.bytes());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IDPSAMLAccessInformation {
    return { assertion: isSet(object.assertion) ? Buffer.from(bytesFromBase64(object.assertion)) : Buffer.alloc(0) };
  },

  toJSON(message: IDPSAMLAccessInformation): unknown {
    const obj: any = {};
    if (message.assertion.length !== 0) {
      obj.assertion = base64FromBytes(message.assertion);
    }
    return obj;
  },

  create(base?: DeepPartial<IDPSAMLAccessInformation>): IDPSAMLAccessInformation {
    return IDPSAMLAccessInformation.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<IDPSAMLAccessInformation>): IDPSAMLAccessInformation {
    const message = createBaseIDPSAMLAccessInformation();
    message.assertion = object.assertion ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseIDPAuthenticator(): IDPAuthenticator {
  return { idpId: "", userId: "", userName: "" };
}

export const IDPAuthenticator: MessageFns<IDPAuthenticator> = {
  encode(message: IDPAuthenticator, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.idpId !== "") {
      writer.uint32(10).string(message.idpId);
    }
    if (message.userId !== "") {
      writer.uint32(18).string(message.userId);
    }
    if (message.userName !== "") {
      writer.uint32(26).string(message.userName);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IDPAuthenticator {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIDPAuthenticator();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.idpId = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.userId = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.userName = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IDPAuthenticator {
    return {
      idpId: isSet(object.idpId) ? globalThis.String(object.idpId) : "",
      userId: isSet(object.userId) ? globalThis.String(object.userId) : "",
      userName: isSet(object.userName) ? globalThis.String(object.userName) : "",
    };
  },

  toJSON(message: IDPAuthenticator): unknown {
    const obj: any = {};
    if (message.idpId !== "") {
      obj.idpId = message.idpId;
    }
    if (message.userId !== "") {
      obj.userId = message.userId;
    }
    if (message.userName !== "") {
      obj.userName = message.userName;
    }
    return obj;
  },

  create(base?: DeepPartial<IDPAuthenticator>): IDPAuthenticator {
    return IDPAuthenticator.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<IDPAuthenticator>): IDPAuthenticator {
    const message = createBaseIDPAuthenticator();
    message.idpId = object.idpId ?? "";
    message.userId = object.userId ?? "";
    message.userName = object.userName ?? "";
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
}

function base64FromBytes(arr: Uint8Array): string {
  return globalThis.Buffer.from(arr).toString("base64");
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = numberToLong(Math.trunc(date.getTime() / 1_000));
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds.toNumber() || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function numberToLong(number: number) {
  return Long.fromNumber(number);
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

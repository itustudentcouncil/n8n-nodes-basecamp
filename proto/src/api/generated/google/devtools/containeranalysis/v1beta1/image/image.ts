// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.0
//   protoc               unknown
// source: google/devtools/containeranalysis/v1beta1/image/image.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";

export const protobufPackage = "grafeas.v1beta1.image";

/** Layer holds metadata specific to a layer of a Docker image. */
export interface Layer {
  /** Required. The recovered Dockerfile directive used to construct this layer. */
  directive: Layer_Directive;
  /** The recovered arguments to the Dockerfile directive. */
  arguments: string;
}

/** Instructions from Dockerfile. */
export enum Layer_Directive {
  /** DIRECTIVE_UNSPECIFIED - Default value for unsupported/missing directive. */
  DIRECTIVE_UNSPECIFIED = 0,
  /** MAINTAINER - https://docs.docker.com/engine/reference/builder/ */
  MAINTAINER = 1,
  /** RUN - https://docs.docker.com/engine/reference/builder/ */
  RUN = 2,
  /** CMD - https://docs.docker.com/engine/reference/builder/ */
  CMD = 3,
  /** LABEL - https://docs.docker.com/engine/reference/builder/ */
  LABEL = 4,
  /** EXPOSE - https://docs.docker.com/engine/reference/builder/ */
  EXPOSE = 5,
  /** ENV - https://docs.docker.com/engine/reference/builder/ */
  ENV = 6,
  /** ADD - https://docs.docker.com/engine/reference/builder/ */
  ADD = 7,
  /** COPY - https://docs.docker.com/engine/reference/builder/ */
  COPY = 8,
  /** ENTRYPOINT - https://docs.docker.com/engine/reference/builder/ */
  ENTRYPOINT = 9,
  /** VOLUME - https://docs.docker.com/engine/reference/builder/ */
  VOLUME = 10,
  /** USER - https://docs.docker.com/engine/reference/builder/ */
  USER = 11,
  /** WORKDIR - https://docs.docker.com/engine/reference/builder/ */
  WORKDIR = 12,
  /** ARG - https://docs.docker.com/engine/reference/builder/ */
  ARG = 13,
  /** ONBUILD - https://docs.docker.com/engine/reference/builder/ */
  ONBUILD = 14,
  /** STOPSIGNAL - https://docs.docker.com/engine/reference/builder/ */
  STOPSIGNAL = 15,
  /** HEALTHCHECK - https://docs.docker.com/engine/reference/builder/ */
  HEALTHCHECK = 16,
  /** SHELL - https://docs.docker.com/engine/reference/builder/ */
  SHELL = 17,
  UNRECOGNIZED = -1,
}

export function layer_DirectiveFromJSON(object: any): Layer_Directive {
  switch (object) {
    case 0:
    case "DIRECTIVE_UNSPECIFIED":
      return Layer_Directive.DIRECTIVE_UNSPECIFIED;
    case 1:
    case "MAINTAINER":
      return Layer_Directive.MAINTAINER;
    case 2:
    case "RUN":
      return Layer_Directive.RUN;
    case 3:
    case "CMD":
      return Layer_Directive.CMD;
    case 4:
    case "LABEL":
      return Layer_Directive.LABEL;
    case 5:
    case "EXPOSE":
      return Layer_Directive.EXPOSE;
    case 6:
    case "ENV":
      return Layer_Directive.ENV;
    case 7:
    case "ADD":
      return Layer_Directive.ADD;
    case 8:
    case "COPY":
      return Layer_Directive.COPY;
    case 9:
    case "ENTRYPOINT":
      return Layer_Directive.ENTRYPOINT;
    case 10:
    case "VOLUME":
      return Layer_Directive.VOLUME;
    case 11:
    case "USER":
      return Layer_Directive.USER;
    case 12:
    case "WORKDIR":
      return Layer_Directive.WORKDIR;
    case 13:
    case "ARG":
      return Layer_Directive.ARG;
    case 14:
    case "ONBUILD":
      return Layer_Directive.ONBUILD;
    case 15:
    case "STOPSIGNAL":
      return Layer_Directive.STOPSIGNAL;
    case 16:
    case "HEALTHCHECK":
      return Layer_Directive.HEALTHCHECK;
    case 17:
    case "SHELL":
      return Layer_Directive.SHELL;
    case -1:
    case "UNRECOGNIZED":
    default:
      return Layer_Directive.UNRECOGNIZED;
  }
}

export function layer_DirectiveToJSON(object: Layer_Directive): string {
  switch (object) {
    case Layer_Directive.DIRECTIVE_UNSPECIFIED:
      return "DIRECTIVE_UNSPECIFIED";
    case Layer_Directive.MAINTAINER:
      return "MAINTAINER";
    case Layer_Directive.RUN:
      return "RUN";
    case Layer_Directive.CMD:
      return "CMD";
    case Layer_Directive.LABEL:
      return "LABEL";
    case Layer_Directive.EXPOSE:
      return "EXPOSE";
    case Layer_Directive.ENV:
      return "ENV";
    case Layer_Directive.ADD:
      return "ADD";
    case Layer_Directive.COPY:
      return "COPY";
    case Layer_Directive.ENTRYPOINT:
      return "ENTRYPOINT";
    case Layer_Directive.VOLUME:
      return "VOLUME";
    case Layer_Directive.USER:
      return "USER";
    case Layer_Directive.WORKDIR:
      return "WORKDIR";
    case Layer_Directive.ARG:
      return "ARG";
    case Layer_Directive.ONBUILD:
      return "ONBUILD";
    case Layer_Directive.STOPSIGNAL:
      return "STOPSIGNAL";
    case Layer_Directive.HEALTHCHECK:
      return "HEALTHCHECK";
    case Layer_Directive.SHELL:
      return "SHELL";
    case Layer_Directive.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** A set of properties that uniquely identify a given Docker image. */
export interface Fingerprint {
  /**
   * Required. The layer ID of the final layer in the Docker image's v1
   * representation.
   */
  v1Name: string;
  /** Required. The ordered list of v2 blobs that represent a given image. */
  v2Blob: string[];
  /**
   * Output only. The name of the image's v2 blobs computed via:
   *   [bottom] := v2_blob[bottom]
   *   [N] := sha256(v2_blob[N] + " " + v2_name[N+1])
   * Only the name of the final blob is kept.
   */
  v2Name: string;
}

/**
 * Basis describes the base image portion (Note) of the DockerImage
 * relationship. Linked occurrences are derived from this or an
 * equivalent image via:
 *   FROM <Basis.resource_url>
 * Or an equivalent reference, e.g. a tag of the resource_url.
 */
export interface Basis {
  /**
   * Required. Immutable. The resource_url for the resource representing the
   * basis of associated occurrence images.
   */
  resourceUrl: string;
  /** Required. Immutable. The fingerprint of the base image. */
  fingerprint: Fingerprint | undefined;
}

/** Details of an image occurrence. */
export interface Details {
  /** Required. Immutable. The child image derived from the base image. */
  derivedImage: Derived | undefined;
}

/**
 * Derived describes the derived image portion (Occurrence) of the DockerImage
 * relationship. This image would be produced from a Dockerfile with FROM
 * <DockerImage.Basis in attached Note>.
 */
export interface Derived {
  /** Required. The fingerprint of the derived image. */
  fingerprint:
    | Fingerprint
    | undefined;
  /**
   * Output only. The number of layers by which this image differs from the
   * associated image basis.
   */
  distance: number;
  /**
   * This contains layer-specific metadata, if populated it has length
   * "distance" and is ordered with [distance] being the layer immediately
   * following the base image and [1] being the final layer.
   */
  layerInfo: Layer[];
  /**
   * Output only. This contains the base image URL for the derived image
   * occurrence.
   */
  baseResourceUrl: string;
}

function createBaseLayer(): Layer {
  return { directive: 0, arguments: "" };
}

export const Layer: MessageFns<Layer> = {
  encode(message: Layer, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.directive !== 0) {
      writer.uint32(8).int32(message.directive);
    }
    if (message.arguments !== "") {
      writer.uint32(18).string(message.arguments);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Layer {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseLayer();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.directive = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.arguments = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Layer {
    return {
      directive: isSet(object.directive) ? layer_DirectiveFromJSON(object.directive) : 0,
      arguments: isSet(object.arguments) ? globalThis.String(object.arguments) : "",
    };
  },

  toJSON(message: Layer): unknown {
    const obj: any = {};
    if (message.directive !== 0) {
      obj.directive = layer_DirectiveToJSON(message.directive);
    }
    if (message.arguments !== "") {
      obj.arguments = message.arguments;
    }
    return obj;
  },

  create(base?: DeepPartial<Layer>): Layer {
    return Layer.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Layer>): Layer {
    const message = createBaseLayer();
    message.directive = object.directive ?? 0;
    message.arguments = object.arguments ?? "";
    return message;
  },
};

function createBaseFingerprint(): Fingerprint {
  return { v1Name: "", v2Blob: [], v2Name: "" };
}

export const Fingerprint: MessageFns<Fingerprint> = {
  encode(message: Fingerprint, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.v1Name !== "") {
      writer.uint32(10).string(message.v1Name);
    }
    for (const v of message.v2Blob) {
      writer.uint32(18).string(v!);
    }
    if (message.v2Name !== "") {
      writer.uint32(26).string(message.v2Name);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Fingerprint {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseFingerprint();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.v1Name = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.v2Blob.push(reader.string());
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.v2Name = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Fingerprint {
    return {
      v1Name: isSet(object.v1Name) ? globalThis.String(object.v1Name) : "",
      v2Blob: globalThis.Array.isArray(object?.v2Blob) ? object.v2Blob.map((e: any) => globalThis.String(e)) : [],
      v2Name: isSet(object.v2Name) ? globalThis.String(object.v2Name) : "",
    };
  },

  toJSON(message: Fingerprint): unknown {
    const obj: any = {};
    if (message.v1Name !== "") {
      obj.v1Name = message.v1Name;
    }
    if (message.v2Blob?.length) {
      obj.v2Blob = message.v2Blob;
    }
    if (message.v2Name !== "") {
      obj.v2Name = message.v2Name;
    }
    return obj;
  },

  create(base?: DeepPartial<Fingerprint>): Fingerprint {
    return Fingerprint.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Fingerprint>): Fingerprint {
    const message = createBaseFingerprint();
    message.v1Name = object.v1Name ?? "";
    message.v2Blob = object.v2Blob?.map((e) => e) || [];
    message.v2Name = object.v2Name ?? "";
    return message;
  },
};

function createBaseBasis(): Basis {
  return { resourceUrl: "", fingerprint: undefined };
}

export const Basis: MessageFns<Basis> = {
  encode(message: Basis, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.resourceUrl !== "") {
      writer.uint32(10).string(message.resourceUrl);
    }
    if (message.fingerprint !== undefined) {
      Fingerprint.encode(message.fingerprint, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Basis {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBasis();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.resourceUrl = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.fingerprint = Fingerprint.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Basis {
    return {
      resourceUrl: isSet(object.resourceUrl) ? globalThis.String(object.resourceUrl) : "",
      fingerprint: isSet(object.fingerprint) ? Fingerprint.fromJSON(object.fingerprint) : undefined,
    };
  },

  toJSON(message: Basis): unknown {
    const obj: any = {};
    if (message.resourceUrl !== "") {
      obj.resourceUrl = message.resourceUrl;
    }
    if (message.fingerprint !== undefined) {
      obj.fingerprint = Fingerprint.toJSON(message.fingerprint);
    }
    return obj;
  },

  create(base?: DeepPartial<Basis>): Basis {
    return Basis.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Basis>): Basis {
    const message = createBaseBasis();
    message.resourceUrl = object.resourceUrl ?? "";
    message.fingerprint = (object.fingerprint !== undefined && object.fingerprint !== null)
      ? Fingerprint.fromPartial(object.fingerprint)
      : undefined;
    return message;
  },
};

function createBaseDetails(): Details {
  return { derivedImage: undefined };
}

export const Details: MessageFns<Details> = {
  encode(message: Details, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.derivedImage !== undefined) {
      Derived.encode(message.derivedImage, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Details {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDetails();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.derivedImage = Derived.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Details {
    return { derivedImage: isSet(object.derivedImage) ? Derived.fromJSON(object.derivedImage) : undefined };
  },

  toJSON(message: Details): unknown {
    const obj: any = {};
    if (message.derivedImage !== undefined) {
      obj.derivedImage = Derived.toJSON(message.derivedImage);
    }
    return obj;
  },

  create(base?: DeepPartial<Details>): Details {
    return Details.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Details>): Details {
    const message = createBaseDetails();
    message.derivedImage = (object.derivedImage !== undefined && object.derivedImage !== null)
      ? Derived.fromPartial(object.derivedImage)
      : undefined;
    return message;
  },
};

function createBaseDerived(): Derived {
  return { fingerprint: undefined, distance: 0, layerInfo: [], baseResourceUrl: "" };
}

export const Derived: MessageFns<Derived> = {
  encode(message: Derived, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.fingerprint !== undefined) {
      Fingerprint.encode(message.fingerprint, writer.uint32(10).fork()).join();
    }
    if (message.distance !== 0) {
      writer.uint32(16).int32(message.distance);
    }
    for (const v of message.layerInfo) {
      Layer.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.baseResourceUrl !== "") {
      writer.uint32(34).string(message.baseResourceUrl);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Derived {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDerived();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.fingerprint = Fingerprint.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.distance = reader.int32();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.layerInfo.push(Layer.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.baseResourceUrl = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Derived {
    return {
      fingerprint: isSet(object.fingerprint) ? Fingerprint.fromJSON(object.fingerprint) : undefined,
      distance: isSet(object.distance) ? globalThis.Number(object.distance) : 0,
      layerInfo: globalThis.Array.isArray(object?.layerInfo) ? object.layerInfo.map((e: any) => Layer.fromJSON(e)) : [],
      baseResourceUrl: isSet(object.baseResourceUrl) ? globalThis.String(object.baseResourceUrl) : "",
    };
  },

  toJSON(message: Derived): unknown {
    const obj: any = {};
    if (message.fingerprint !== undefined) {
      obj.fingerprint = Fingerprint.toJSON(message.fingerprint);
    }
    if (message.distance !== 0) {
      obj.distance = Math.round(message.distance);
    }
    if (message.layerInfo?.length) {
      obj.layerInfo = message.layerInfo.map((e) => Layer.toJSON(e));
    }
    if (message.baseResourceUrl !== "") {
      obj.baseResourceUrl = message.baseResourceUrl;
    }
    return obj;
  },

  create(base?: DeepPartial<Derived>): Derived {
    return Derived.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Derived>): Derived {
    const message = createBaseDerived();
    message.fingerprint = (object.fingerprint !== undefined && object.fingerprint !== null)
      ? Fingerprint.fromPartial(object.fingerprint)
      : undefined;
    message.distance = object.distance ?? 0;
    message.layerInfo = object.layerInfo?.map((e) => Layer.fromPartial(e)) || [];
    message.baseResourceUrl = object.baseResourceUrl ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.0
//   protoc               unknown
// source: google/monitoring/dashboard/v1/single_view_group.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";

export const protobufPackage = "google.monitoring.dashboard.v1";

/**
 * A widget that groups the other widgets by using a dropdown menu. All widgets
 * that are within the area spanned by the grouping widget are considered
 * member widgets.
 */
export interface SingleViewGroup {
}

function createBaseSingleViewGroup(): SingleViewGroup {
  return {};
}

export const SingleViewGroup: MessageFns<SingleViewGroup> = {
  encode(_: SingleViewGroup, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SingleViewGroup {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSingleViewGroup();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): SingleViewGroup {
    return {};
  },

  toJSON(_: SingleViewGroup): unknown {
    const obj: any = {};
    return obj;
  },

  create(base?: DeepPartial<SingleViewGroup>): SingleViewGroup {
    return SingleViewGroup.fromPartial(base ?? {});
  },
  fromPartial(_: DeepPartial<SingleViewGroup>): SingleViewGroup {
    const message = createBaseSingleViewGroup();
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

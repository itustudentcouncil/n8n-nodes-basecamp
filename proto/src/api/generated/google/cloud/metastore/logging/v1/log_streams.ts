// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.0
//   protoc               unknown
// source: google/cloud/metastore/logging/v1/log_streams.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";

export const protobufPackage = "google.cloud.metastore.logging.v1";

/**
 * Stackdriver structured-payload for events generated from Hive Metastore
 * API requests.
 */
export interface RequestsLogEntry {
  /** A free-text string describing the request. */
  message: string;
}

/**
 * Stackdriver structured-payload for events generated from Hive Metastore
 * system activity.
 */
export interface SystemActivityLogEntry {
  /** A free-text string describing the system activity. */
  message: string;
}

function createBaseRequestsLogEntry(): RequestsLogEntry {
  return { message: "" };
}

export const RequestsLogEntry: MessageFns<RequestsLogEntry> = {
  encode(message: RequestsLogEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.message !== "") {
      writer.uint32(10).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RequestsLogEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRequestsLogEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.message = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RequestsLogEntry {
    return { message: isSet(object.message) ? globalThis.String(object.message) : "" };
  },

  toJSON(message: RequestsLogEntry): unknown {
    const obj: any = {};
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create(base?: DeepPartial<RequestsLogEntry>): RequestsLogEntry {
    return RequestsLogEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<RequestsLogEntry>): RequestsLogEntry {
    const message = createBaseRequestsLogEntry();
    message.message = object.message ?? "";
    return message;
  },
};

function createBaseSystemActivityLogEntry(): SystemActivityLogEntry {
  return { message: "" };
}

export const SystemActivityLogEntry: MessageFns<SystemActivityLogEntry> = {
  encode(message: SystemActivityLogEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.message !== "") {
      writer.uint32(10).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SystemActivityLogEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseSystemActivityLogEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.message = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): SystemActivityLogEntry {
    return { message: isSet(object.message) ? globalThis.String(object.message) : "" };
  },

  toJSON(message: SystemActivityLogEntry): unknown {
    const obj: any = {};
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create(base?: DeepPartial<SystemActivityLogEntry>): SystemActivityLogEntry {
    return SystemActivityLogEntry.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<SystemActivityLogEntry>): SystemActivityLogEntry {
    const message = createBaseSystemActivityLogEntry();
    message.message = object.message ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

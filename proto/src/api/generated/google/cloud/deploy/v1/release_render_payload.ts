// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.0
//   protoc               unknown
// source: google/cloud/deploy/v1/release_render_payload.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";
import { Release_RenderState, release_RenderStateFromJSON, release_RenderStateToJSON } from "./cloud_deploy.js";
import { Type, typeFromJSON, typeToJSON } from "./log_enums.js";

export const protobufPackage = "google.cloud.deploy.v1";

/**
 * Payload proto for "clouddeploy.googleapis.com/release_render"
 * Platform Log event that describes the render status change.
 */
export interface ReleaseRenderEvent {
  /**
   * Debug message for when a render transition occurs. Provides further
   * details as rendering progresses through render states.
   */
  message: string;
  /** Unique identifier of the `DeliveryPipeline`. */
  pipelineUid: string;
  /**
   * The name of the release.
   * release_uid is not in this log message because we write some of these log
   * messages at release creation time, before we've generated the uid.
   */
  release: string;
  /** Type of this notification, e.g. for a release render state change event. */
  type: Type;
  /** The state of the release render. */
  releaseRenderState: Release_RenderState;
}

function createBaseReleaseRenderEvent(): ReleaseRenderEvent {
  return { message: "", pipelineUid: "", release: "", type: 0, releaseRenderState: 0 };
}

export const ReleaseRenderEvent: MessageFns<ReleaseRenderEvent> = {
  encode(message: ReleaseRenderEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.message !== "") {
      writer.uint32(10).string(message.message);
    }
    if (message.pipelineUid !== "") {
      writer.uint32(34).string(message.pipelineUid);
    }
    if (message.release !== "") {
      writer.uint32(18).string(message.release);
    }
    if (message.type !== 0) {
      writer.uint32(40).int32(message.type);
    }
    if (message.releaseRenderState !== 0) {
      writer.uint32(24).int32(message.releaseRenderState);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReleaseRenderEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseReleaseRenderEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.message = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.pipelineUid = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.release = reader.string();
          continue;
        case 5:
          if (tag !== 40) {
            break;
          }

          message.type = reader.int32() as any;
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.releaseRenderState = reader.int32() as any;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ReleaseRenderEvent {
    return {
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      pipelineUid: isSet(object.pipelineUid) ? globalThis.String(object.pipelineUid) : "",
      release: isSet(object.release) ? globalThis.String(object.release) : "",
      type: isSet(object.type) ? typeFromJSON(object.type) : 0,
      releaseRenderState: isSet(object.releaseRenderState) ? release_RenderStateFromJSON(object.releaseRenderState) : 0,
    };
  },

  toJSON(message: ReleaseRenderEvent): unknown {
    const obj: any = {};
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.pipelineUid !== "") {
      obj.pipelineUid = message.pipelineUid;
    }
    if (message.release !== "") {
      obj.release = message.release;
    }
    if (message.type !== 0) {
      obj.type = typeToJSON(message.type);
    }
    if (message.releaseRenderState !== 0) {
      obj.releaseRenderState = release_RenderStateToJSON(message.releaseRenderState);
    }
    return obj;
  },

  create(base?: DeepPartial<ReleaseRenderEvent>): ReleaseRenderEvent {
    return ReleaseRenderEvent.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ReleaseRenderEvent>): ReleaseRenderEvent {
    const message = createBaseReleaseRenderEvent();
    message.message = object.message ?? "";
    message.pipelineUid = object.pipelineUid ?? "";
    message.release = object.release ?? "";
    message.type = object.type ?? 0;
    message.releaseRenderState = object.releaseRenderState ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

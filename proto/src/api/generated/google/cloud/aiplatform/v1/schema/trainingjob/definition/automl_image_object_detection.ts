// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.0
//   protoc               unknown
// source: google/cloud/aiplatform/v1/schema/trainingjob/definition/automl_image_object_detection.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";

export const protobufPackage = "google.cloud.aiplatform.v1.schema.trainingjob.definition";

/** A TrainingJob that trains and uploads an AutoML Image Object Detection Model. */
export interface AutoMlImageObjectDetection {
  /** The input parameters of this TrainingJob. */
  inputs:
    | AutoMlImageObjectDetectionInputs
    | undefined;
  /** The metadata information */
  metadata: AutoMlImageObjectDetectionMetadata | undefined;
}

export interface AutoMlImageObjectDetectionInputs {
  modelType: AutoMlImageObjectDetectionInputs_ModelType;
  /**
   * The training budget of creating this model, expressed in milli node
   * hours i.e. 1,000 value in this field means 1 node hour. The actual
   * metadata.costMilliNodeHours will be equal or less than this value.
   * If further model training ceases to provide any improvements, it will
   * stop without using the full budget and the metadata.successfulStopReason
   * will be `model-converged`.
   * Note, node_hour  = actual_hour * number_of_nodes_involved.
   * For modelType `cloud`(default), the budget must be between 20,000
   * and 900,000 milli node hours, inclusive. The default value is 216,000
   * which represents one day in wall time, considering 9 nodes are used.
   * For model types `mobile-tf-low-latency-1`, `mobile-tf-versatile-1`,
   * `mobile-tf-high-accuracy-1`
   * the training budget must be between 1,000 and 100,000 milli node hours,
   * inclusive. The default value is 24,000 which represents one day in
   * wall time on a single node that is used.
   */
  budgetMilliNodeHours: Long;
  /**
   * Use the entire training budget. This disables the early stopping feature.
   * When false the early stopping feature is enabled, which means that AutoML
   * Image Object Detection might stop training before the entire training
   * budget has been used.
   */
  disableEarlyStopping: boolean;
}

export enum AutoMlImageObjectDetectionInputs_ModelType {
  /** MODEL_TYPE_UNSPECIFIED - Should not be set. */
  MODEL_TYPE_UNSPECIFIED = 0,
  /**
   * CLOUD_HIGH_ACCURACY_1 - A model best tailored to be used within Google Cloud, and which cannot
   * be exported. Expected to have a higher latency, but should also have a
   * higher prediction quality than other cloud models.
   */
  CLOUD_HIGH_ACCURACY_1 = 1,
  /**
   * CLOUD_LOW_LATENCY_1 - A model best tailored to be used within Google Cloud, and which cannot
   * be exported. Expected to have a low latency, but may have lower
   * prediction quality than other cloud models.
   */
  CLOUD_LOW_LATENCY_1 = 2,
  /**
   * MOBILE_TF_LOW_LATENCY_1 - A model that, in addition to being available within Google
   * Cloud can also be exported (see ModelService.ExportModel) and
   * used on a mobile or edge device with TensorFlow afterwards.
   * Expected to have low latency, but may have lower prediction
   * quality than other mobile models.
   */
  MOBILE_TF_LOW_LATENCY_1 = 3,
  /**
   * MOBILE_TF_VERSATILE_1 - A model that, in addition to being available within Google
   * Cloud can also be exported (see ModelService.ExportModel) and
   * used on a mobile or edge device with TensorFlow afterwards.
   */
  MOBILE_TF_VERSATILE_1 = 4,
  /**
   * MOBILE_TF_HIGH_ACCURACY_1 - A model that, in addition to being available within Google
   * Cloud, can also be exported (see ModelService.ExportModel) and
   * used on a mobile or edge device with TensorFlow afterwards.
   * Expected to have a higher latency, but should also have a higher
   * prediction quality than other mobile models.
   */
  MOBILE_TF_HIGH_ACCURACY_1 = 5,
  UNRECOGNIZED = -1,
}

export function autoMlImageObjectDetectionInputs_ModelTypeFromJSON(
  object: any,
): AutoMlImageObjectDetectionInputs_ModelType {
  switch (object) {
    case 0:
    case "MODEL_TYPE_UNSPECIFIED":
      return AutoMlImageObjectDetectionInputs_ModelType.MODEL_TYPE_UNSPECIFIED;
    case 1:
    case "CLOUD_HIGH_ACCURACY_1":
      return AutoMlImageObjectDetectionInputs_ModelType.CLOUD_HIGH_ACCURACY_1;
    case 2:
    case "CLOUD_LOW_LATENCY_1":
      return AutoMlImageObjectDetectionInputs_ModelType.CLOUD_LOW_LATENCY_1;
    case 3:
    case "MOBILE_TF_LOW_LATENCY_1":
      return AutoMlImageObjectDetectionInputs_ModelType.MOBILE_TF_LOW_LATENCY_1;
    case 4:
    case "MOBILE_TF_VERSATILE_1":
      return AutoMlImageObjectDetectionInputs_ModelType.MOBILE_TF_VERSATILE_1;
    case 5:
    case "MOBILE_TF_HIGH_ACCURACY_1":
      return AutoMlImageObjectDetectionInputs_ModelType.MOBILE_TF_HIGH_ACCURACY_1;
    case -1:
    case "UNRECOGNIZED":
    default:
      return AutoMlImageObjectDetectionInputs_ModelType.UNRECOGNIZED;
  }
}

export function autoMlImageObjectDetectionInputs_ModelTypeToJSON(
  object: AutoMlImageObjectDetectionInputs_ModelType,
): string {
  switch (object) {
    case AutoMlImageObjectDetectionInputs_ModelType.MODEL_TYPE_UNSPECIFIED:
      return "MODEL_TYPE_UNSPECIFIED";
    case AutoMlImageObjectDetectionInputs_ModelType.CLOUD_HIGH_ACCURACY_1:
      return "CLOUD_HIGH_ACCURACY_1";
    case AutoMlImageObjectDetectionInputs_ModelType.CLOUD_LOW_LATENCY_1:
      return "CLOUD_LOW_LATENCY_1";
    case AutoMlImageObjectDetectionInputs_ModelType.MOBILE_TF_LOW_LATENCY_1:
      return "MOBILE_TF_LOW_LATENCY_1";
    case AutoMlImageObjectDetectionInputs_ModelType.MOBILE_TF_VERSATILE_1:
      return "MOBILE_TF_VERSATILE_1";
    case AutoMlImageObjectDetectionInputs_ModelType.MOBILE_TF_HIGH_ACCURACY_1:
      return "MOBILE_TF_HIGH_ACCURACY_1";
    case AutoMlImageObjectDetectionInputs_ModelType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface AutoMlImageObjectDetectionMetadata {
  /**
   * The actual training cost of creating this model, expressed in
   * milli node hours, i.e. 1,000 value in this field means 1 node hour.
   * Guaranteed to not exceed inputs.budgetMilliNodeHours.
   */
  costMilliNodeHours: Long;
  /**
   * For successful job completions, this is the reason why the job has
   * finished.
   */
  successfulStopReason: AutoMlImageObjectDetectionMetadata_SuccessfulStopReason;
}

export enum AutoMlImageObjectDetectionMetadata_SuccessfulStopReason {
  /** SUCCESSFUL_STOP_REASON_UNSPECIFIED - Should not be set. */
  SUCCESSFUL_STOP_REASON_UNSPECIFIED = 0,
  /** BUDGET_REACHED - The inputs.budgetMilliNodeHours had been reached. */
  BUDGET_REACHED = 1,
  /**
   * MODEL_CONVERGED - Further training of the Model ceased to increase its quality, since it
   * already has converged.
   */
  MODEL_CONVERGED = 2,
  UNRECOGNIZED = -1,
}

export function autoMlImageObjectDetectionMetadata_SuccessfulStopReasonFromJSON(
  object: any,
): AutoMlImageObjectDetectionMetadata_SuccessfulStopReason {
  switch (object) {
    case 0:
    case "SUCCESSFUL_STOP_REASON_UNSPECIFIED":
      return AutoMlImageObjectDetectionMetadata_SuccessfulStopReason.SUCCESSFUL_STOP_REASON_UNSPECIFIED;
    case 1:
    case "BUDGET_REACHED":
      return AutoMlImageObjectDetectionMetadata_SuccessfulStopReason.BUDGET_REACHED;
    case 2:
    case "MODEL_CONVERGED":
      return AutoMlImageObjectDetectionMetadata_SuccessfulStopReason.MODEL_CONVERGED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return AutoMlImageObjectDetectionMetadata_SuccessfulStopReason.UNRECOGNIZED;
  }
}

export function autoMlImageObjectDetectionMetadata_SuccessfulStopReasonToJSON(
  object: AutoMlImageObjectDetectionMetadata_SuccessfulStopReason,
): string {
  switch (object) {
    case AutoMlImageObjectDetectionMetadata_SuccessfulStopReason.SUCCESSFUL_STOP_REASON_UNSPECIFIED:
      return "SUCCESSFUL_STOP_REASON_UNSPECIFIED";
    case AutoMlImageObjectDetectionMetadata_SuccessfulStopReason.BUDGET_REACHED:
      return "BUDGET_REACHED";
    case AutoMlImageObjectDetectionMetadata_SuccessfulStopReason.MODEL_CONVERGED:
      return "MODEL_CONVERGED";
    case AutoMlImageObjectDetectionMetadata_SuccessfulStopReason.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

function createBaseAutoMlImageObjectDetection(): AutoMlImageObjectDetection {
  return { inputs: undefined, metadata: undefined };
}

export const AutoMlImageObjectDetection: MessageFns<AutoMlImageObjectDetection> = {
  encode(message: AutoMlImageObjectDetection, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.inputs !== undefined) {
      AutoMlImageObjectDetectionInputs.encode(message.inputs, writer.uint32(10).fork()).join();
    }
    if (message.metadata !== undefined) {
      AutoMlImageObjectDetectionMetadata.encode(message.metadata, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AutoMlImageObjectDetection {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAutoMlImageObjectDetection();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.inputs = AutoMlImageObjectDetectionInputs.decode(reader, reader.uint32());
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.metadata = AutoMlImageObjectDetectionMetadata.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AutoMlImageObjectDetection {
    return {
      inputs: isSet(object.inputs) ? AutoMlImageObjectDetectionInputs.fromJSON(object.inputs) : undefined,
      metadata: isSet(object.metadata) ? AutoMlImageObjectDetectionMetadata.fromJSON(object.metadata) : undefined,
    };
  },

  toJSON(message: AutoMlImageObjectDetection): unknown {
    const obj: any = {};
    if (message.inputs !== undefined) {
      obj.inputs = AutoMlImageObjectDetectionInputs.toJSON(message.inputs);
    }
    if (message.metadata !== undefined) {
      obj.metadata = AutoMlImageObjectDetectionMetadata.toJSON(message.metadata);
    }
    return obj;
  },

  create(base?: DeepPartial<AutoMlImageObjectDetection>): AutoMlImageObjectDetection {
    return AutoMlImageObjectDetection.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AutoMlImageObjectDetection>): AutoMlImageObjectDetection {
    const message = createBaseAutoMlImageObjectDetection();
    message.inputs = (object.inputs !== undefined && object.inputs !== null)
      ? AutoMlImageObjectDetectionInputs.fromPartial(object.inputs)
      : undefined;
    message.metadata = (object.metadata !== undefined && object.metadata !== null)
      ? AutoMlImageObjectDetectionMetadata.fromPartial(object.metadata)
      : undefined;
    return message;
  },
};

function createBaseAutoMlImageObjectDetectionInputs(): AutoMlImageObjectDetectionInputs {
  return { modelType: 0, budgetMilliNodeHours: Long.ZERO, disableEarlyStopping: false };
}

export const AutoMlImageObjectDetectionInputs: MessageFns<AutoMlImageObjectDetectionInputs> = {
  encode(message: AutoMlImageObjectDetectionInputs, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.modelType !== 0) {
      writer.uint32(8).int32(message.modelType);
    }
    if (!message.budgetMilliNodeHours.equals(Long.ZERO)) {
      writer.uint32(16).int64(message.budgetMilliNodeHours.toString());
    }
    if (message.disableEarlyStopping !== false) {
      writer.uint32(24).bool(message.disableEarlyStopping);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AutoMlImageObjectDetectionInputs {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAutoMlImageObjectDetectionInputs();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.modelType = reader.int32() as any;
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.budgetMilliNodeHours = Long.fromString(reader.int64().toString());
          continue;
        case 3:
          if (tag !== 24) {
            break;
          }

          message.disableEarlyStopping = reader.bool();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AutoMlImageObjectDetectionInputs {
    return {
      modelType: isSet(object.modelType) ? autoMlImageObjectDetectionInputs_ModelTypeFromJSON(object.modelType) : 0,
      budgetMilliNodeHours: isSet(object.budgetMilliNodeHours)
        ? Long.fromValue(object.budgetMilliNodeHours)
        : Long.ZERO,
      disableEarlyStopping: isSet(object.disableEarlyStopping)
        ? globalThis.Boolean(object.disableEarlyStopping)
        : false,
    };
  },

  toJSON(message: AutoMlImageObjectDetectionInputs): unknown {
    const obj: any = {};
    if (message.modelType !== 0) {
      obj.modelType = autoMlImageObjectDetectionInputs_ModelTypeToJSON(message.modelType);
    }
    if (!message.budgetMilliNodeHours.equals(Long.ZERO)) {
      obj.budgetMilliNodeHours = (message.budgetMilliNodeHours || Long.ZERO).toString();
    }
    if (message.disableEarlyStopping !== false) {
      obj.disableEarlyStopping = message.disableEarlyStopping;
    }
    return obj;
  },

  create(base?: DeepPartial<AutoMlImageObjectDetectionInputs>): AutoMlImageObjectDetectionInputs {
    return AutoMlImageObjectDetectionInputs.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AutoMlImageObjectDetectionInputs>): AutoMlImageObjectDetectionInputs {
    const message = createBaseAutoMlImageObjectDetectionInputs();
    message.modelType = object.modelType ?? 0;
    message.budgetMilliNodeHours = (object.budgetMilliNodeHours !== undefined && object.budgetMilliNodeHours !== null)
      ? Long.fromValue(object.budgetMilliNodeHours)
      : Long.ZERO;
    message.disableEarlyStopping = object.disableEarlyStopping ?? false;
    return message;
  },
};

function createBaseAutoMlImageObjectDetectionMetadata(): AutoMlImageObjectDetectionMetadata {
  return { costMilliNodeHours: Long.ZERO, successfulStopReason: 0 };
}

export const AutoMlImageObjectDetectionMetadata: MessageFns<AutoMlImageObjectDetectionMetadata> = {
  encode(message: AutoMlImageObjectDetectionMetadata, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (!message.costMilliNodeHours.equals(Long.ZERO)) {
      writer.uint32(8).int64(message.costMilliNodeHours.toString());
    }
    if (message.successfulStopReason !== 0) {
      writer.uint32(16).int32(message.successfulStopReason);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AutoMlImageObjectDetectionMetadata {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAutoMlImageObjectDetectionMetadata();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.costMilliNodeHours = Long.fromString(reader.int64().toString());
          continue;
        case 2:
          if (tag !== 16) {
            break;
          }

          message.successfulStopReason = reader.int32() as any;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AutoMlImageObjectDetectionMetadata {
    return {
      costMilliNodeHours: isSet(object.costMilliNodeHours) ? Long.fromValue(object.costMilliNodeHours) : Long.ZERO,
      successfulStopReason: isSet(object.successfulStopReason)
        ? autoMlImageObjectDetectionMetadata_SuccessfulStopReasonFromJSON(object.successfulStopReason)
        : 0,
    };
  },

  toJSON(message: AutoMlImageObjectDetectionMetadata): unknown {
    const obj: any = {};
    if (!message.costMilliNodeHours.equals(Long.ZERO)) {
      obj.costMilliNodeHours = (message.costMilliNodeHours || Long.ZERO).toString();
    }
    if (message.successfulStopReason !== 0) {
      obj.successfulStopReason = autoMlImageObjectDetectionMetadata_SuccessfulStopReasonToJSON(
        message.successfulStopReason,
      );
    }
    return obj;
  },

  create(base?: DeepPartial<AutoMlImageObjectDetectionMetadata>): AutoMlImageObjectDetectionMetadata {
    return AutoMlImageObjectDetectionMetadata.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<AutoMlImageObjectDetectionMetadata>): AutoMlImageObjectDetectionMetadata {
    const message = createBaseAutoMlImageObjectDetectionMetadata();
    message.costMilliNodeHours = (object.costMilliNodeHours !== undefined && object.costMilliNodeHours !== null)
      ? Long.fromValue(object.costMilliNodeHours)
      : Long.ZERO;
    message.successfulStopReason = object.successfulStopReason ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

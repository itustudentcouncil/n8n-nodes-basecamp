// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.0
//   protoc               unknown
// source: google/cloud/aiplatform/v1/manual_batch_tuning_parameters.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";

export const protobufPackage = "google.cloud.aiplatform.v1";

/** Manual batch tuning parameters. */
export interface ManualBatchTuningParameters {
  /**
   * Immutable. The number of the records (e.g. instances) of the operation
   * given in each batch to a machine replica. Machine type, and size of a
   * single record should be considered when setting this parameter, higher
   * value speeds up the batch operation's execution, but too high value will
   * result in a whole batch not fitting in a machine's memory, and the whole
   * operation will fail.
   * The default value is 64.
   */
  batchSize: number;
}

function createBaseManualBatchTuningParameters(): ManualBatchTuningParameters {
  return { batchSize: 0 };
}

export const ManualBatchTuningParameters: MessageFns<ManualBatchTuningParameters> = {
  encode(message: ManualBatchTuningParameters, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.batchSize !== 0) {
      writer.uint32(8).int32(message.batchSize);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ManualBatchTuningParameters {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseManualBatchTuningParameters();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 8) {
            break;
          }

          message.batchSize = reader.int32();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ManualBatchTuningParameters {
    return { batchSize: isSet(object.batchSize) ? globalThis.Number(object.batchSize) : 0 };
  },

  toJSON(message: ManualBatchTuningParameters): unknown {
    const obj: any = {};
    if (message.batchSize !== 0) {
      obj.batchSize = Math.round(message.batchSize);
    }
    return obj;
  },

  create(base?: DeepPartial<ManualBatchTuningParameters>): ManualBatchTuningParameters {
    return ManualBatchTuningParameters.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ManualBatchTuningParameters>): ManualBatchTuningParameters {
    const message = createBaseManualBatchTuningParameters();
    message.batchSize = object.batchSize ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

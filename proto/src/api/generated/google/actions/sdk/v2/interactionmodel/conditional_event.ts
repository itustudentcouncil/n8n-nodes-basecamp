// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.0
//   protoc               unknown
// source: google/actions/sdk/v2/interactionmodel/conditional_event.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";
import { EventHandler } from "./event_handler.js";

export const protobufPackage = "google.actions.sdk.v2.interactionmodel";

/** Registers events that trigger as the result of a true condition. */
export interface ConditionalEvent {
  /**
   * Required. Filter condition for this event to trigger. If condition is evaluated to
   * true then the associated `handler` will be triggered.
   * The following variable references are supported:
   *   `$session` - To reference data in session storage.
   *   `$user` - To reference data in user storage.
   * The following boolean operators are supported (with examples):
   *   `&&` - `session.params.counter > 0 && session.params.counter < 100`
   *   `||` - `session.params.foo == "John" || session.params.counter == "Adam"`
   *   `!`  - `!(session.params.counter == 5)`
   * The following comparisons are supported:
   *   `==`, `!=`, `<`, `>`, `<=`, `>=`
   * The following list and string operators are supported (with examples):
   *   `in`        - "Watermelon" in `session.params.fruitList`
   *   `size`      - `size(session.params.fruitList) > 2`
   *   `substring` - `session.params.fullName.contains("John")`
   */
  condition: string;
  /**
   * Optional. Destination scene which the conversation should jump to when the associated
   * condition is evaluated to true. The state of the current scene is destroyed
   * on the transition.
   */
  transitionToScene: string;
  /**
   * Optional. Event handler which is triggered when the associated condition is evaluated
   * to `true`. Should execute before transitioning to the destination scene.
   * Useful to generate Prompts in response to events.
   */
  handler: EventHandler | undefined;
}

function createBaseConditionalEvent(): ConditionalEvent {
  return { condition: "", transitionToScene: "", handler: undefined };
}

export const ConditionalEvent: MessageFns<ConditionalEvent> = {
  encode(message: ConditionalEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.condition !== "") {
      writer.uint32(10).string(message.condition);
    }
    if (message.transitionToScene !== "") {
      writer.uint32(18).string(message.transitionToScene);
    }
    if (message.handler !== undefined) {
      EventHandler.encode(message.handler, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConditionalEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseConditionalEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.condition = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.transitionToScene = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.handler = EventHandler.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ConditionalEvent {
    return {
      condition: isSet(object.condition) ? globalThis.String(object.condition) : "",
      transitionToScene: isSet(object.transitionToScene) ? globalThis.String(object.transitionToScene) : "",
      handler: isSet(object.handler) ? EventHandler.fromJSON(object.handler) : undefined,
    };
  },

  toJSON(message: ConditionalEvent): unknown {
    const obj: any = {};
    if (message.condition !== "") {
      obj.condition = message.condition;
    }
    if (message.transitionToScene !== "") {
      obj.transitionToScene = message.transitionToScene;
    }
    if (message.handler !== undefined) {
      obj.handler = EventHandler.toJSON(message.handler);
    }
    return obj;
  },

  create(base?: DeepPartial<ConditionalEvent>): ConditionalEvent {
    return ConditionalEvent.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ConditionalEvent>): ConditionalEvent {
    const message = createBaseConditionalEvent();
    message.condition = object.condition ?? "";
    message.transitionToScene = object.transitionToScene ?? "";
    message.handler = (object.handler !== undefined && object.handler !== null)
      ? EventHandler.fromPartial(object.handler)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

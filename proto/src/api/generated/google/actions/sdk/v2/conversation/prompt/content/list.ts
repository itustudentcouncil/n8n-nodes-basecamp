// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.0
//   protoc               unknown
// source: google/actions/sdk/v2/conversation/prompt/content/list.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";

export const protobufPackage = "google.actions.sdk.v2.conversation";

/** A card for presenting a list of options to select from. */
export interface List {
  /** Title of the list. Optional. */
  title: string;
  /** Subtitle of the list. Optional. */
  subtitle: string;
  /** min: 2 max: 30 */
  items: List_ListItem[];
}

/** An item in the list */
export interface List_ListItem {
  /**
   * Required. The NLU key that matches the entry key name in the associated
   * Type.
   */
  key: string;
}

function createBaseList(): List {
  return { title: "", subtitle: "", items: [] };
}

export const List: MessageFns<List> = {
  encode(message: List, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.title !== "") {
      writer.uint32(10).string(message.title);
    }
    if (message.subtitle !== "") {
      writer.uint32(18).string(message.subtitle);
    }
    for (const v of message.items) {
      List_ListItem.encode(v!, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): List {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseList();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.title = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.subtitle = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.items.push(List_ListItem.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): List {
    return {
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      subtitle: isSet(object.subtitle) ? globalThis.String(object.subtitle) : "",
      items: globalThis.Array.isArray(object?.items) ? object.items.map((e: any) => List_ListItem.fromJSON(e)) : [],
    };
  },

  toJSON(message: List): unknown {
    const obj: any = {};
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.subtitle !== "") {
      obj.subtitle = message.subtitle;
    }
    if (message.items?.length) {
      obj.items = message.items.map((e) => List_ListItem.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<List>): List {
    return List.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<List>): List {
    const message = createBaseList();
    message.title = object.title ?? "";
    message.subtitle = object.subtitle ?? "";
    message.items = object.items?.map((e) => List_ListItem.fromPartial(e)) || [];
    return message;
  },
};

function createBaseList_ListItem(): List_ListItem {
  return { key: "" };
}

export const List_ListItem: MessageFns<List_ListItem> = {
  encode(message: List_ListItem, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): List_ListItem {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseList_ListItem();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): List_ListItem {
    return { key: isSet(object.key) ? globalThis.String(object.key) : "" };
  },

  toJSON(message: List_ListItem): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    return obj;
  },

  create(base?: DeepPartial<List_ListItem>): List_ListItem {
    return List_ListItem.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<List_ListItem>): List_ListItem {
    const message = createBaseList_ListItem();
    message.key = object.key ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

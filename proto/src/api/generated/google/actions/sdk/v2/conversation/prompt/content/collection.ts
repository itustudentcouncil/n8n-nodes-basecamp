// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.0
//   protoc               unknown
// source: google/actions/sdk/v2/conversation/prompt/content/collection.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";
import { Image_ImageFill, image_ImageFillFromJSON, image_ImageFillToJSON } from "./image.js";

export const protobufPackage = "google.actions.sdk.v2.conversation";

/** A card for presenting a collection of options to select from. */
export interface Collection {
  /** Title of the collection. Optional. */
  title: string;
  /** Subtitle of the collection. Optional. */
  subtitle: string;
  /** min: 2 max: 10 */
  items: Collection_CollectionItem[];
  /** How the image backgrounds of collection items will be filled. Optional. */
  imageFill: Image_ImageFill;
}

/** An item in the collection */
export interface Collection_CollectionItem {
  /**
   * Required. The NLU key that matches the entry key name in the associated
   * Type.
   */
  key: string;
}

function createBaseCollection(): Collection {
  return { title: "", subtitle: "", items: [], imageFill: 0 };
}

export const Collection: MessageFns<Collection> = {
  encode(message: Collection, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.title !== "") {
      writer.uint32(10).string(message.title);
    }
    if (message.subtitle !== "") {
      writer.uint32(18).string(message.subtitle);
    }
    for (const v of message.items) {
      Collection_CollectionItem.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.imageFill !== 0) {
      writer.uint32(32).int32(message.imageFill);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Collection {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCollection();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.title = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.subtitle = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.items.push(Collection_CollectionItem.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.imageFill = reader.int32() as any;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Collection {
    return {
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      subtitle: isSet(object.subtitle) ? globalThis.String(object.subtitle) : "",
      items: globalThis.Array.isArray(object?.items)
        ? object.items.map((e: any) => Collection_CollectionItem.fromJSON(e))
        : [],
      imageFill: isSet(object.imageFill) ? image_ImageFillFromJSON(object.imageFill) : 0,
    };
  },

  toJSON(message: Collection): unknown {
    const obj: any = {};
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.subtitle !== "") {
      obj.subtitle = message.subtitle;
    }
    if (message.items?.length) {
      obj.items = message.items.map((e) => Collection_CollectionItem.toJSON(e));
    }
    if (message.imageFill !== 0) {
      obj.imageFill = image_ImageFillToJSON(message.imageFill);
    }
    return obj;
  },

  create(base?: DeepPartial<Collection>): Collection {
    return Collection.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Collection>): Collection {
    const message = createBaseCollection();
    message.title = object.title ?? "";
    message.subtitle = object.subtitle ?? "";
    message.items = object.items?.map((e) => Collection_CollectionItem.fromPartial(e)) || [];
    message.imageFill = object.imageFill ?? 0;
    return message;
  },
};

function createBaseCollection_CollectionItem(): Collection_CollectionItem {
  return { key: "" };
}

export const Collection_CollectionItem: MessageFns<Collection_CollectionItem> = {
  encode(message: Collection_CollectionItem, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Collection_CollectionItem {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCollection_CollectionItem();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Collection_CollectionItem {
    return { key: isSet(object.key) ? globalThis.String(object.key) : "" };
  },

  toJSON(message: Collection_CollectionItem): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    return obj;
  },

  create(base?: DeepPartial<Collection_CollectionItem>): Collection_CollectionItem {
    return Collection_CollectionItem.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<Collection_CollectionItem>): Collection_CollectionItem {
    const message = createBaseCollection_CollectionItem();
    message.key = object.key ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

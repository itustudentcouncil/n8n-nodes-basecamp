// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.0
//   protoc               unknown
// source: google/actions/sdk/v2/interactionmodel/prompt/content/static_collection_prompt.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";
import {
  StaticImagePrompt,
  StaticImagePrompt_ImageFill,
  staticImagePrompt_ImageFillFromJSON,
  staticImagePrompt_ImageFillToJSON,
} from "./static_image_prompt.js";

export const protobufPackage = "google.actions.sdk.v2.interactionmodel.prompt";

/** A card for presenting a collection of options to select from. */
export interface StaticCollectionPrompt {
  /** Optional. Title of the collection. */
  title: string;
  /** Optional. Subtitle of the collection. */
  subtitle: string;
  /** Required. Collection items. */
  items: StaticCollectionPrompt_CollectionItem[];
  /** Optional. Type of image display option. */
  imageFill: StaticImagePrompt_ImageFill;
}

/** An item in the collection. */
export interface StaticCollectionPrompt_CollectionItem {
  /**
   * Required. The NLU key that matches the entry key name in the associated
   * Type. When item tapped, this key will be posted back as a select option
   * parameter.
   */
  key: string;
  /**
   * Required. Title of the item. When tapped, this text will be
   * posted back to the conversation verbatim as if the user had typed it.
   * Each title must be unique among the set of items.
   */
  title: string;
  /** Optional. Body text of the item. */
  description: string;
  /** Optional. Item image. */
  image: StaticImagePrompt | undefined;
}

function createBaseStaticCollectionPrompt(): StaticCollectionPrompt {
  return { title: "", subtitle: "", items: [], imageFill: 0 };
}

export const StaticCollectionPrompt: MessageFns<StaticCollectionPrompt> = {
  encode(message: StaticCollectionPrompt, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.title !== "") {
      writer.uint32(10).string(message.title);
    }
    if (message.subtitle !== "") {
      writer.uint32(18).string(message.subtitle);
    }
    for (const v of message.items) {
      StaticCollectionPrompt_CollectionItem.encode(v!, writer.uint32(26).fork()).join();
    }
    if (message.imageFill !== 0) {
      writer.uint32(32).int32(message.imageFill);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StaticCollectionPrompt {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStaticCollectionPrompt();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.title = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.subtitle = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.items.push(StaticCollectionPrompt_CollectionItem.decode(reader, reader.uint32()));
          continue;
        case 4:
          if (tag !== 32) {
            break;
          }

          message.imageFill = reader.int32() as any;
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StaticCollectionPrompt {
    return {
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      subtitle: isSet(object.subtitle) ? globalThis.String(object.subtitle) : "",
      items: globalThis.Array.isArray(object?.items)
        ? object.items.map((e: any) => StaticCollectionPrompt_CollectionItem.fromJSON(e))
        : [],
      imageFill: isSet(object.imageFill) ? staticImagePrompt_ImageFillFromJSON(object.imageFill) : 0,
    };
  },

  toJSON(message: StaticCollectionPrompt): unknown {
    const obj: any = {};
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.subtitle !== "") {
      obj.subtitle = message.subtitle;
    }
    if (message.items?.length) {
      obj.items = message.items.map((e) => StaticCollectionPrompt_CollectionItem.toJSON(e));
    }
    if (message.imageFill !== 0) {
      obj.imageFill = staticImagePrompt_ImageFillToJSON(message.imageFill);
    }
    return obj;
  },

  create(base?: DeepPartial<StaticCollectionPrompt>): StaticCollectionPrompt {
    return StaticCollectionPrompt.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StaticCollectionPrompt>): StaticCollectionPrompt {
    const message = createBaseStaticCollectionPrompt();
    message.title = object.title ?? "";
    message.subtitle = object.subtitle ?? "";
    message.items = object.items?.map((e) => StaticCollectionPrompt_CollectionItem.fromPartial(e)) || [];
    message.imageFill = object.imageFill ?? 0;
    return message;
  },
};

function createBaseStaticCollectionPrompt_CollectionItem(): StaticCollectionPrompt_CollectionItem {
  return { key: "", title: "", description: "", image: undefined };
}

export const StaticCollectionPrompt_CollectionItem: MessageFns<StaticCollectionPrompt_CollectionItem> = {
  encode(message: StaticCollectionPrompt_CollectionItem, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.title !== "") {
      writer.uint32(18).string(message.title);
    }
    if (message.description !== "") {
      writer.uint32(26).string(message.description);
    }
    if (message.image !== undefined) {
      StaticImagePrompt.encode(message.image, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StaticCollectionPrompt_CollectionItem {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStaticCollectionPrompt_CollectionItem();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.title = reader.string();
          continue;
        case 3:
          if (tag !== 26) {
            break;
          }

          message.description = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.image = StaticImagePrompt.decode(reader, reader.uint32());
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StaticCollectionPrompt_CollectionItem {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      title: isSet(object.title) ? globalThis.String(object.title) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      image: isSet(object.image) ? StaticImagePrompt.fromJSON(object.image) : undefined,
    };
  },

  toJSON(message: StaticCollectionPrompt_CollectionItem): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.title !== "") {
      obj.title = message.title;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.image !== undefined) {
      obj.image = StaticImagePrompt.toJSON(message.image);
    }
    return obj;
  },

  create(base?: DeepPartial<StaticCollectionPrompt_CollectionItem>): StaticCollectionPrompt_CollectionItem {
    return StaticCollectionPrompt_CollectionItem.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StaticCollectionPrompt_CollectionItem>): StaticCollectionPrompt_CollectionItem {
    const message = createBaseStaticCollectionPrompt_CollectionItem();
    message.key = object.key ?? "";
    message.title = object.title ?? "";
    message.description = object.description ?? "";
    message.image = (object.image !== undefined && object.image !== null)
      ? StaticImagePrompt.fromPartial(object.image)
      : undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

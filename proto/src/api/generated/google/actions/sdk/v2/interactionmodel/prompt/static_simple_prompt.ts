// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.0
//   protoc               unknown
// source: google/actions/sdk/v2/interactionmodel/prompt/static_simple_prompt.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";

export const protobufPackage = "google.actions.sdk.v2.interactionmodel.prompt";

/** Represents a simple prompt to be send to a user. */
export interface StaticSimplePrompt {
  /** List of possible variants. */
  variants: StaticSimplePrompt_Variant[];
}

/** Represents a variant which is part of the simple prompt. */
export interface StaticSimplePrompt_Variant {
  /**
   * Optional. Represents the speech to be spoken to the user.  Can be SSML or text to
   * speech.
   * By default, speech will be appended to previous Simple prompt's
   * speech. If the `override` field in the containing prompt is `true` the
   * speech defined in this field will override previous Simple prompt's
   * speech.
   */
  speech: string;
  /**
   * Optional. Text to display in the chat bubble. If not given, a display rendering of
   * the speech field above will be used. Limited to 640 chars.
   * By default, text will be appended to previous Simple prompt's text.
   * If the `override` field in the containing prompt is `true` the text
   * defined in this field will override previous Simple prompt's text.
   */
  text: string;
}

function createBaseStaticSimplePrompt(): StaticSimplePrompt {
  return { variants: [] };
}

export const StaticSimplePrompt: MessageFns<StaticSimplePrompt> = {
  encode(message: StaticSimplePrompt, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.variants) {
      StaticSimplePrompt_Variant.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StaticSimplePrompt {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStaticSimplePrompt();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.variants.push(StaticSimplePrompt_Variant.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StaticSimplePrompt {
    return {
      variants: globalThis.Array.isArray(object?.variants)
        ? object.variants.map((e: any) => StaticSimplePrompt_Variant.fromJSON(e))
        : [],
    };
  },

  toJSON(message: StaticSimplePrompt): unknown {
    const obj: any = {};
    if (message.variants?.length) {
      obj.variants = message.variants.map((e) => StaticSimplePrompt_Variant.toJSON(e));
    }
    return obj;
  },

  create(base?: DeepPartial<StaticSimplePrompt>): StaticSimplePrompt {
    return StaticSimplePrompt.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StaticSimplePrompt>): StaticSimplePrompt {
    const message = createBaseStaticSimplePrompt();
    message.variants = object.variants?.map((e) => StaticSimplePrompt_Variant.fromPartial(e)) || [];
    return message;
  },
};

function createBaseStaticSimplePrompt_Variant(): StaticSimplePrompt_Variant {
  return { speech: "", text: "" };
}

export const StaticSimplePrompt_Variant: MessageFns<StaticSimplePrompt_Variant> = {
  encode(message: StaticSimplePrompt_Variant, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.speech !== "") {
      writer.uint32(10).string(message.speech);
    }
    if (message.text !== "") {
      writer.uint32(18).string(message.text);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StaticSimplePrompt_Variant {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStaticSimplePrompt_Variant();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.speech = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.text = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StaticSimplePrompt_Variant {
    return {
      speech: isSet(object.speech) ? globalThis.String(object.speech) : "",
      text: isSet(object.text) ? globalThis.String(object.text) : "",
    };
  },

  toJSON(message: StaticSimplePrompt_Variant): unknown {
    const obj: any = {};
    if (message.speech !== "") {
      obj.speech = message.speech;
    }
    if (message.text !== "") {
      obj.text = message.text;
    }
    return obj;
  },

  create(base?: DeepPartial<StaticSimplePrompt_Variant>): StaticSimplePrompt_Variant {
    return StaticSimplePrompt_Variant.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<StaticSimplePrompt_Variant>): StaticSimplePrompt_Variant {
    const message = createBaseStaticSimplePrompt_Variant();
    message.speech = object.speech ?? "";
    message.text = object.text ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.0
//   protoc               unknown
// source: google/chat/v1/thread_read_state.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";
import { Timestamp } from "../../protobuf/timestamp.js";

export const protobufPackage = "google.chat.v1";

/**
 * A user's read state within a thread, used to identify read and unread
 * messages.
 */
export interface ThreadReadState {
  /**
   * Resource name of the thread read state.
   *
   * Format: `users/{user}/spaces/{space}/threads/{thread}/threadReadState`
   */
  name: string;
  /**
   * The time when the user's thread read state was updated. Usually this
   * corresponds with the timestamp of the last read message in a thread.
   */
  lastReadTime: Date | undefined;
}

/** Request message for GetThreadReadStateRequest API. */
export interface GetThreadReadStateRequest {
  /**
   * Required. Resource name of the thread read state to retrieve.
   *
   * Only supports getting read state for the calling user.
   *
   * To refer to the calling user, set one of the following:
   *
   * - The `me` alias. For example,
   * `users/me/spaces/{space}/threads/{thread}/threadReadState`.
   *
   * - Their Workspace email address. For example,
   * `users/user@example.com/spaces/{space}/threads/{thread}/threadReadState`.
   *
   * - Their user id. For example,
   * `users/123456789/spaces/{space}/threads/{thread}/threadReadState`.
   *
   * Format: users/{user}/spaces/{space}/threads/{thread}/threadReadState
   */
  name: string;
}

function createBaseThreadReadState(): ThreadReadState {
  return { name: "", lastReadTime: undefined };
}

export const ThreadReadState: MessageFns<ThreadReadState> = {
  encode(message: ThreadReadState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    if (message.lastReadTime !== undefined) {
      Timestamp.encode(toTimestamp(message.lastReadTime), writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ThreadReadState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseThreadReadState();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.lastReadTime = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ThreadReadState {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      lastReadTime: isSet(object.lastReadTime) ? fromJsonTimestamp(object.lastReadTime) : undefined,
    };
  },

  toJSON(message: ThreadReadState): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.lastReadTime !== undefined) {
      obj.lastReadTime = message.lastReadTime.toISOString();
    }
    return obj;
  },

  create(base?: DeepPartial<ThreadReadState>): ThreadReadState {
    return ThreadReadState.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<ThreadReadState>): ThreadReadState {
    const message = createBaseThreadReadState();
    message.name = object.name ?? "";
    message.lastReadTime = object.lastReadTime ?? undefined;
    return message;
  },
};

function createBaseGetThreadReadStateRequest(): GetThreadReadStateRequest {
  return { name: "" };
}

export const GetThreadReadStateRequest: MessageFns<GetThreadReadStateRequest> = {
  encode(message: GetThreadReadStateRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetThreadReadStateRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetThreadReadStateRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.name = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetThreadReadStateRequest {
    return { name: isSet(object.name) ? globalThis.String(object.name) : "" };
  },

  toJSON(message: GetThreadReadStateRequest): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    return obj;
  },

  create(base?: DeepPartial<GetThreadReadStateRequest>): GetThreadReadStateRequest {
    return GetThreadReadStateRequest.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<GetThreadReadStateRequest>): GetThreadReadStateRequest {
    const message = createBaseGetThreadReadStateRequest();
    message.name = object.name ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function toTimestamp(date: Date): Timestamp {
  const seconds = numberToLong(Math.trunc(date.getTime() / 1_000));
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds.toNumber() || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function numberToLong(number: number) {
  return Long.fromNumber(number);
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.0
//   protoc               unknown
// source: google/ads/googleads/v16/resources/per_store_view.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";

export const protobufPackage = "google.ads.googleads.v16.resources";

/**
 * A per store view.
 * This view provides per store impression reach and local action conversion
 * stats for advertisers.
 */
export interface PerStoreView {
  /**
   * Output only. The resource name of the per store view.
   * Per Store view resource names have the form:
   *
   * `customers/{customer_id}/perStoreViews/{place_id}`
   */
  resourceName: string;
  /** Output only. The place ID of the per store view. */
  placeId: string;
}

function createBasePerStoreView(): PerStoreView {
  return { resourceName: "", placeId: "" };
}

export const PerStoreView: MessageFns<PerStoreView> = {
  encode(message: PerStoreView, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.resourceName !== "") {
      writer.uint32(10).string(message.resourceName);
    }
    if (message.placeId !== "") {
      writer.uint32(18).string(message.placeId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PerStoreView {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePerStoreView();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.resourceName = reader.string();
          continue;
        case 2:
          if (tag !== 18) {
            break;
          }

          message.placeId = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PerStoreView {
    return {
      resourceName: isSet(object.resourceName) ? globalThis.String(object.resourceName) : "",
      placeId: isSet(object.placeId) ? globalThis.String(object.placeId) : "",
    };
  },

  toJSON(message: PerStoreView): unknown {
    const obj: any = {};
    if (message.resourceName !== "") {
      obj.resourceName = message.resourceName;
    }
    if (message.placeId !== "") {
      obj.placeId = message.placeId;
    }
    return obj;
  },

  create(base?: DeepPartial<PerStoreView>): PerStoreView {
    return PerStoreView.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<PerStoreView>): PerStoreView {
    const message = createBasePerStoreView();
    message.resourceName = object.resourceName ?? "";
    message.placeId = object.placeId ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}

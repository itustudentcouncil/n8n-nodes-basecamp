// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.2.0
//   protoc               unknown
// source: google/ads/googleads/v16/resources/customer_label.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import Long from "long";

export const protobufPackage = "google.ads.googleads.v16.resources";

/**
 * Represents a relationship between a customer and a label. This customer may
 * not have access to all the labels attached to it. Additional CustomerLabels
 * may be returned by increasing permissions with login-customer-id.
 */
export interface CustomerLabel {
  /**
   * Immutable. Name of the resource.
   * Customer label resource names have the form:
   * `customers/{customer_id}/customerLabels/{label_id}`
   */
  resourceName: string;
  /**
   * Output only. The resource name of the customer to which the label is
   * attached. Read only.
   */
  customer?:
    | string
    | undefined;
  /**
   * Output only. The resource name of the label assigned to the customer.
   *
   * Note: the Customer ID portion of the label resource name is not
   * validated when creating a new CustomerLabel.
   */
  label?: string | undefined;
}

function createBaseCustomerLabel(): CustomerLabel {
  return { resourceName: "", customer: undefined, label: undefined };
}

export const CustomerLabel: MessageFns<CustomerLabel> = {
  encode(message: CustomerLabel, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.resourceName !== "") {
      writer.uint32(10).string(message.resourceName);
    }
    if (message.customer !== undefined) {
      writer.uint32(34).string(message.customer);
    }
    if (message.label !== undefined) {
      writer.uint32(42).string(message.label);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CustomerLabel {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    let end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCustomerLabel();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1:
          if (tag !== 10) {
            break;
          }

          message.resourceName = reader.string();
          continue;
        case 4:
          if (tag !== 34) {
            break;
          }

          message.customer = reader.string();
          continue;
        case 5:
          if (tag !== 42) {
            break;
          }

          message.label = reader.string();
          continue;
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CustomerLabel {
    return {
      resourceName: isSet(object.resourceName) ? globalThis.String(object.resourceName) : "",
      customer: isSet(object.customer) ? globalThis.String(object.customer) : undefined,
      label: isSet(object.label) ? globalThis.String(object.label) : undefined,
    };
  },

  toJSON(message: CustomerLabel): unknown {
    const obj: any = {};
    if (message.resourceName !== "") {
      obj.resourceName = message.resourceName;
    }
    if (message.customer !== undefined) {
      obj.customer = message.customer;
    }
    if (message.label !== undefined) {
      obj.label = message.label;
    }
    return obj;
  },

  create(base?: DeepPartial<CustomerLabel>): CustomerLabel {
    return CustomerLabel.fromPartial(base ?? {});
  },
  fromPartial(object: DeepPartial<CustomerLabel>): CustomerLabel {
    const message = createBaseCustomerLabel();
    message.resourceName = object.resourceName ?? "";
    message.customer = object.customer ?? undefined;
    message.label = object.label ?? undefined;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends Long ? string | number | Long : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create(base?: DeepPartial<T>): T;
  fromPartial(object: DeepPartial<T>): T;
}
